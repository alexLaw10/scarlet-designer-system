/* eslint-disable */
/* tslint:disable */
/**
 * This is an autogenerated file created by the Stencil compiler.
 * It contains typing information for all components that exist in this project.
 */
import { HTMLStencilElement, JSXBase } from "@stencil/core/internal";
import { ScarletAccordionItem } from "./components/navigation/scarlet-accordion/scarlet-accordion";
import { ScarletAlertStatus, ScarletAlertVariant } from "./components/feedback/scarlet-alert/scarlet-alert";
import { Alignment, Color, Direction, JustifyContent, Size, TextAlign, TextColor, Variant, Wrap } from "./types/index";
import { ScarletAvatarShape } from "./components/data-display/scarlet-avatar/scarlet-avatar";
import { ScarletBadgeSize, ScarletBadgeVariant } from "./components/feedback/scarlet-badge/scarlet-badge";
import { ScarletBreadcrumbItem } from "./components/navigation/scarlet-breadcrumb/scarlet-breadcrumb";
import { ScarletCardVariant } from "./components/data-display/scarlet-card/scarlet-card";
import { ScarletChipVariant } from "./components/data-display/scarlet-chip/scarlet-chip";
import { ScarletComboboxOption } from "./components/form/scarlet-combobox/scarlet-combobox";
import { ScarletContainerMaxWidth } from "./components/layout/scarlet-container/scarlet-container";
import { ScarletDateRangeChange } from "./components/form-masked/scarlet-date-range-picker/scarlet-date-range-picker";
import { ScarletDividerOrientation } from "./components/data-display/scarlet-divider/scarlet-divider";
import { ScarletDrawerPlacement, ScarletDrawerSize } from "./components/feedback/scarlet-drawer/scarlet-drawer";
import { ScarletIconName } from "./components/foundation/scarlet-icon/icons";
import { ScarletHeadingLevel, ScarletHeadingVariant } from "./components/foundation/scarlet-heading/scarlet-heading";
import { ScarletIconName as ScarletIconName1 } from "./components/foundation/scarlet-icon/icons";
import { ScarletInputType } from "./components/form/scarlet-input/scarlet-input";
import { CreditCardBrand } from "./utils/validators";
import { ScarletDocumentType } from "./components/form-masked/scarlet-input-document/scarlet-input-document";
import { ScarletLicensePlateFormat } from "./components/form-masked/scarlet-input-license-plate/scarlet-input-license-plate";
import { ScarletLinkUnderline } from "./components/foundation/scarlet-link/scarlet-link";
import { ScarletMenuItem } from "./components/navigation/scarlet-menu/scarlet-menu";
import { ScarletModalSize } from "./components/feedback/scarlet-modal/scarlet-modal";
import { ScarletPopoverPlacement, ScarletPopoverTriggerMode } from "./components/feedback/scarlet-popover/scarlet-popover";
import { ScarletProgressSize } from "./components/feedback/scarlet-progress/scarlet-progress";
import { ScarletSelectOption } from "./components/form/scarlet-select/scarlet-select";
import { ScarletSkeletonVariant } from "./components/feedback/scarlet-skeleton/scarlet-skeleton";
import { ScarletSpinnerSize, ScarletSpinnerVariant } from "./components/feedback/scarlet-spinner/scarlet-spinner";
import { ScarletStatTrend } from "./components/data-display/scarlet-stat/scarlet-stat";
import { ScarletTableColumn, ScarletTableRow, ScarletTableSortChange } from "./components/data-display/scarlet-table/scarlet-table";
import { ScarletTabItem } from "./components/navigation/scarlet-tabs/scarlet-tabs";
import { ScarletTextAs, ScarletTextVariant, ScarletTextWeight } from "./components/foundation/scarlet-text/scarlet-text";
import { ScarletTextareaResize } from "./components/form/scarlet-textarea/scarlet-textarea";
import { ScarletTimelineItem } from "./components/data-display/scarlet-timeline/scarlet-timeline";
import { ScarletToastPosition } from "./components/feedback/scarlet-toast/scarlet-toast";
import { ScarletTooltipPlacement } from "./components/feedback/scarlet-tooltip/scarlet-tooltip";
export { ScarletAccordionItem } from "./components/navigation/scarlet-accordion/scarlet-accordion";
export { ScarletAlertStatus, ScarletAlertVariant } from "./components/feedback/scarlet-alert/scarlet-alert";
export { Alignment, Color, Direction, JustifyContent, Size, TextAlign, TextColor, Variant, Wrap } from "./types/index";
export { ScarletAvatarShape } from "./components/data-display/scarlet-avatar/scarlet-avatar";
export { ScarletBadgeSize, ScarletBadgeVariant } from "./components/feedback/scarlet-badge/scarlet-badge";
export { ScarletBreadcrumbItem } from "./components/navigation/scarlet-breadcrumb/scarlet-breadcrumb";
export { ScarletCardVariant } from "./components/data-display/scarlet-card/scarlet-card";
export { ScarletChipVariant } from "./components/data-display/scarlet-chip/scarlet-chip";
export { ScarletComboboxOption } from "./components/form/scarlet-combobox/scarlet-combobox";
export { ScarletContainerMaxWidth } from "./components/layout/scarlet-container/scarlet-container";
export { ScarletDateRangeChange } from "./components/form-masked/scarlet-date-range-picker/scarlet-date-range-picker";
export { ScarletDividerOrientation } from "./components/data-display/scarlet-divider/scarlet-divider";
export { ScarletDrawerPlacement, ScarletDrawerSize } from "./components/feedback/scarlet-drawer/scarlet-drawer";
export { ScarletIconName } from "./components/foundation/scarlet-icon/icons";
export { ScarletHeadingLevel, ScarletHeadingVariant } from "./components/foundation/scarlet-heading/scarlet-heading";
export { ScarletIconName as ScarletIconName1 } from "./components/foundation/scarlet-icon/icons";
export { ScarletInputType } from "./components/form/scarlet-input/scarlet-input";
export { CreditCardBrand } from "./utils/validators";
export { ScarletDocumentType } from "./components/form-masked/scarlet-input-document/scarlet-input-document";
export { ScarletLicensePlateFormat } from "./components/form-masked/scarlet-input-license-plate/scarlet-input-license-plate";
export { ScarletLinkUnderline } from "./components/foundation/scarlet-link/scarlet-link";
export { ScarletMenuItem } from "./components/navigation/scarlet-menu/scarlet-menu";
export { ScarletModalSize } from "./components/feedback/scarlet-modal/scarlet-modal";
export { ScarletPopoverPlacement, ScarletPopoverTriggerMode } from "./components/feedback/scarlet-popover/scarlet-popover";
export { ScarletProgressSize } from "./components/feedback/scarlet-progress/scarlet-progress";
export { ScarletSelectOption } from "./components/form/scarlet-select/scarlet-select";
export { ScarletSkeletonVariant } from "./components/feedback/scarlet-skeleton/scarlet-skeleton";
export { ScarletSpinnerSize, ScarletSpinnerVariant } from "./components/feedback/scarlet-spinner/scarlet-spinner";
export { ScarletStatTrend } from "./components/data-display/scarlet-stat/scarlet-stat";
export { ScarletTableColumn, ScarletTableRow, ScarletTableSortChange } from "./components/data-display/scarlet-table/scarlet-table";
export { ScarletTabItem } from "./components/navigation/scarlet-tabs/scarlet-tabs";
export { ScarletTextAs, ScarletTextVariant, ScarletTextWeight } from "./components/foundation/scarlet-text/scarlet-text";
export { ScarletTextareaResize } from "./components/form/scarlet-textarea/scarlet-textarea";
export { ScarletTimelineItem } from "./components/data-display/scarlet-timeline/scarlet-timeline";
export { ScarletToastPosition } from "./components/feedback/scarlet-toast/scarlet-toast";
export { ScarletTooltipPlacement } from "./components/feedback/scarlet-tooltip/scarlet-tooltip";
export namespace Components {
    /**
     * A set of collapsible sections. Panel content is projected via a named
     * slot per item, matching `item.value`.
     */
    interface ScarletAccordion {
        /**
          * Values of the currently expanded sections.
          * @default []
         */
        "expandedValues": string[];
        /**
          * The sections to render.
          * @default []
         */
        "items": ScarletAccordionItem[];
        /**
          * Allows more than one section to be expanded at the same time.
          * @default false
         */
        "multiple": false;
    }
    /**
     * An inline status message with optional title, icon and dismiss action.
     */
    interface ScarletAlert {
        /**
          * Shows a dismiss (close) button.
          * @default false
         */
        "dismissible": false;
        /**
          * Shows a status icon before the content.
          * @default true
         */
        "icon": true;
        /**
          * Semantic status of the alert.
          * @default 'info'
         */
        "status": ScarletAlertStatus;
        /**
          * Visual style of the alert.
          * @default 'soft'
         */
        "variant": ScarletAlertVariant;
    }
    /**
     * A user/entity avatar: shows an image, falling back to initials derived
     * from `name`, falling back to a generic icon.
     */
    interface ScarletAvatar {
        /**
          * Accessible alt text for the image. Defaults to `name` when omitted.
         */
        "alt"?: string;
        /**
          * Full name used to derive initials and the default alt text.
         */
        "name"?: string;
        /**
          * Shape of the avatar.
          * @default 'circle'
         */
        "shape": ScarletAvatarShape;
        /**
          * Size of the avatar.
          * @default 'md'
         */
        "size": Size;
        /**
          * Image URL.
         */
        "src"?: string;
    }
    /**
     * Overlaps a set of `<scarlet-avatar>` children into a stack, with an
     * optional "+N" indicator when there are more than `max`. Propagates `size`
     * down to every avatar so they all render at the same size regardless of
     * what each one was given individually.
     */
    interface ScarletAvatarGroup {
        /**
          * Caps how many avatars are shown before the rest collapse into a "+N" indicator. Unset shows every avatar.
         */
        "max"?: number;
        /**
          * Size applied to every avatar in the group.
          * @default 'md'
         */
        "size": Size;
    }
    /**
     * A small status/count label, or a bare notification dot.
     */
    interface ScarletBadge {
        /**
          * Semantic color of the badge.
          * @default 'primary'
         */
        "color": Color;
        /**
          * Renders as a small notification dot instead of a text pill.
          * @default false
         */
        "dot": false;
        /**
          * Size of the badge.
          * @default 'sm'
         */
        "size": ScarletBadgeSize;
        /**
          * Visual style of the badge.
          * @default 'solid'
         */
        "variant": ScarletBadgeVariant;
    }
    /**
     * A trail of links showing the current page's position in a hierarchy,
     * following the WAI-ARIA breadcrumb pattern (`<nav aria-label>` wrapping an
     * ordered list, the last item marked `aria-current="page"`).
     * Items render as real `<a href>` elements — usable with no JS at all — but
     * every click first emits a cancelable `scarletNavigate`, so a framework
     * router can call `preventDefault()` and handle the navigation itself
     * instead of a full page load.
     */
    interface ScarletBreadcrumb {
        /**
          * Accessible label for the `<nav>` landmark.
          * @default 'Navegação estrutural'
         */
        "ariaLabel": "Navegação estrutural";
        /**
          * The trail of items, in order from the root down to the current page.
          * @default []
         */
        "items": ScarletBreadcrumbItem[];
        /**
          * Text rendered between items. Purely decorative (hidden from assistive tech) — the DOM order already conveys the hierarchy.
          * @default '›'
         */
        "separator": "›";
    }
    /**
     * A clickable action element with solid, outline, ghost and link variants.
     * An icon (e.g. `<scarlet-icon>`) goes in the `start`/`end` slot alongside
     * the default slot's text — `<scarlet-button><scarlet-icon slot="start"
     * name="check" />Salvar</scarlet-button>`. For an icon with no visible text
     * at all, add `iconOnly` (which turns the button square instead of its
     * usual text-driven width) and `ariaLabel` (required then — nothing else
     * gives the button an accessible name).
     */
    interface ScarletButton {
        /**
          * Accessible label. Required when the button has no visible text (icon-only buttons).
         */
        "ariaLabel"?: string;
        /**
          * Semantic color of the button.
          * @default 'primary'
         */
        "color": Color;
        /**
          * Disables the button, preventing interaction and the `scarletClick` event.
          * @default false
         */
        "disabled": false;
        /**
          * Stretches the button to fill the width of its container.
          * @default false
         */
        "fullWidth": false;
        /**
          * Makes the button square (width matches its height) instead of sized to its text — for a button whose only content is an icon. Set `ariaLabel` alongside it.
          * @default false
         */
        "iconOnly": false;
        /**
          * Shows a loading spinner and blocks interaction, without changing layout width.
          * @default false
         */
        "loading": false;
        /**
          * Size of the button.
          * @default 'md'
         */
        "size": Size;
        /**
          * Native `type` attribute passed to the underlying `<button>`.
          * @default 'button'
         */
        "type": 'button' | 'submit' | 'reset';
        /**
          * Visual style of the button.
          * @default 'solid'
         */
        "variant": Variant;
    }
    /**
     * A container for grouping related content, with optional header/footer slots.
     */
    interface ScarletCard {
        /**
          * Makes the whole card behave as a single clickable/keyboard-activatable control.
          * @default false
         */
        "interactive": false;
        /**
          * Internal padding of the card body.
          * @default 'md'
         */
        "padding": Size;
        /**
          * Visual style of the card.
          * @default 'elevated'
         */
        "variant": ScarletCardVariant;
    }
    /**
     * A checkbox input with label, indeterminate state and accessible wiring.
     */
    interface ScarletCheckbox {
        /**
          * Whether the checkbox is checked.
          * @default false
         */
        "checked": boolean;
        /**
          * Disables the checkbox.
          * @default false
         */
        "disabled": false;
        /**
          * Shows a visual "partially checked" state, independent of `checked`.
          * @default false
         */
        "indeterminate": false;
        /**
          * Visible label rendered next to the checkbox.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Marks the checkbox as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Value submitted with a parent form when checked.
         */
        "value"?: string;
    }
    /**
     * Groups a set of `<scarlet-checkbox>` options and coordinates which ones
     * are checked as a single `value` array — unlike `<scarlet-radio-group>`,
     * each checkbox keeps its own native Tab stop (WAI-ARIA's `group` role, not
     * `radiogroup`, doesn't call for roving tabindex/arrow-key navigation the
     * way a radio group does).
     */
    interface ScarletCheckboxGroup {
        /**
          * Accessible label for the group, when there is no visible heading nearby.
         */
        "ariaLabel"?: string;
        /**
          * Disables every checkbox in the group.
          * @default false
         */
        "disabled": false;
        /**
          * Lays the checkboxes out in a row instead of stacked.
          * @default false
         */
        "horizontal": false;
        /**
          * Name applied to every checkbox in the group, so they submit together in a form.
         */
        "name"?: string;
        /**
          * Values of the currently checked checkboxes.
          * @default []
         */
        "value": string[];
    }
    /**
     * A small, dismissible tag — for an active filter, a selected option, an
     * added item in a multi-value input. Not a `<scarlet-badge>` with a close
     * button bolted on: unlike a badge, this is meant to be interactive.
     */
    interface ScarletChip {
        /**
          * Semantic color of the chip.
          * @default 'neutral'
         */
        "color": Color;
        /**
          * Disables the remove button and dims the chip.
          * @default false
         */
        "disabled": false;
        /**
          * Shows a remove ("x") button.
          * @default false
         */
        "removable": false;
        /**
          * Visual style of the chip.
          * @default 'soft'
         */
        "variant": ScarletChipVariant;
    }
    /**
     * A searchable select — type to filter a list of options, following the
     * WAI-ARIA 1.2 "editable combobox with list autocomplete" pattern. Unlike
     * `scarlet-menu`/`scarlet-date-picker`'s popovers, DOM focus never leaves
     * the text input while the list is open: the highlighted option is tracked
     * with `aria-activedescendant` instead of moving real focus button-to-
     * button, so the user can keep typing without ever losing their place.
     * Known limitation: like `scarlet-tooltip`/`scarlet-date-picker`/
     * `scarlet-menu`, positioning is plain CSS anchored to the field — it
     * doesn't flip or reposition to stay in the viewport.
     */
    interface ScarletCombobox {
        /**
          * Disables the field.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the field. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the field. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the field as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the field.
         */
        "label"?: string;
        /**
          * Message shown in the list when no option matches the current query.
          * @default 'Nenhum resultado encontrado.'
         */
        "noResultsMessage": "Nenhum resultado encontrado.";
        /**
          * The options to search through.
          * @default []
         */
        "options": ScarletComboboxOption[];
        /**
          * Placeholder text shown when nothing is typed.
         */
        "placeholder"?: string;
        /**
          * Marks the field as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal text input.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the field.
          * @default 'md'
         */
        "size": Size;
        /**
          * Value of the currently selected option.
          * @default ''
         */
        "value": string;
    }
    /**
     * A centered, max-width content wrapper — the outermost layout primitive
     * for page sections.
     */
    interface ScarletContainer {
        /**
          * Centers the container horizontally within its parent.
          * @default true
         */
        "center": true;
        /**
          * Maximum width of the container.
          * @default 'lg'
         */
        "maxWidth": ScarletContainerMaxWidth;
        /**
          * Adds horizontal padding.
          * @default true
         */
        "padding": true;
    }
    /**
     * A `DD/MM/AAAA` date input (typing, masking and calendar validation shared
     * with `scarlet-input-date`) plus a calendar popover for picking a date
     * visually — a button next to the field opens a month grid; arrow keys move
     * within it (Home/End jump to the start/end of the week, PageUp/PageDown
     * change month, Shift+PageUp/PageDown change year), Enter/Space picks the
     * focused day, Escape closes and returns focus to the toggle button.
     * Known limitations:
     * - The popover closes on Escape, on picking a day, or on clicking outside
     *   it — not on Tab-ing past its last focusable element. A keyboard user who
     *   tabs out instead of pressing Escape will move focus past the component
     *   with the panel still visually open.
     * - Like `scarlet-tooltip`, positioning is plain CSS anchored to the host —
     *   it never flips or shifts to stay in the viewport. It's shrunk to never
     *   exceed the viewport's own width, so it can't run off the right edge of
     *   the *screen*, but placing the field near that edge can still overflow
     *   past its own container.
     */
    interface ScarletDatePicker {
        /**
          * Disables the field and the calendar toggle.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the field. Takes priority over automatic calendar validation errors.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the field. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Closes the calendar popover.
         */
        "hide": () => Promise<void>;
        /**
          * Marks the field as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Whether the current value is a complete, real calendar date.
         */
        "isValid": () => Promise<boolean>;
        /**
          * Visible label rendered above the field.
         */
        "label"?: string;
        /**
          * Latest selectable date, as `DD/MM/AAAA`. Days after it render disabled in the calendar.
         */
        "max"?: string;
        /**
          * Earliest selectable date, as `DD/MM/AAAA`. Days before it render disabled in the calendar (typing them is still possible; `validate` doesn't enforce range).
         */
        "min"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'DD/MM/AAAA'
         */
        "placeholder": "DD/MM/AAAA";
        /**
          * Marks the field as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal text input.
         */
        "setFocus": () => Promise<void>;
        /**
          * Opens the calendar popover.
         */
        "show": () => Promise<void>;
        /**
          * Size of the field.
          * @default 'md'
         */
        "size": Size;
        /**
          * The value as a native `Date`, or `undefined` if it isn't a complete valid date.
         */
        "toDate": () => Promise<Date | undefined>;
        /**
          * Validates the value is a real calendar date on blur once it's complete (8 digits), showing a default "Data inválida" message when it isn't — unless `errorMessage` is already set, which always wins.
          * @default true
         */
        "validate": true;
        /**
          * Current formatted value, e.g. `31/12/2026`.
          * @default ''
         */
        "value": string;
    }
    /**
     * Two `DD/MM/AAAA` fields (start/end) sharing one calendar popover for
     * picking both ends of a range — built the same way `scarlet-date-picker`
     * is (same masking/validation per field, same popover mechanics), doubled.
     * See that component's own doc comment for the shared known limitations
     * (no Tab-out auto-close, no viewport flip).
     * Picking works the classic two-click way: the first day clicked becomes
     * the start (clearing any previous end); the second becomes the end and
     * closes the popover — unless it's *before* the start, in which case it
     * becomes the new start instead and the popover stays open for the end.
     */
    interface ScarletDateRangePicker {
        /**
          * Disables both fields and the calendar toggle.
          * @default false
         */
        "disabled": false;
        /**
          * Placeholder for the end field.
          * @default 'Data final'
         */
        "endPlaceholder": "Data final";
        /**
          * End of the range, as `DD/MM/AAAA`.
          * @default ''
         */
        "endValue": string;
        /**
          * Error message rendered below the fields.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the fields. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Closes the calendar popover.
         */
        "hide": () => Promise<void>;
        /**
          * Marks the fields as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above both fields.
         */
        "label"?: string;
        /**
          * Latest selectable date, as `DD/MM/AAAA`.
         */
        "max"?: string;
        /**
          * Earliest selectable date, as `DD/MM/AAAA`.
         */
        "min"?: string;
        /**
          * Marks the fields as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the start field.
         */
        "setFocus": () => Promise<void>;
        /**
          * Opens the calendar popover.
         */
        "show": () => Promise<void>;
        /**
          * Size of the fields.
          * @default 'md'
         */
        "size": Size;
        /**
          * Placeholder for the start field.
          * @default 'Data inicial'
         */
        "startPlaceholder": "Data inicial";
        /**
          * Start of the range, as `DD/MM/AAAA`.
          * @default ''
         */
        "startValue": string;
    }
    /**
     * A visual separator between sections of content, with an optional centered label.
     */
    interface ScarletDivider {
        /**
          * Optional text rendered centered on the line (horizontal orientation only).
         */
        "label"?: string;
        /**
          * Direction of the divider line.
          * @default 'horizontal'
         */
        "orientation": ScarletDividerOrientation;
    }
    /**
     * A panel that slides in from an edge of the screen — the mobile-friendlier
     * sibling of `scarlet-modal` for a filter panel, a form, a details view.
     * Built the same way `scarlet-modal` is, on the native `<dialog>` element,
     * for the same free focus trapping/top-layer stacking/Escape handling — see
     * that component's own comments for the mechanics shared here (focus
     * restore, the cancelable `scarletClose`, `show()`/`hide()`).
     * `size` sets the dimension along the axis the drawer slides on: width for
     * `left`/`right`, height for `top`/`bottom`.
     */
    interface ScarletDrawer {
        /**
          * Accessible label for the dialog. When omitted, the `header` slot's content is used instead (via `aria-labelledby`) — set this explicitly only when the drawer has no visible header, or the header text alone isn't a good accessible name.
         */
        "ariaLabel"?: string;
        /**
          * Closes the drawer when the backdrop (area outside the panel) is clicked.
          * @default true
         */
        "dismissOnBackdropClick": true;
        /**
          * Closes the drawer when Escape is pressed.
          * @default true
         */
        "dismissOnEsc": true;
        /**
          * Requests the drawer to close (fires the cancelable `scarletClose` event first).
         */
        "hide": () => Promise<void>;
        /**
          * Whether the drawer is open.
          * @default false
         */
        "open": boolean;
        /**
          * Which edge of the screen the drawer slides in from.
          * @default 'right'
         */
        "placement": ScarletDrawerPlacement;
        /**
          * Opens the drawer.
         */
        "show": () => Promise<void>;
        /**
          * Size along the slide axis: width for `left`/`right`, height for `top`/`bottom`.
          * @default 'md'
         */
        "size": ScarletDrawerSize;
    }
    /**
     * A placeholder for a list, search result, or section with nothing to
     * show — an icon, a heading, an optional description, and an optional
     * action (e.g. "Limpar filtros" or "Criar o primeiro item").
     */
    interface ScarletEmptyState {
        /**
          * Supporting text below the heading.
         */
        "description"?: string;
        /**
          * Main heading.
          * @default 'Nenhum resultado encontrado.'
         */
        "heading": "Nenhum resultado encontrado.";
        /**
          * Icon shown above the heading. Ignored if the `icon` slot has content.
         */
        "icon"?: ScarletIconName;
    }
    /**
     * A drag-and-drop file picker, backed by a real (visually hidden) native
     * `<input type="file">` — so it's a real form control and works with no JS
     * at all for the "click to browse" path; only the drag-and-drop layer is
     * enhancement on top.
     */
    interface ScarletFileUpload {
        /**
          * Native `accept` attribute, e.g. `"image/*"` or `".pdf,.docx"`.
         */
        "accept"?: string;
        /**
          * Clears every selected file.
         */
        "clear": () => Promise<void>;
        /**
          * Disables the dropzone and the native input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the dropzone. Takes priority over the automatic max-size error.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the dropzone. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Visible label rendered above the dropzone.
         */
        "label"?: string;
        /**
          * Rejects any file over this size, showing a default error message instead of accepting it.
         */
        "maxSizeBytes"?: number;
        /**
          * Allows more than one file.
          * @default false
         */
        "multiple": false;
    }
    /**
     * A CSS grid layout primitive with equal-width columns and token-based
     * gaps. Pair with `<scarlet-grid-item>` to span multiple columns/rows.
     * `columns` can change per breakpoint via `columnsSm`/`columnsMd`/
     * `columnsLg`/`columnsXl` — each one only takes effect from its breakpoint
     * up and, left unset, falls back to the next smaller breakpoint that *is*
     * set (mobile-first cascade), down to `columns` itself. E.g. `columns={1}
     * columns-md={3}` renders 1 column below 768px and 3 from 768px up, with
     * no separate `sm`/`lg`/`xl` value needed.
     */
    interface ScarletGrid {
        /**
          * Cross-axis alignment (`align-items`) of each item within its own cell.
          * @default 'stretch'
         */
        "align": Alignment;
        /**
          * Overrides the column gap.
         */
        "columnGap"?: Size;
        /**
          * Number of equal-width columns below the `sm` breakpoint (or at every size, if no responsive override is set).
          * @default 12
         */
        "columns": 12;
        /**
          * Column count from the `lg` breakpoint (1024px) up. Falls back to `columnsMd`/`columnsSm`/`columns` when unset.
         */
        "columnsLg"?: number;
        /**
          * Column count from the `md` breakpoint (768px) up. Falls back to `columnsSm`/`columns` when unset.
         */
        "columnsMd"?: number;
        /**
          * Column count from the `sm` breakpoint (640px) up. Falls back to `columns` when unset.
         */
        "columnsSm"?: number;
        /**
          * Column count from the `xl` breakpoint (1280px) up. Falls back to `columnsLg`/`columnsMd`/`columnsSm`/`columns` when unset.
         */
        "columnsXl"?: number;
        /**
          * Gap between rows and columns, from the design system's spacing scale. Overridden individually by `rowGap`/`columnGap`.
          * @default 'md'
         */
        "gap": Size;
        /**
          * In-cell horizontal alignment (`justify-items`) of each item within its own cell — the column analog of `align`.
          * @default 'stretch'
         */
        "justify": Alignment;
        /**
          * Overrides the row gap.
         */
        "rowGap"?: Size;
    }
    /**
     * A cell inside a `<scarlet-grid>` that can span multiple columns/rows.
     * `colSpan`/`rowSpan` can change per breakpoint via `colSpanSm`/`colSpanMd`/
     * `colSpanLg`/`colSpanXl` (and the `rowSpan` equivalents) — the classic "full
     * row on mobile, a couple columns on desktop" pattern is
     * `col-span="{columns}" col-span-md="2"`. Each one only takes effect from
     * its breakpoint up and, left unset, falls back to the next smaller
     * breakpoint that *is* set (mobile-first cascade, same as `<scarlet-grid>`'s
     * own `columns`/`columnsSm`/etc.), down to `colSpan`/`rowSpan` themselves.
     */
    interface ScarletGridItem {
        /**
          * Number of columns this item spans below the `sm` breakpoint (or at every size, if no responsive override is set).
          * @default 1
         */
        "colSpan": 1;
        /**
          * Column span from the `lg` breakpoint (1024px) up. Falls back to `colSpanMd`/`colSpanSm`/`colSpan` when unset.
         */
        "colSpanLg"?: number;
        /**
          * Column span from the `md` breakpoint (768px) up. Falls back to `colSpanSm`/`colSpan` when unset.
         */
        "colSpanMd"?: number;
        /**
          * Column span from the `sm` breakpoint (640px) up. Falls back to `colSpan` when unset.
         */
        "colSpanSm"?: number;
        /**
          * Column span from the `xl` breakpoint (1280px) up. Falls back to `colSpanLg`/`colSpanMd`/`colSpanSm`/`colSpan` when unset.
         */
        "colSpanXl"?: number;
        /**
          * Number of rows this item spans below the `sm` breakpoint (or at every size, if no responsive override is set).
          * @default 1
         */
        "rowSpan": 1;
        /**
          * Row span from the `lg` breakpoint (1024px) up. Falls back to `rowSpanMd`/`rowSpanSm`/`rowSpan` when unset.
         */
        "rowSpanLg"?: number;
        /**
          * Row span from the `md` breakpoint (768px) up. Falls back to `rowSpanSm`/`rowSpan` when unset.
         */
        "rowSpanMd"?: number;
        /**
          * Row span from the `sm` breakpoint (640px) up. Falls back to `rowSpan` when unset.
         */
        "rowSpanSm"?: number;
        /**
          * Row span from the `xl` breakpoint (1280px) up. Falls back to `rowSpanLg`/`rowSpanMd`/`rowSpanSm`/`rowSpan` when unset.
         */
        "rowSpanXl"?: number;
    }
    /**
     * A semantic heading (`<h1>`–`<h6>`) with an independently controllable
     * visual size — document structure (`level`) and visual hierarchy
     * (`variant`) don't have to match one-to-one.
     */
    interface ScarletHeading {
        /**
          * Text alignment.
          * @default 'left'
         */
        "align": TextAlign;
        /**
          * Text color token.
          * @default 'primary'
         */
        "color": TextColor;
        /**
          * Semantic heading level — which HTML tag (`h1`–`h6`) is rendered.
          * @default 2
         */
        "level": ScarletHeadingLevel;
        /**
          * Visual size. Defaults to a sensible size for `level` when omitted.
         */
        "variant"?: ScarletHeadingVariant;
    }
    /**
     * Renders an icon from the design system's shared built-in set. Icons are
     * stroke-based and inherit `color`/`font-size` from their context, so they
     * line up with surrounding text and other components by default.
     */
    interface ScarletIcon {
        /**
          * Accessible label. Omit for a purely decorative icon (the default) — it is then hidden from assistive tech.
         */
        "label"?: string;
        /**
          * Name of the built-in icon to render.
         */
        "name"?: ScarletIconName1;
        /**
          * Size of the icon, as any valid CSS length. Defaults to `1em`, so it scales with the surrounding font size.
         */
        "size"?: string;
    }
    /**
     * A labeled text input with helper/error text and built-in accessibility wiring.
     */
    interface ScarletInput {
        /**
          * Native `autocomplete` attribute.
         */
        "autocomplete"?: string;
        /**
          * Disables the input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
         */
        "placeholder"?: string;
        /**
          * Makes the input read-only.
          * @default false
         */
        "readonly": false;
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size": Size;
        /**
          * Native input type.
          * @default 'text'
         */
        "type": ScarletInputType;
        /**
          * Current value of the input.
          * @default ''
         */
        "value": string;
    }
    /**
     * A Brazilian postal code (CEP) input — formats as `01310-100`. This
     * component only formats the value; looking up the matching address (e.g.
     * via ViaCEP) is the consuming app's responsibility — listen for
     * `scarletChange` and call your own API with `getRawValue()`.
     */
    interface ScarletInputCep {
        /**
          * Disables the input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * The raw 8 digits behind the formatted value (e.g. `01310100`).
         */
        "getRawValue": () => Promise<string>;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
          * @default '00000-000'
         */
        "placeholder": "00000-000";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size": Size;
        /**
          * Current formatted value, e.g. `01310-100`.
          * @default ''
         */
        "value": string;
    }
    /**
     * A credit card number input — formats in groups of 4 (or Amex's 4-6-5 /
     * Diners' 4-6-4 grouping once that brand is detected from the card's
     * BIN/IIN prefix), with a Luhn checksum validation on blur.
     */
    interface ScarletInputCreditCard {
        /**
          * Disables the input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the input. Takes priority over automatic Luhn validation errors.
         */
        "errorMessage"?: string;
        /**
          * The detected card network (`'visa'`, `'mastercard'`, ...), or `undefined` if not yet recognizable.
         */
        "getBrand": () => Promise<CreditCardBrand | undefined>;
        /**
          * The raw digits behind the formatted value.
         */
        "getRawValue": () => Promise<string>;
        /**
          * Helper text rendered below the input. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Whether the current value passes the Luhn checksum.
         */
        "isValid": () => Promise<boolean>;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
          * @default '0000 0000 0000 0000'
         */
        "placeholder": "0000 0000 0000 0000";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size": Size;
        /**
          * Validates the number against the Luhn checksum on blur once it's complete, showing a default "Número de cartão inválido" message when it fails — unless `errorMessage` is already set, which always wins.
          * @default true
         */
        "validate": true;
        /**
          * Current formatted value.
          * @default ''
         */
        "value": string;
    }
    /**
     * A monetary input that formats digits as currency growing from the right
     * (like a card machine) — typing "1234" produces "R$ 12,34". Unlike the
     * other masked inputs, `scarletInput`/`scarletChange` emit the plain
     * **numeric** amount (e.g. `12.34`), not the formatted string — that's
     * almost always what you actually want from a money field. Read the
     * formatted text itself via `value`.
     */
    interface ScarletInputCurrency {
        /**
          * Currency symbol shown before the amount.
          * @default 'R$'
         */
        "currencySymbol": "R$";
        /**
          * Disables the input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * The current amount as a plain number (same value `scarletInput`/`scarletChange` emit).
         */
        "getNumericValue": () => Promise<number>;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'R$ 0,00'
         */
        "placeholder": "R$ 0,00";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size": Size;
        /**
          * Current formatted value, e.g. `R$ 1.234,56`.
          * @default ''
         */
        "value": string;
    }
    /**
     * A `DD/MM/AAAA` date input. Unlike a plain positional mask, `validate`
     * checks it's a *real* calendar date (rejects `31/02/2026`, honors leap
     * years) on blur, not just that 8 digits were typed.
     */
    interface ScarletInputDate {
        /**
          * Disables the input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the input. Takes priority over automatic calendar validation errors.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Whether the current value is a complete, real calendar date.
         */
        "isValid": () => Promise<boolean>;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'DD/MM/AAAA'
         */
        "placeholder": "DD/MM/AAAA";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size": Size;
        /**
          * The value as a native `Date`, or `undefined` if it isn't a complete valid date.
         */
        "toDate": () => Promise<Date | undefined>;
        /**
          * Validates the value is a real calendar date on blur once it's complete (8 digits), showing a default "Data inválida" message when it isn't — unless `errorMessage` is already set, which always wins.
          * @default true
         */
        "validate": true;
        /**
          * Current formatted value, e.g. `31/12/2026`.
          * @default ''
         */
        "value": string;
    }
    /**
     * A single "CPF/CNPJ" field that auto-detects and formats whichever
     * document type is being typed (11 digits → CPF `123.456.789-01`, 12+ →
     * CNPJ `12.345.678/0001-90`) — the common pattern for a field that accepts
     * either an individual or a company.
     */
    interface ScarletInputDocument {
        /**
          * Disables the input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the input. Takes priority over automatic check-digit validation errors.
         */
        "errorMessage"?: string;
        /**
          * `'cpf'` or `'cnpj'` based on the current digit count, or `undefined` if empty.
         */
        "getDocumentType": () => Promise<ScarletDocumentType | undefined>;
        /**
          * The raw digits behind the formatted value.
         */
        "getRawValue": () => Promise<string>;
        /**
          * Helper text rendered below the input. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Whether the current value is a complete, check-digit-valid CPF or CNPJ.
         */
        "isValid": () => Promise<boolean>;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'CPF ou CNPJ'
         */
        "placeholder": "CPF ou CNPJ";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size": Size;
        /**
          * Validates the CPF/CNPJ check digits on blur once the value is complete (11 or 14 digits), showing a default "CPF/CNPJ inválido" message when they don't check out — unless `errorMessage` is already set, which always wins.
          * @default true
         */
        "validate": true;
        /**
          * Current formatted value.
          * @default ''
         */
        "value": string;
    }
    /**
     * A Brazilian vehicle plate input — formats as the old `ABC-1234` pattern
     * or the newer Mercosul `ABC1D23` pattern, detected automatically from
     * whether a letter or digit lands in the 5th character.
     */
    interface ScarletInputLicensePlate {
        /**
          * Disables the input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Whether the current value looks like a complete old-format or Mercosul plate.
         */
        "getFormat": () => Promise<ScarletLicensePlateFormat | undefined>;
        /**
          * The raw alphanumeric characters behind the formatted value (no dash).
         */
        "getRawValue": () => Promise<string>;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'ABC-1234'
         */
        "placeholder": "ABC-1234";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size": Size;
        /**
          * Current formatted value, e.g. `ABC-1234` or `ABC1D23`.
          * @default ''
         */
        "value": string;
    }
    /**
     * A percentage input that formats digits growing from the right, like
     * `scarlet-input-currency` — typing "1234" produces "12,34%". Emits the
     * plain numeric percentage (e.g. `12.34`) via `scarletInput`/`scarletChange`.
     */
    interface ScarletInputPercentage {
        /**
          * How many digits are kept after the decimal comma.
          * @default 2
         */
        "decimals": 2;
        /**
          * Disables the input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * The current percentage as a plain number (same value `scarletInput`/`scarletChange` emit).
         */
        "getNumericValue": () => Promise<number>;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
          * @default '0,00%'
         */
        "placeholder": "0,00%";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size": Size;
        /**
          * Current formatted value, e.g. `12,34%`.
          * @default ''
         */
        "value": string;
    }
    /**
     * A Brazilian phone number input — formats as `(11) 91234-5678` (mobile,
     * 11 digits) or `(11) 1234-5678` (landline, 10 digits), switching
     * automatically as the user types.
     */
    interface ScarletInputPhone {
        /**
          * Disables the input.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * The raw digits behind the formatted value (e.g. `11912345678`) — what you'd actually submit to an API.
         */
        "getRawValue": () => Promise<string>;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the input is empty.
          * @default '(11) 91234-5678'
         */
        "placeholder": "(11) 91234-5678";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size": Size;
        /**
          * Current formatted value, e.g. `(11) 91234-5678`.
          * @default ''
         */
        "value": string;
    }
    /**
     * A styled inline text link — for a link inside a sentence/paragraph.
     * `scarlet-button variant="link"` is the button-semantics equivalent (an
     * action styled like a link); this is the reverse, a real `<a>` styled like
     * one. `target="_blank"` automatically gets `rel="noopener noreferrer"`
     * (unless `rel` is set explicitly) and a small external-link icon.
     */
    interface ScarletLink {
        /**
          * Semantic color of the link.
          * @default 'primary'
         */
        "color": Color;
        /**
          * Renders as inert text — no `href`, `aria-disabled`.
          * @default false
         */
        "disabled": false;
        /**
          * Native `href`. Omitting it (and setting `disabled`) renders inert text styled like a link.
         */
        "href"?: string;
        /**
          * Native `rel`. Defaults to `noopener noreferrer` when `target="_blank"`.
         */
        "rel"?: string;
        /**
          * Native `target`, e.g. `_blank`.
         */
        "target"?: string;
        /**
          * When the underline shows.
          * @default 'hover'
         */
        "underline": ScarletLinkUnderline;
    }
    /**
     * A dropdown menu of actions opened from an arbitrary trigger element (the
     * `trigger` slot — typically an icon button like "⋮"), following the
     * WAI-ARIA menu button pattern: the trigger gets `aria-haspopup="menu"`/
     * `aria-expanded` (set directly on whatever element is slotted in), the
     * popover is `role="menu"` with `role="menuitem"` buttons, arrow keys move
     * a roving tab stop within the list, Home/End jump to the first/last enabled
     * item, Escape closes and returns focus to the trigger. Enter/Space opening
     * the menu is *not* handled here — the trigger must be a real, natively
     * activatable element (a `<button>` or `<scarlet-button>`); its own native
     * click already opens the menu, and re-handling those keys here would fire
     * twice (open, then immediately close again from the resulting click).
     * Known limitation: like `scarlet-tooltip`/`scarlet-date-picker`,
     * positioning is plain CSS anchored to the trigger — it doesn't flip or
     * reposition to stay in the viewport, and it doesn't close on Tab-ing past
     * its last item.
     */
    interface ScarletMenu {
        /**
          * Accessible label for the menu list, when the trigger's own accessible name doesn't already describe it.
          * @default 'Menu'
         */
        "ariaLabel": "Menu";
        /**
          * The actions to list.
          * @default []
         */
        "items": ScarletMenuItem[];
        /**
          * Which side of the trigger the menu aligns to.
          * @default 'start'
         */
        "placement": 'start' | 'end';
    }
    /**
     * A modal dialog built on the native `<dialog>` element, which provides
     * focus trapping, top-layer stacking and Escape handling for free.
     */
    interface ScarletModal {
        /**
          * Accessible label for the dialog. When omitted, the `header` slot's content is used instead (via `aria-labelledby`) — set this explicitly only when the modal has no visible header, or the header text alone isn't a good accessible name.
         */
        "ariaLabel"?: string;
        /**
          * Closes the modal when the backdrop (area outside the dialog box) is clicked.
          * @default true
         */
        "dismissOnBackdropClick": true;
        /**
          * Closes the modal when Escape is pressed.
          * @default true
         */
        "dismissOnEsc": true;
        /**
          * Requests the modal to close (fires the cancelable `scarletClose` event first).
         */
        "hide": () => Promise<void>;
        /**
          * Whether the modal is open.
          * @default false
         */
        "open": boolean;
        /**
          * Opens the modal.
         */
        "show": () => Promise<void>;
        /**
          * Size of the modal.
          * @default 'md'
         */
        "size": ScarletModalSize;
    }
    /**
     * A numeric input with decrement/increment buttons — for a quantity field,
     * not a general-purpose text field that happens to hold numbers (that's
     * `scarlet-input type="number"`). Stays within `min`/`max` on every path:
     * the buttons, typing, and blur all clamp.
     */
    interface ScarletNumberInput {
        /**
          * Disables the field and both buttons.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the field. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the field. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the field as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the field.
         */
        "label"?: string;
        /**
          * Upper bound. Omit for no maximum.
         */
        "max"?: number;
        /**
          * Lower bound. Omit for no minimum.
         */
        "min"?: number;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Marks the field as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal input element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the field.
          * @default 'md'
         */
        "size": Size;
        /**
          * Amount each +/- click changes the value by.
          * @default 1
         */
        "step": 1;
        /**
          * Current value.
          * @default 0
         */
        "value": number;
    }
    /**
     * Page number navigation, following the WAI-ARIA pattern of a `<nav>`
     * landmark wrapping a list of buttons — the current page is a real button
     * (not a link, since this component doesn't own routing) marked
     * `aria-current="page"`. Collapses distant page numbers into an ellipsis
     * once `totalPages` is large, always keeping the first page, the last page,
     * and `siblingCount` pages on each side of the current one visible.
     */
    interface ScarletPagination {
        /**
          * Accessible label for the `<nav>` landmark.
          * @default 'Paginação'
         */
        "ariaLabel": "Paginação";
        /**
          * Current page (1-indexed).
          * @default 1
         */
        "page": number;
        /**
          * How many page numbers to show on each side of the current page before collapsing into an ellipsis.
          * @default 1
         */
        "siblingCount": 1;
        /**
          * Total number of pages.
          * @default 1
         */
        "totalPages": 1;
    }
    /**
     * A generic overlay anchored to a trigger element (the `trigger` slot),
     * with arbitrary content in the default slot — the shared primitive
     * `scarlet-menu`/`scarlet-date-picker`/`scarlet-combobox` each built their
     * own narrower version of for their specific popovers. Use this one
     * directly for anything that doesn't need a menu list or a calendar grid:
     * a rich tooltip, a filter panel, a confirmation prompt.
     * `trigger="click"` (the default) toggles on click, closes on Escape or a
     * click outside, and sets `aria-haspopup`/`aria-expanded` on whatever's
     * slotted as the trigger. `trigger="hover"` opens/closes on mouse enter/
     * leave instead, with no click/Escape handling — matching how a hover
     * tooltip behaves, not a dialog.
     * Known limitation: like `scarlet-tooltip`/`scarlet-menu`, positioning is
     * plain CSS anchored to the trigger — it doesn't flip or reposition to stay
     * in the viewport.
     */
    interface ScarletPopover {
        /**
          * Accessible label for the popover content region.
         */
        "ariaLabel"?: string;
        /**
          * Which side of the trigger the popover opens on.
          * @default 'bottom'
         */
        "placement": ScarletPopoverPlacement;
        /**
          * What interaction opens/closes the popover.
          * @default 'click'
         */
        "triggerMode": ScarletPopoverTriggerMode;
    }
    /**
     * A determinate progress bar (0–100%) — for a file upload, a multi-step
     * form, anything with a real, known completion percentage. For an
     * indeterminate "something's happening" state, use `scarlet-spinner`.
     */
    interface ScarletProgress {
        /**
          * Accessible label for the `progressbar` role.
          * @default 'Progresso'
         */
        "ariaLabel": "Progresso";
        /**
          * Semantic color of the filled portion.
          * @default 'primary'
         */
        "color": Color;
        /**
          * Value that represents 100%.
          * @default 100
         */
        "max": 100;
        /**
          * Shows the percentage as text next to the track.
          * @default false
         */
        "showLabel": false;
        /**
          * Height of the track.
          * @default 'md'
         */
        "size": ScarletProgressSize;
        /**
          * Current value.
          * @default 0
         */
        "value": 0;
    }
    /**
     * A single radio option. Use inside a `<scarlet-radio-group>` for mutually
     * exclusive selection — standalone, it behaves like an isolated toggle.
     */
    interface ScarletRadio {
        /**
          * Whether this radio is selected. Managed by the parent `<scarlet-radio-group>` when present.
          * @default false
         */
        "checked": boolean;
        /**
          * Disables this radio. Set by a parent `<scarlet-radio-group>` when the whole group is disabled.
          * @default false
         */
        "disabled": boolean;
        /**
          * Whether this radio is a tab stop. A parent `<scarlet-radio-group>` keeps this `true` on exactly one radio at a time (roving tabindex, per the WAI-ARIA radiogroup pattern) so the group has a single Tab stop and arrow keys move both focus and selection — matching how native radio buttons behave. Standalone (no group), this defaults to `true` so a lone radio is reachable by Tab like any other control.
          * @default true
         */
        "focusable": boolean;
        /**
          * Visible label rendered next to the radio.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form. Set by a parent `<scarlet-radio-group>` when present.
         */
        "name"?: string;
        /**
          * Value identifying this option within its group.
         */
        "value"?: string;
    }
    /**
     * Groups a set of `<scarlet-radio>` options and enforces single selection —
     * native browser radio grouping does not cross shadow-DOM boundaries, so
     * this component coordinates `checked`/`name`/`disabled` across its children.
     */
    interface ScarletRadioGroup {
        /**
          * Accessible label for the group, when there is no visible heading nearby.
         */
        "ariaLabel"?: string;
        /**
          * Disables every radio in the group.
          * @default false
         */
        "disabled": false;
        /**
          * Lays the radios out in a row instead of stacked.
          * @default false
         */
        "horizontal": false;
        /**
          * Name applied to every radio in the group, so they submit together in a form.
         */
        "name"?: string;
        /**
          * Value of the currently selected radio.
         */
        "value"?: string;
    }
    /**
     * A labeled native `<select>` dropdown with helper/error text and built-in
     * accessibility wiring. Options are passed as a `options` property (an
     * array), not slotted — native `<select>` cannot reliably project slotted
     * `<option>` elements into its picker UI across browsers.
     */
    interface ScarletSelect {
        /**
          * Disables the select.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the select. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the select. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the select as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the select.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Options rendered inside the select.
          * @default []
         */
        "options": ScarletSelectOption[];
        /**
          * Placeholder shown as a disabled first option when no value is selected.
         */
        "placeholder"?: string;
        /**
          * Marks the select as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Focuses the internal select element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the select.
          * @default 'md'
         */
        "size": Size;
        /**
          * Currently selected value.
          * @default ''
         */
        "value": string;
    }
    /**
     * A loading placeholder shape — swap it in for content that hasn't arrived
     * yet (a card, an avatar, a paragraph). Purely decorative (`aria-hidden`):
     * announcing the *loading* state itself (e.g. `aria-busy` on the section
     * being replaced) is the consumer's responsibility, not this component's.
     */
    interface ScarletSkeleton {
        /**
          * Any valid CSS height. Defaults to one text line's height for `text`, or `width`'s value for `circle`.
         */
        "height"?: string;
        /**
          * Number of stacked lines, only for `variant="text"`. The last line renders narrower, like a paragraph's ragged edge.
          * @default 1
         */
        "lines": 1;
        /**
          * Shape of the placeholder.
          * @default 'text'
         */
        "variant": ScarletSkeletonVariant;
        /**
          * Any valid CSS width. Defaults to 100% for `text`/`rect`, or `height`'s value for `circle`.
         */
        "width"?: string;
    }
    /**
     * A loading indicator — `variant="circle"` for the generic spinner (the
     * same shape `scarlet-button`'s own `loading` state uses), `variant="logo"`
     * for the Scarlet mark itself pulsing, e.g. for a full-page loading state.
     * `role="status"` with `label` as its accessible name, so assistive tech
     * announces the loading state without needing separate visible text.
     */
    interface ScarletSpinner {
        /**
          * Accessible label, announced by assistive tech via `role="status"`.
          * @default 'Carregando'
         */
        "label": "Carregando";
        /**
          * Size of the indicator.
          * @default 'md'
         */
        "size": ScarletSpinnerSize;
        /**
          * Which loading indicator to show.
          * @default 'circle'
         */
        "variant": ScarletSpinnerVariant;
    }
    /**
     * A flexbox layout primitive for stacking or rowing children with
     * consistent, token-based spacing.
     * `direction` can change per breakpoint via `directionSm`/`directionMd`/
     * `directionLg`/`directionXl` — the classic "stacked on mobile, row on
     * desktop" pattern is `direction="column" direction-md="row"`. Each one
     * only takes effect from its breakpoint up and, left unset, falls back to
     * the next smaller breakpoint that *is* set (mobile-first cascade), down
     * to `direction` itself.
     */
    interface ScarletStack {
        /**
          * Cross-axis alignment (`align-items`).
          * @default 'stretch'
         */
        "align": Alignment;
        /**
          * Flex direction below the `sm` breakpoint (or at every size, if no responsive override is set).
          * @default 'column'
         */
        "direction": Direction;
        /**
          * Flex direction from the `lg` breakpoint (1024px) up. Falls back to `directionMd`/`directionSm`/`direction` when unset.
         */
        "directionLg"?: Direction;
        /**
          * Flex direction from the `md` breakpoint (768px) up. Falls back to `directionSm`/`direction` when unset.
         */
        "directionMd"?: Direction;
        /**
          * Flex direction from the `sm` breakpoint (640px) up. Falls back to `direction` when unset.
         */
        "directionSm"?: Direction;
        /**
          * Flex direction from the `xl` breakpoint (1280px) up. Falls back to `directionLg`/`directionMd`/`directionSm`/`direction` when unset.
         */
        "directionXl"?: Direction;
        /**
          * Space between items, from the design system's spacing scale.
          * @default 'md'
         */
        "gap": Size;
        /**
          * Main-axis distribution (`justify-content`).
          * @default 'start'
         */
        "justify": JustifyContent;
        /**
          * Whether items wrap onto multiple lines.
          * @default 'nowrap'
         */
        "wrap": Wrap;
    }
    /**
     * A single labeled metric — a number/value, its label, and an optional
     * change indicator (e.g. "+12%" with an up/down arrow) for a dashboard.
     */
    interface ScarletStat {
        /**
          * Change text, e.g. "+12% vs. mês anterior". Omit to hide the whole change row.
         */
        "change"?: string;
        /**
          * Label above the value, e.g. "Receita total".
          * @default ''
         */
        "label": "";
        /**
          * Direction the change indicates — colors and arrows the change text accordingly.
          * @default 'neutral'
         */
        "trend": ScarletStatTrend;
        /**
          * The metric itself, e.g. "R$ 42.900".
          * @default ''
         */
        "value": "";
    }
    /**
     * A toggle switch for binary on/off settings.
     */
    interface ScarletSwitch {
        /**
          * Whether the switch is on.
          * @default false
         */
        "checked": boolean;
        /**
          * Disables the switch.
          * @default false
         */
        "disabled": false;
        /**
          * Visible label rendered next to the switch.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Marks the switch as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Value submitted with a parent form when on.
         */
        "value"?: string;
    }
    /**
     * A data table: sortable columns, optional row selection (checkboxes),
     * empty/loading states, and a horizontally-scrolling wrapper so a wide
     * table never breaks the surrounding layout on a narrow viewport (same
     * pattern `scarlet-tabs` uses for its tab list).
     * Cell content is `String(row[column.key])` by default — good enough for
     * plain text/numbers, but there's no per-cell slot system (that needs a
     * stable id per row and gets complex fast). For anything richer — a
     * `scarlet-badge` status, a formatted currency, a truncated link — pass
     * `formatCell`, a plain function `(row, column) => string` set as a JS
     * property (like `columns`/`rows` themselves, it isn't parseable from an
     * HTML attribute).
     * Sorting is handled internally using a locale-aware comparator (numeric
     * for `number` values, `localeCompare` otherwise) — good for typical
     * text/number columns without any setup. `scarletSort` still fires on every
     * click if a consumer wants to replace that with their own comparator (e.g.
     * sorting a formatted date column by its real underlying timestamp) by
     * reassigning `rows` themselves in response.
     */
    interface ScarletTable {
        /**
          * Accessible label for the table, when there is no visible heading nearby.
         */
        "ariaLabel"?: string;
        /**
          * Makes whole rows clickable (emits `scarletRowClick`), e.g. to open a detail view.
          * @default false
         */
        "clickableRows": false;
        /**
          * Column definitions, in display order.
          * @default []
         */
        "columns": ScarletTableColumn[];
        /**
          * Message shown instead of rows when `rows` is empty.
          * @default 'Nenhum registro encontrado.'
         */
        "emptyMessage": "Nenhum registro encontrado.";
        /**
          * Optional per-cell formatter, overriding the default `String(row[column.key])`. Set as a JS property, not an HTML attribute.
         */
        "formatCell"?: (row: ScarletTableRow, column: ScarletTableColumn) => string;
        /**
          * Shows `loadingMessage` instead of rows/empty state.
          * @default false
         */
        "loading": false;
        /**
          * Message shown while `loading` is true.
          * @default 'Carregando…'
         */
        "loadingMessage": "Carregando…";
        /**
          * Field on each row object holding its unique identifier — used for selection tracking.
          * @default 'id'
         */
        "rowKey": "id";
        /**
          * The data to render, one object per row.
          * @default []
         */
        "rows": ScarletTableRow[];
        /**
          * Shows a checkbox column and enables row selection.
          * @default false
         */
        "selectable": false;
        /**
          * Keys (from `rowKey`) of the currently selected rows.
          * @default []
         */
        "selectedRowKeys": Array<string | number>;
    }
    /**
     * A set of tabs with associated panels. Panel content is projected via a
     * named slot per item, matching `item.value`.
     */
    interface ScarletTabs {
        /**
          * The tabs to render.
          * @default []
         */
        "items": ScarletTabItem[];
        /**
          * Value of the currently selected tab. Defaults to the first enabled item.
         */
        "value"?: string;
    }
    /**
     * Body copy with the design system's text scale, color tokens and
     * single-line truncation — the general-purpose counterpart to `scarlet-heading`.
     */
    interface ScarletText {
        /**
          * Text alignment.
          * @default 'left'
         */
        "align": TextAlign;
        /**
          * HTML tag rendered for the text.
          * @default 'p'
         */
        "as": ScarletTextAs;
        /**
          * Text color token.
          * @default 'primary'
         */
        "color": TextColor;
        /**
          * Truncates overflowing text to a single line with an ellipsis.
          * @default false
         */
        "truncate": false;
        /**
          * Visual size, from the design system's body/caption text scale.
          * @default 'body-md'
         */
        "variant": ScarletTextVariant;
        /**
          * Overrides the variant's default font weight.
         */
        "weight"?: ScarletTextWeight;
    }
    /**
     * A multi-line labeled text input with helper/error text and built-in accessibility wiring.
     */
    interface ScarletTextarea {
        /**
          * Disables the textarea.
          * @default false
         */
        "disabled": false;
        /**
          * Error message rendered below the textarea. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the textarea. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the textarea as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid": false;
        /**
          * Visible label rendered above the textarea.
         */
        "label"?: string;
        /**
          * Maximum number of characters allowed.
         */
        "maxlength"?: number;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Placeholder text shown when the textarea is empty.
         */
        "placeholder"?: string;
        /**
          * Makes the textarea read-only.
          * @default false
         */
        "readonly": false;
        /**
          * Marks the textarea as required in a parent form.
          * @default false
         */
        "required": false;
        /**
          * Controls whether/how the user can resize the textarea.
          * @default 'vertical'
         */
        "resize": ScarletTextareaResize;
        /**
          * Number of visible text rows.
          * @default 4
         */
        "rows": 4;
        /**
          * Focuses the internal textarea element.
         */
        "setFocus": () => Promise<void>;
        /**
          * Size of the textarea (affects padding and font size).
          * @default 'md'
         */
        "size": Size;
        /**
          * Current value of the textarea.
          * @default ''
         */
        "value": string;
    }
    /**
     * A vertical sequence of events — an order's status history, an activity
     * feed, a step-by-step audit trail.
     */
    interface ScarletTimeline {
        /**
          * The events, in order from first to last.
          * @default []
         */
        "items": ScarletTimelineItem[];
    }
    /**
     * A self-dismissing status message, fixed to a corner of the viewport.
     * For multiple simultaneous toasts, render several instances inside a
     * consumer-owned stacking container (this component does not manage a queue).
     */
    interface ScarletToast {
        /**
          * Shows a dismiss (close) button.
          * @default true
         */
        "dismissible": true;
        /**
          * Milliseconds before the toast auto-dismisses. `0` disables auto-dismiss.
          * @default 4000
         */
        "duration": 4000;
        /**
          * Whether the toast is visible.
          * @default true
         */
        "open": boolean;
        /**
          * Corner of the viewport the toast is anchored to.
          * @default 'bottom-right'
         */
        "position": ScarletToastPosition;
        /**
          * Semantic status of the toast.
          * @default 'info'
         */
        "status": ScarletAlertStatus;
    }
    /**
     * A horizontal bar for grouping related actions (buttons, a search field,
     * an icon menu) — `role="toolbar"` with token-based gap, distinct from
     * `scarlet-stack` in intent (actions, not arbitrary layout) even though
     * the underlying flex mechanics are similar.
     */
    interface ScarletToolbar {
        /**
          * Accessible label for the `toolbar` role — required whenever there's more than one toolbar on the page.
         */
        "ariaLabel"?: string;
        /**
          * Main-axis distribution of items.
          * @default 'start'
         */
        "justify": JustifyContent;
    }
    /**
     * A text hint shown next to its trigger on hover/focus. Positioning is
     * plain CSS anchored to the host — it does not flip to stay in the
     * viewport, so pick a `placement` that has room to render.
     */
    interface ScarletTooltip {
        /**
          * Text content of the tooltip.
         */
        "content"?: string;
        /**
          * Delay in milliseconds before the tooltip appears.
          * @default 200
         */
        "delay": 200;
        /**
          * Disables the tooltip entirely.
          * @default false
         */
        "disabled": false;
        /**
          * Where the tooltip renders relative to its trigger.
          * @default 'top'
         */
        "placement": ScarletTooltipPlacement;
    }
}
export interface ScarletAccordionCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletAccordionElement;
}
export interface ScarletAlertCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletAlertElement;
}
export interface ScarletBreadcrumbCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletBreadcrumbElement;
}
export interface ScarletButtonCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletButtonElement;
}
export interface ScarletCardCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletCardElement;
}
export interface ScarletCheckboxCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletCheckboxElement;
}
export interface ScarletCheckboxGroupCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletCheckboxGroupElement;
}
export interface ScarletChipCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletChipElement;
}
export interface ScarletComboboxCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletComboboxElement;
}
export interface ScarletDatePickerCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletDatePickerElement;
}
export interface ScarletDateRangePickerCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletDateRangePickerElement;
}
export interface ScarletDrawerCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletDrawerElement;
}
export interface ScarletFileUploadCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletFileUploadElement;
}
export interface ScarletInputCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletInputElement;
}
export interface ScarletInputCepCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletInputCepElement;
}
export interface ScarletInputCreditCardCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletInputCreditCardElement;
}
export interface ScarletInputCurrencyCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletInputCurrencyElement;
}
export interface ScarletInputDateCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletInputDateElement;
}
export interface ScarletInputDocumentCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletInputDocumentElement;
}
export interface ScarletInputLicensePlateCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletInputLicensePlateElement;
}
export interface ScarletInputPercentageCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletInputPercentageElement;
}
export interface ScarletInputPhoneCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletInputPhoneElement;
}
export interface ScarletMenuCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletMenuElement;
}
export interface ScarletModalCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletModalElement;
}
export interface ScarletNumberInputCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletNumberInputElement;
}
export interface ScarletPaginationCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletPaginationElement;
}
export interface ScarletPopoverCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletPopoverElement;
}
export interface ScarletRadioCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletRadioElement;
}
export interface ScarletRadioGroupCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletRadioGroupElement;
}
export interface ScarletSelectCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletSelectElement;
}
export interface ScarletSwitchCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletSwitchElement;
}
export interface ScarletTableCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletTableElement;
}
export interface ScarletTabsCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletTabsElement;
}
export interface ScarletTextareaCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletTextareaElement;
}
export interface ScarletToastCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletToastElement;
}
export interface ScarletTooltipCustomEvent<T> extends CustomEvent<T> {
    detail: T;
    target: HTMLScarletTooltipElement;
}
declare global {
    interface HTMLScarletAccordionElementEventMap {
        "scarletChange": string[];
    }
    /**
     * A set of collapsible sections. Panel content is projected via a named
     * slot per item, matching `item.value`.
     */
    interface HTMLScarletAccordionElement extends Components.ScarletAccordion, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletAccordionElementEventMap>(type: K, listener: (this: HTMLScarletAccordionElement, ev: ScarletAccordionCustomEvent<HTMLScarletAccordionElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletAccordionElementEventMap>(type: K, listener: (this: HTMLScarletAccordionElement, ev: ScarletAccordionCustomEvent<HTMLScarletAccordionElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletAccordionElement: {
        prototype: HTMLScarletAccordionElement;
        new (): HTMLScarletAccordionElement;
    };
    interface HTMLScarletAlertElementEventMap {
        "scarletDismiss": void;
    }
    /**
     * An inline status message with optional title, icon and dismiss action.
     */
    interface HTMLScarletAlertElement extends Components.ScarletAlert, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletAlertElementEventMap>(type: K, listener: (this: HTMLScarletAlertElement, ev: ScarletAlertCustomEvent<HTMLScarletAlertElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletAlertElementEventMap>(type: K, listener: (this: HTMLScarletAlertElement, ev: ScarletAlertCustomEvent<HTMLScarletAlertElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletAlertElement: {
        prototype: HTMLScarletAlertElement;
        new (): HTMLScarletAlertElement;
    };
    /**
     * A user/entity avatar: shows an image, falling back to initials derived
     * from `name`, falling back to a generic icon.
     */
    interface HTMLScarletAvatarElement extends Components.ScarletAvatar, HTMLStencilElement {
    }
    var HTMLScarletAvatarElement: {
        prototype: HTMLScarletAvatarElement;
        new (): HTMLScarletAvatarElement;
    };
    /**
     * Overlaps a set of `<scarlet-avatar>` children into a stack, with an
     * optional "+N" indicator when there are more than `max`. Propagates `size`
     * down to every avatar so they all render at the same size regardless of
     * what each one was given individually.
     */
    interface HTMLScarletAvatarGroupElement extends Components.ScarletAvatarGroup, HTMLStencilElement {
    }
    var HTMLScarletAvatarGroupElement: {
        prototype: HTMLScarletAvatarGroupElement;
        new (): HTMLScarletAvatarGroupElement;
    };
    /**
     * A small status/count label, or a bare notification dot.
     */
    interface HTMLScarletBadgeElement extends Components.ScarletBadge, HTMLStencilElement {
    }
    var HTMLScarletBadgeElement: {
        prototype: HTMLScarletBadgeElement;
        new (): HTMLScarletBadgeElement;
    };
    interface HTMLScarletBreadcrumbElementEventMap {
        "scarletNavigate": ScarletBreadcrumbItem;
    }
    /**
     * A trail of links showing the current page's position in a hierarchy,
     * following the WAI-ARIA breadcrumb pattern (`<nav aria-label>` wrapping an
     * ordered list, the last item marked `aria-current="page"`).
     * Items render as real `<a href>` elements — usable with no JS at all — but
     * every click first emits a cancelable `scarletNavigate`, so a framework
     * router can call `preventDefault()` and handle the navigation itself
     * instead of a full page load.
     */
    interface HTMLScarletBreadcrumbElement extends Components.ScarletBreadcrumb, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletBreadcrumbElementEventMap>(type: K, listener: (this: HTMLScarletBreadcrumbElement, ev: ScarletBreadcrumbCustomEvent<HTMLScarletBreadcrumbElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletBreadcrumbElementEventMap>(type: K, listener: (this: HTMLScarletBreadcrumbElement, ev: ScarletBreadcrumbCustomEvent<HTMLScarletBreadcrumbElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletBreadcrumbElement: {
        prototype: HTMLScarletBreadcrumbElement;
        new (): HTMLScarletBreadcrumbElement;
    };
    interface HTMLScarletButtonElementEventMap {
        "scarletClick": MouseEvent;
    }
    /**
     * A clickable action element with solid, outline, ghost and link variants.
     * An icon (e.g. `<scarlet-icon>`) goes in the `start`/`end` slot alongside
     * the default slot's text — `<scarlet-button><scarlet-icon slot="start"
     * name="check" />Salvar</scarlet-button>`. For an icon with no visible text
     * at all, add `iconOnly` (which turns the button square instead of its
     * usual text-driven width) and `ariaLabel` (required then — nothing else
     * gives the button an accessible name).
     */
    interface HTMLScarletButtonElement extends Components.ScarletButton, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletButtonElementEventMap>(type: K, listener: (this: HTMLScarletButtonElement, ev: ScarletButtonCustomEvent<HTMLScarletButtonElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletButtonElementEventMap>(type: K, listener: (this: HTMLScarletButtonElement, ev: ScarletButtonCustomEvent<HTMLScarletButtonElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletButtonElement: {
        prototype: HTMLScarletButtonElement;
        new (): HTMLScarletButtonElement;
    };
    interface HTMLScarletCardElementEventMap {
        "scarletClick": MouseEvent | KeyboardEvent;
    }
    /**
     * A container for grouping related content, with optional header/footer slots.
     */
    interface HTMLScarletCardElement extends Components.ScarletCard, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletCardElementEventMap>(type: K, listener: (this: HTMLScarletCardElement, ev: ScarletCardCustomEvent<HTMLScarletCardElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletCardElementEventMap>(type: K, listener: (this: HTMLScarletCardElement, ev: ScarletCardCustomEvent<HTMLScarletCardElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletCardElement: {
        prototype: HTMLScarletCardElement;
        new (): HTMLScarletCardElement;
    };
    interface HTMLScarletCheckboxElementEventMap {
        "scarletChange": boolean;
        "scarletCheckboxChange": boolean;
    }
    /**
     * A checkbox input with label, indeterminate state and accessible wiring.
     */
    interface HTMLScarletCheckboxElement extends Components.ScarletCheckbox, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletCheckboxElementEventMap>(type: K, listener: (this: HTMLScarletCheckboxElement, ev: ScarletCheckboxCustomEvent<HTMLScarletCheckboxElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletCheckboxElementEventMap>(type: K, listener: (this: HTMLScarletCheckboxElement, ev: ScarletCheckboxCustomEvent<HTMLScarletCheckboxElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletCheckboxElement: {
        prototype: HTMLScarletCheckboxElement;
        new (): HTMLScarletCheckboxElement;
    };
    interface HTMLScarletCheckboxGroupElementEventMap {
        "scarletChange": string[];
    }
    /**
     * Groups a set of `<scarlet-checkbox>` options and coordinates which ones
     * are checked as a single `value` array — unlike `<scarlet-radio-group>`,
     * each checkbox keeps its own native Tab stop (WAI-ARIA's `group` role, not
     * `radiogroup`, doesn't call for roving tabindex/arrow-key navigation the
     * way a radio group does).
     */
    interface HTMLScarletCheckboxGroupElement extends Components.ScarletCheckboxGroup, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletCheckboxGroupElementEventMap>(type: K, listener: (this: HTMLScarletCheckboxGroupElement, ev: ScarletCheckboxGroupCustomEvent<HTMLScarletCheckboxGroupElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletCheckboxGroupElementEventMap>(type: K, listener: (this: HTMLScarletCheckboxGroupElement, ev: ScarletCheckboxGroupCustomEvent<HTMLScarletCheckboxGroupElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletCheckboxGroupElement: {
        prototype: HTMLScarletCheckboxGroupElement;
        new (): HTMLScarletCheckboxGroupElement;
    };
    interface HTMLScarletChipElementEventMap {
        "scarletRemove": void;
    }
    /**
     * A small, dismissible tag — for an active filter, a selected option, an
     * added item in a multi-value input. Not a `<scarlet-badge>` with a close
     * button bolted on: unlike a badge, this is meant to be interactive.
     */
    interface HTMLScarletChipElement extends Components.ScarletChip, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletChipElementEventMap>(type: K, listener: (this: HTMLScarletChipElement, ev: ScarletChipCustomEvent<HTMLScarletChipElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletChipElementEventMap>(type: K, listener: (this: HTMLScarletChipElement, ev: ScarletChipCustomEvent<HTMLScarletChipElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletChipElement: {
        prototype: HTMLScarletChipElement;
        new (): HTMLScarletChipElement;
    };
    interface HTMLScarletComboboxElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
    }
    /**
     * A searchable select — type to filter a list of options, following the
     * WAI-ARIA 1.2 "editable combobox with list autocomplete" pattern. Unlike
     * `scarlet-menu`/`scarlet-date-picker`'s popovers, DOM focus never leaves
     * the text input while the list is open: the highlighted option is tracked
     * with `aria-activedescendant` instead of moving real focus button-to-
     * button, so the user can keep typing without ever losing their place.
     * Known limitation: like `scarlet-tooltip`/`scarlet-date-picker`/
     * `scarlet-menu`, positioning is plain CSS anchored to the field — it
     * doesn't flip or reposition to stay in the viewport.
     */
    interface HTMLScarletComboboxElement extends Components.ScarletCombobox, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletComboboxElementEventMap>(type: K, listener: (this: HTMLScarletComboboxElement, ev: ScarletComboboxCustomEvent<HTMLScarletComboboxElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletComboboxElementEventMap>(type: K, listener: (this: HTMLScarletComboboxElement, ev: ScarletComboboxCustomEvent<HTMLScarletComboboxElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletComboboxElement: {
        prototype: HTMLScarletComboboxElement;
        new (): HTMLScarletComboboxElement;
    };
    /**
     * A centered, max-width content wrapper — the outermost layout primitive
     * for page sections.
     */
    interface HTMLScarletContainerElement extends Components.ScarletContainer, HTMLStencilElement {
    }
    var HTMLScarletContainerElement: {
        prototype: HTMLScarletContainerElement;
        new (): HTMLScarletContainerElement;
    };
    interface HTMLScarletDatePickerElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
        "scarletValidityChange": boolean;
        "scarletShow": void;
        "scarletHide": void;
    }
    /**
     * A `DD/MM/AAAA` date input (typing, masking and calendar validation shared
     * with `scarlet-input-date`) plus a calendar popover for picking a date
     * visually — a button next to the field opens a month grid; arrow keys move
     * within it (Home/End jump to the start/end of the week, PageUp/PageDown
     * change month, Shift+PageUp/PageDown change year), Enter/Space picks the
     * focused day, Escape closes and returns focus to the toggle button.
     * Known limitations:
     * - The popover closes on Escape, on picking a day, or on clicking outside
     *   it — not on Tab-ing past its last focusable element. A keyboard user who
     *   tabs out instead of pressing Escape will move focus past the component
     *   with the panel still visually open.
     * - Like `scarlet-tooltip`, positioning is plain CSS anchored to the host —
     *   it never flips or shifts to stay in the viewport. It's shrunk to never
     *   exceed the viewport's own width, so it can't run off the right edge of
     *   the *screen*, but placing the field near that edge can still overflow
     *   past its own container.
     */
    interface HTMLScarletDatePickerElement extends Components.ScarletDatePicker, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletDatePickerElementEventMap>(type: K, listener: (this: HTMLScarletDatePickerElement, ev: ScarletDatePickerCustomEvent<HTMLScarletDatePickerElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletDatePickerElementEventMap>(type: K, listener: (this: HTMLScarletDatePickerElement, ev: ScarletDatePickerCustomEvent<HTMLScarletDatePickerElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletDatePickerElement: {
        prototype: HTMLScarletDatePickerElement;
        new (): HTMLScarletDatePickerElement;
    };
    interface HTMLScarletDateRangePickerElementEventMap {
        "scarletChange": ScarletDateRangeChange;
        "scarletShow": void;
        "scarletHide": void;
    }
    /**
     * Two `DD/MM/AAAA` fields (start/end) sharing one calendar popover for
     * picking both ends of a range — built the same way `scarlet-date-picker`
     * is (same masking/validation per field, same popover mechanics), doubled.
     * See that component's own doc comment for the shared known limitations
     * (no Tab-out auto-close, no viewport flip).
     * Picking works the classic two-click way: the first day clicked becomes
     * the start (clearing any previous end); the second becomes the end and
     * closes the popover — unless it's *before* the start, in which case it
     * becomes the new start instead and the popover stays open for the end.
     */
    interface HTMLScarletDateRangePickerElement extends Components.ScarletDateRangePicker, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletDateRangePickerElementEventMap>(type: K, listener: (this: HTMLScarletDateRangePickerElement, ev: ScarletDateRangePickerCustomEvent<HTMLScarletDateRangePickerElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletDateRangePickerElementEventMap>(type: K, listener: (this: HTMLScarletDateRangePickerElement, ev: ScarletDateRangePickerCustomEvent<HTMLScarletDateRangePickerElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletDateRangePickerElement: {
        prototype: HTMLScarletDateRangePickerElement;
        new (): HTMLScarletDateRangePickerElement;
    };
    /**
     * A visual separator between sections of content, with an optional centered label.
     */
    interface HTMLScarletDividerElement extends Components.ScarletDivider, HTMLStencilElement {
    }
    var HTMLScarletDividerElement: {
        prototype: HTMLScarletDividerElement;
        new (): HTMLScarletDividerElement;
    };
    interface HTMLScarletDrawerElementEventMap {
        "scarletShow": void;
        "scarletClose": void;
    }
    /**
     * A panel that slides in from an edge of the screen — the mobile-friendlier
     * sibling of `scarlet-modal` for a filter panel, a form, a details view.
     * Built the same way `scarlet-modal` is, on the native `<dialog>` element,
     * for the same free focus trapping/top-layer stacking/Escape handling — see
     * that component's own comments for the mechanics shared here (focus
     * restore, the cancelable `scarletClose`, `show()`/`hide()`).
     * `size` sets the dimension along the axis the drawer slides on: width for
     * `left`/`right`, height for `top`/`bottom`.
     */
    interface HTMLScarletDrawerElement extends Components.ScarletDrawer, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletDrawerElementEventMap>(type: K, listener: (this: HTMLScarletDrawerElement, ev: ScarletDrawerCustomEvent<HTMLScarletDrawerElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletDrawerElementEventMap>(type: K, listener: (this: HTMLScarletDrawerElement, ev: ScarletDrawerCustomEvent<HTMLScarletDrawerElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletDrawerElement: {
        prototype: HTMLScarletDrawerElement;
        new (): HTMLScarletDrawerElement;
    };
    /**
     * A placeholder for a list, search result, or section with nothing to
     * show — an icon, a heading, an optional description, and an optional
     * action (e.g. "Limpar filtros" or "Criar o primeiro item").
     */
    interface HTMLScarletEmptyStateElement extends Components.ScarletEmptyState, HTMLStencilElement {
    }
    var HTMLScarletEmptyStateElement: {
        prototype: HTMLScarletEmptyStateElement;
        new (): HTMLScarletEmptyStateElement;
    };
    interface HTMLScarletFileUploadElementEventMap {
        "scarletChange": File[];
    }
    /**
     * A drag-and-drop file picker, backed by a real (visually hidden) native
     * `<input type="file">` — so it's a real form control and works with no JS
     * at all for the "click to browse" path; only the drag-and-drop layer is
     * enhancement on top.
     */
    interface HTMLScarletFileUploadElement extends Components.ScarletFileUpload, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletFileUploadElementEventMap>(type: K, listener: (this: HTMLScarletFileUploadElement, ev: ScarletFileUploadCustomEvent<HTMLScarletFileUploadElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletFileUploadElementEventMap>(type: K, listener: (this: HTMLScarletFileUploadElement, ev: ScarletFileUploadCustomEvent<HTMLScarletFileUploadElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletFileUploadElement: {
        prototype: HTMLScarletFileUploadElement;
        new (): HTMLScarletFileUploadElement;
    };
    /**
     * A CSS grid layout primitive with equal-width columns and token-based
     * gaps. Pair with `<scarlet-grid-item>` to span multiple columns/rows.
     * `columns` can change per breakpoint via `columnsSm`/`columnsMd`/
     * `columnsLg`/`columnsXl` — each one only takes effect from its breakpoint
     * up and, left unset, falls back to the next smaller breakpoint that *is*
     * set (mobile-first cascade), down to `columns` itself. E.g. `columns={1}
     * columns-md={3}` renders 1 column below 768px and 3 from 768px up, with
     * no separate `sm`/`lg`/`xl` value needed.
     */
    interface HTMLScarletGridElement extends Components.ScarletGrid, HTMLStencilElement {
    }
    var HTMLScarletGridElement: {
        prototype: HTMLScarletGridElement;
        new (): HTMLScarletGridElement;
    };
    /**
     * A cell inside a `<scarlet-grid>` that can span multiple columns/rows.
     * `colSpan`/`rowSpan` can change per breakpoint via `colSpanSm`/`colSpanMd`/
     * `colSpanLg`/`colSpanXl` (and the `rowSpan` equivalents) — the classic "full
     * row on mobile, a couple columns on desktop" pattern is
     * `col-span="{columns}" col-span-md="2"`. Each one only takes effect from
     * its breakpoint up and, left unset, falls back to the next smaller
     * breakpoint that *is* set (mobile-first cascade, same as `<scarlet-grid>`'s
     * own `columns`/`columnsSm`/etc.), down to `colSpan`/`rowSpan` themselves.
     */
    interface HTMLScarletGridItemElement extends Components.ScarletGridItem, HTMLStencilElement {
    }
    var HTMLScarletGridItemElement: {
        prototype: HTMLScarletGridItemElement;
        new (): HTMLScarletGridItemElement;
    };
    /**
     * A semantic heading (`<h1>`–`<h6>`) with an independently controllable
     * visual size — document structure (`level`) and visual hierarchy
     * (`variant`) don't have to match one-to-one.
     */
    interface HTMLScarletHeadingElement extends Components.ScarletHeading, HTMLStencilElement {
    }
    var HTMLScarletHeadingElement: {
        prototype: HTMLScarletHeadingElement;
        new (): HTMLScarletHeadingElement;
    };
    /**
     * Renders an icon from the design system's shared built-in set. Icons are
     * stroke-based and inherit `color`/`font-size` from their context, so they
     * line up with surrounding text and other components by default.
     */
    interface HTMLScarletIconElement extends Components.ScarletIcon, HTMLStencilElement {
    }
    var HTMLScarletIconElement: {
        prototype: HTMLScarletIconElement;
        new (): HTMLScarletIconElement;
    };
    interface HTMLScarletInputElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
    }
    /**
     * A labeled text input with helper/error text and built-in accessibility wiring.
     */
    interface HTMLScarletInputElement extends Components.ScarletInput, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletInputElementEventMap>(type: K, listener: (this: HTMLScarletInputElement, ev: ScarletInputCustomEvent<HTMLScarletInputElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletInputElementEventMap>(type: K, listener: (this: HTMLScarletInputElement, ev: ScarletInputCustomEvent<HTMLScarletInputElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletInputElement: {
        prototype: HTMLScarletInputElement;
        new (): HTMLScarletInputElement;
    };
    interface HTMLScarletInputCepElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletComplete": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
    }
    /**
     * A Brazilian postal code (CEP) input — formats as `01310-100`. This
     * component only formats the value; looking up the matching address (e.g.
     * via ViaCEP) is the consuming app's responsibility — listen for
     * `scarletChange` and call your own API with `getRawValue()`.
     */
    interface HTMLScarletInputCepElement extends Components.ScarletInputCep, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletInputCepElementEventMap>(type: K, listener: (this: HTMLScarletInputCepElement, ev: ScarletInputCepCustomEvent<HTMLScarletInputCepElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletInputCepElementEventMap>(type: K, listener: (this: HTMLScarletInputCepElement, ev: ScarletInputCepCustomEvent<HTMLScarletInputCepElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletInputCepElement: {
        prototype: HTMLScarletInputCepElement;
        new (): HTMLScarletInputCepElement;
    };
    interface HTMLScarletInputCreditCardElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
        "scarletValidityChange": boolean;
        "scarletBrandChange": CreditCardBrand | undefined;
    }
    /**
     * A credit card number input — formats in groups of 4 (or Amex's 4-6-5 /
     * Diners' 4-6-4 grouping once that brand is detected from the card's
     * BIN/IIN prefix), with a Luhn checksum validation on blur.
     */
    interface HTMLScarletInputCreditCardElement extends Components.ScarletInputCreditCard, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletInputCreditCardElementEventMap>(type: K, listener: (this: HTMLScarletInputCreditCardElement, ev: ScarletInputCreditCardCustomEvent<HTMLScarletInputCreditCardElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletInputCreditCardElementEventMap>(type: K, listener: (this: HTMLScarletInputCreditCardElement, ev: ScarletInputCreditCardCustomEvent<HTMLScarletInputCreditCardElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletInputCreditCardElement: {
        prototype: HTMLScarletInputCreditCardElement;
        new (): HTMLScarletInputCreditCardElement;
    };
    interface HTMLScarletInputCurrencyElementEventMap {
        "scarletInput": number;
        "scarletChange": number;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
    }
    /**
     * A monetary input that formats digits as currency growing from the right
     * (like a card machine) — typing "1234" produces "R$ 12,34". Unlike the
     * other masked inputs, `scarletInput`/`scarletChange` emit the plain
     * **numeric** amount (e.g. `12.34`), not the formatted string — that's
     * almost always what you actually want from a money field. Read the
     * formatted text itself via `value`.
     */
    interface HTMLScarletInputCurrencyElement extends Components.ScarletInputCurrency, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletInputCurrencyElementEventMap>(type: K, listener: (this: HTMLScarletInputCurrencyElement, ev: ScarletInputCurrencyCustomEvent<HTMLScarletInputCurrencyElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletInputCurrencyElementEventMap>(type: K, listener: (this: HTMLScarletInputCurrencyElement, ev: ScarletInputCurrencyCustomEvent<HTMLScarletInputCurrencyElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletInputCurrencyElement: {
        prototype: HTMLScarletInputCurrencyElement;
        new (): HTMLScarletInputCurrencyElement;
    };
    interface HTMLScarletInputDateElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
        "scarletValidityChange": boolean;
    }
    /**
     * A `DD/MM/AAAA` date input. Unlike a plain positional mask, `validate`
     * checks it's a *real* calendar date (rejects `31/02/2026`, honors leap
     * years) on blur, not just that 8 digits were typed.
     */
    interface HTMLScarletInputDateElement extends Components.ScarletInputDate, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletInputDateElementEventMap>(type: K, listener: (this: HTMLScarletInputDateElement, ev: ScarletInputDateCustomEvent<HTMLScarletInputDateElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletInputDateElementEventMap>(type: K, listener: (this: HTMLScarletInputDateElement, ev: ScarletInputDateCustomEvent<HTMLScarletInputDateElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletInputDateElement: {
        prototype: HTMLScarletInputDateElement;
        new (): HTMLScarletInputDateElement;
    };
    interface HTMLScarletInputDocumentElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
        "scarletValidityChange": boolean;
    }
    /**
     * A single "CPF/CNPJ" field that auto-detects and formats whichever
     * document type is being typed (11 digits → CPF `123.456.789-01`, 12+ →
     * CNPJ `12.345.678/0001-90`) — the common pattern for a field that accepts
     * either an individual or a company.
     */
    interface HTMLScarletInputDocumentElement extends Components.ScarletInputDocument, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletInputDocumentElementEventMap>(type: K, listener: (this: HTMLScarletInputDocumentElement, ev: ScarletInputDocumentCustomEvent<HTMLScarletInputDocumentElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletInputDocumentElementEventMap>(type: K, listener: (this: HTMLScarletInputDocumentElement, ev: ScarletInputDocumentCustomEvent<HTMLScarletInputDocumentElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletInputDocumentElement: {
        prototype: HTMLScarletInputDocumentElement;
        new (): HTMLScarletInputDocumentElement;
    };
    interface HTMLScarletInputLicensePlateElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
    }
    /**
     * A Brazilian vehicle plate input — formats as the old `ABC-1234` pattern
     * or the newer Mercosul `ABC1D23` pattern, detected automatically from
     * whether a letter or digit lands in the 5th character.
     */
    interface HTMLScarletInputLicensePlateElement extends Components.ScarletInputLicensePlate, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletInputLicensePlateElementEventMap>(type: K, listener: (this: HTMLScarletInputLicensePlateElement, ev: ScarletInputLicensePlateCustomEvent<HTMLScarletInputLicensePlateElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletInputLicensePlateElementEventMap>(type: K, listener: (this: HTMLScarletInputLicensePlateElement, ev: ScarletInputLicensePlateCustomEvent<HTMLScarletInputLicensePlateElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletInputLicensePlateElement: {
        prototype: HTMLScarletInputLicensePlateElement;
        new (): HTMLScarletInputLicensePlateElement;
    };
    interface HTMLScarletInputPercentageElementEventMap {
        "scarletInput": number;
        "scarletChange": number;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
    }
    /**
     * A percentage input that formats digits growing from the right, like
     * `scarlet-input-currency` — typing "1234" produces "12,34%". Emits the
     * plain numeric percentage (e.g. `12.34`) via `scarletInput`/`scarletChange`.
     */
    interface HTMLScarletInputPercentageElement extends Components.ScarletInputPercentage, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletInputPercentageElementEventMap>(type: K, listener: (this: HTMLScarletInputPercentageElement, ev: ScarletInputPercentageCustomEvent<HTMLScarletInputPercentageElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletInputPercentageElementEventMap>(type: K, listener: (this: HTMLScarletInputPercentageElement, ev: ScarletInputPercentageCustomEvent<HTMLScarletInputPercentageElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletInputPercentageElement: {
        prototype: HTMLScarletInputPercentageElement;
        new (): HTMLScarletInputPercentageElement;
    };
    interface HTMLScarletInputPhoneElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
    }
    /**
     * A Brazilian phone number input — formats as `(11) 91234-5678` (mobile,
     * 11 digits) or `(11) 1234-5678` (landline, 10 digits), switching
     * automatically as the user types.
     */
    interface HTMLScarletInputPhoneElement extends Components.ScarletInputPhone, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletInputPhoneElementEventMap>(type: K, listener: (this: HTMLScarletInputPhoneElement, ev: ScarletInputPhoneCustomEvent<HTMLScarletInputPhoneElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletInputPhoneElementEventMap>(type: K, listener: (this: HTMLScarletInputPhoneElement, ev: ScarletInputPhoneCustomEvent<HTMLScarletInputPhoneElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletInputPhoneElement: {
        prototype: HTMLScarletInputPhoneElement;
        new (): HTMLScarletInputPhoneElement;
    };
    /**
     * A styled inline text link — for a link inside a sentence/paragraph.
     * `scarlet-button variant="link"` is the button-semantics equivalent (an
     * action styled like a link); this is the reverse, a real `<a>` styled like
     * one. `target="_blank"` automatically gets `rel="noopener noreferrer"`
     * (unless `rel` is set explicitly) and a small external-link icon.
     */
    interface HTMLScarletLinkElement extends Components.ScarletLink, HTMLStencilElement {
    }
    var HTMLScarletLinkElement: {
        prototype: HTMLScarletLinkElement;
        new (): HTMLScarletLinkElement;
    };
    interface HTMLScarletMenuElementEventMap {
        "scarletSelect": ScarletMenuItem;
        "scarletShow": void;
        "scarletHide": void;
    }
    /**
     * A dropdown menu of actions opened from an arbitrary trigger element (the
     * `trigger` slot — typically an icon button like "⋮"), following the
     * WAI-ARIA menu button pattern: the trigger gets `aria-haspopup="menu"`/
     * `aria-expanded` (set directly on whatever element is slotted in), the
     * popover is `role="menu"` with `role="menuitem"` buttons, arrow keys move
     * a roving tab stop within the list, Home/End jump to the first/last enabled
     * item, Escape closes and returns focus to the trigger. Enter/Space opening
     * the menu is *not* handled here — the trigger must be a real, natively
     * activatable element (a `<button>` or `<scarlet-button>`); its own native
     * click already opens the menu, and re-handling those keys here would fire
     * twice (open, then immediately close again from the resulting click).
     * Known limitation: like `scarlet-tooltip`/`scarlet-date-picker`,
     * positioning is plain CSS anchored to the trigger — it doesn't flip or
     * reposition to stay in the viewport, and it doesn't close on Tab-ing past
     * its last item.
     */
    interface HTMLScarletMenuElement extends Components.ScarletMenu, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletMenuElementEventMap>(type: K, listener: (this: HTMLScarletMenuElement, ev: ScarletMenuCustomEvent<HTMLScarletMenuElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletMenuElementEventMap>(type: K, listener: (this: HTMLScarletMenuElement, ev: ScarletMenuCustomEvent<HTMLScarletMenuElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletMenuElement: {
        prototype: HTMLScarletMenuElement;
        new (): HTMLScarletMenuElement;
    };
    interface HTMLScarletModalElementEventMap {
        "scarletShow": void;
        "scarletClose": void;
    }
    /**
     * A modal dialog built on the native `<dialog>` element, which provides
     * focus trapping, top-layer stacking and Escape handling for free.
     */
    interface HTMLScarletModalElement extends Components.ScarletModal, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletModalElementEventMap>(type: K, listener: (this: HTMLScarletModalElement, ev: ScarletModalCustomEvent<HTMLScarletModalElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletModalElementEventMap>(type: K, listener: (this: HTMLScarletModalElement, ev: ScarletModalCustomEvent<HTMLScarletModalElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletModalElement: {
        prototype: HTMLScarletModalElement;
        new (): HTMLScarletModalElement;
    };
    interface HTMLScarletNumberInputElementEventMap {
        "scarletInput": number;
        "scarletChange": number;
    }
    /**
     * A numeric input with decrement/increment buttons — for a quantity field,
     * not a general-purpose text field that happens to hold numbers (that's
     * `scarlet-input type="number"`). Stays within `min`/`max` on every path:
     * the buttons, typing, and blur all clamp.
     */
    interface HTMLScarletNumberInputElement extends Components.ScarletNumberInput, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletNumberInputElementEventMap>(type: K, listener: (this: HTMLScarletNumberInputElement, ev: ScarletNumberInputCustomEvent<HTMLScarletNumberInputElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletNumberInputElementEventMap>(type: K, listener: (this: HTMLScarletNumberInputElement, ev: ScarletNumberInputCustomEvent<HTMLScarletNumberInputElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletNumberInputElement: {
        prototype: HTMLScarletNumberInputElement;
        new (): HTMLScarletNumberInputElement;
    };
    interface HTMLScarletPaginationElementEventMap {
        "scarletChange": number;
    }
    /**
     * Page number navigation, following the WAI-ARIA pattern of a `<nav>`
     * landmark wrapping a list of buttons — the current page is a real button
     * (not a link, since this component doesn't own routing) marked
     * `aria-current="page"`. Collapses distant page numbers into an ellipsis
     * once `totalPages` is large, always keeping the first page, the last page,
     * and `siblingCount` pages on each side of the current one visible.
     */
    interface HTMLScarletPaginationElement extends Components.ScarletPagination, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletPaginationElementEventMap>(type: K, listener: (this: HTMLScarletPaginationElement, ev: ScarletPaginationCustomEvent<HTMLScarletPaginationElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletPaginationElementEventMap>(type: K, listener: (this: HTMLScarletPaginationElement, ev: ScarletPaginationCustomEvent<HTMLScarletPaginationElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletPaginationElement: {
        prototype: HTMLScarletPaginationElement;
        new (): HTMLScarletPaginationElement;
    };
    interface HTMLScarletPopoverElementEventMap {
        "scarletShow": void;
        "scarletHide": void;
    }
    /**
     * A generic overlay anchored to a trigger element (the `trigger` slot),
     * with arbitrary content in the default slot — the shared primitive
     * `scarlet-menu`/`scarlet-date-picker`/`scarlet-combobox` each built their
     * own narrower version of for their specific popovers. Use this one
     * directly for anything that doesn't need a menu list or a calendar grid:
     * a rich tooltip, a filter panel, a confirmation prompt.
     * `trigger="click"` (the default) toggles on click, closes on Escape or a
     * click outside, and sets `aria-haspopup`/`aria-expanded` on whatever's
     * slotted as the trigger. `trigger="hover"` opens/closes on mouse enter/
     * leave instead, with no click/Escape handling — matching how a hover
     * tooltip behaves, not a dialog.
     * Known limitation: like `scarlet-tooltip`/`scarlet-menu`, positioning is
     * plain CSS anchored to the trigger — it doesn't flip or reposition to stay
     * in the viewport.
     */
    interface HTMLScarletPopoverElement extends Components.ScarletPopover, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletPopoverElementEventMap>(type: K, listener: (this: HTMLScarletPopoverElement, ev: ScarletPopoverCustomEvent<HTMLScarletPopoverElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletPopoverElementEventMap>(type: K, listener: (this: HTMLScarletPopoverElement, ev: ScarletPopoverCustomEvent<HTMLScarletPopoverElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletPopoverElement: {
        prototype: HTMLScarletPopoverElement;
        new (): HTMLScarletPopoverElement;
    };
    /**
     * A determinate progress bar (0–100%) — for a file upload, a multi-step
     * form, anything with a real, known completion percentage. For an
     * indeterminate "something's happening" state, use `scarlet-spinner`.
     */
    interface HTMLScarletProgressElement extends Components.ScarletProgress, HTMLStencilElement {
    }
    var HTMLScarletProgressElement: {
        prototype: HTMLScarletProgressElement;
        new (): HTMLScarletProgressElement;
    };
    interface HTMLScarletRadioElementEventMap {
        "scarletChange": boolean;
        "scarletRadioChange": boolean;
    }
    /**
     * A single radio option. Use inside a `<scarlet-radio-group>` for mutually
     * exclusive selection — standalone, it behaves like an isolated toggle.
     */
    interface HTMLScarletRadioElement extends Components.ScarletRadio, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletRadioElementEventMap>(type: K, listener: (this: HTMLScarletRadioElement, ev: ScarletRadioCustomEvent<HTMLScarletRadioElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletRadioElementEventMap>(type: K, listener: (this: HTMLScarletRadioElement, ev: ScarletRadioCustomEvent<HTMLScarletRadioElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletRadioElement: {
        prototype: HTMLScarletRadioElement;
        new (): HTMLScarletRadioElement;
    };
    interface HTMLScarletRadioGroupElementEventMap {
        "scarletChange": string | undefined;
    }
    /**
     * Groups a set of `<scarlet-radio>` options and enforces single selection —
     * native browser radio grouping does not cross shadow-DOM boundaries, so
     * this component coordinates `checked`/`name`/`disabled` across its children.
     */
    interface HTMLScarletRadioGroupElement extends Components.ScarletRadioGroup, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletRadioGroupElementEventMap>(type: K, listener: (this: HTMLScarletRadioGroupElement, ev: ScarletRadioGroupCustomEvent<HTMLScarletRadioGroupElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletRadioGroupElementEventMap>(type: K, listener: (this: HTMLScarletRadioGroupElement, ev: ScarletRadioGroupCustomEvent<HTMLScarletRadioGroupElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletRadioGroupElement: {
        prototype: HTMLScarletRadioGroupElement;
        new (): HTMLScarletRadioGroupElement;
    };
    interface HTMLScarletSelectElementEventMap {
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
    }
    /**
     * A labeled native `<select>` dropdown with helper/error text and built-in
     * accessibility wiring. Options are passed as a `options` property (an
     * array), not slotted — native `<select>` cannot reliably project slotted
     * `<option>` elements into its picker UI across browsers.
     */
    interface HTMLScarletSelectElement extends Components.ScarletSelect, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletSelectElementEventMap>(type: K, listener: (this: HTMLScarletSelectElement, ev: ScarletSelectCustomEvent<HTMLScarletSelectElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletSelectElementEventMap>(type: K, listener: (this: HTMLScarletSelectElement, ev: ScarletSelectCustomEvent<HTMLScarletSelectElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletSelectElement: {
        prototype: HTMLScarletSelectElement;
        new (): HTMLScarletSelectElement;
    };
    /**
     * A loading placeholder shape — swap it in for content that hasn't arrived
     * yet (a card, an avatar, a paragraph). Purely decorative (`aria-hidden`):
     * announcing the *loading* state itself (e.g. `aria-busy` on the section
     * being replaced) is the consumer's responsibility, not this component's.
     */
    interface HTMLScarletSkeletonElement extends Components.ScarletSkeleton, HTMLStencilElement {
    }
    var HTMLScarletSkeletonElement: {
        prototype: HTMLScarletSkeletonElement;
        new (): HTMLScarletSkeletonElement;
    };
    /**
     * A loading indicator — `variant="circle"` for the generic spinner (the
     * same shape `scarlet-button`'s own `loading` state uses), `variant="logo"`
     * for the Scarlet mark itself pulsing, e.g. for a full-page loading state.
     * `role="status"` with `label` as its accessible name, so assistive tech
     * announces the loading state without needing separate visible text.
     */
    interface HTMLScarletSpinnerElement extends Components.ScarletSpinner, HTMLStencilElement {
    }
    var HTMLScarletSpinnerElement: {
        prototype: HTMLScarletSpinnerElement;
        new (): HTMLScarletSpinnerElement;
    };
    /**
     * A flexbox layout primitive for stacking or rowing children with
     * consistent, token-based spacing.
     * `direction` can change per breakpoint via `directionSm`/`directionMd`/
     * `directionLg`/`directionXl` — the classic "stacked on mobile, row on
     * desktop" pattern is `direction="column" direction-md="row"`. Each one
     * only takes effect from its breakpoint up and, left unset, falls back to
     * the next smaller breakpoint that *is* set (mobile-first cascade), down
     * to `direction` itself.
     */
    interface HTMLScarletStackElement extends Components.ScarletStack, HTMLStencilElement {
    }
    var HTMLScarletStackElement: {
        prototype: HTMLScarletStackElement;
        new (): HTMLScarletStackElement;
    };
    /**
     * A single labeled metric — a number/value, its label, and an optional
     * change indicator (e.g. "+12%" with an up/down arrow) for a dashboard.
     */
    interface HTMLScarletStatElement extends Components.ScarletStat, HTMLStencilElement {
    }
    var HTMLScarletStatElement: {
        prototype: HTMLScarletStatElement;
        new (): HTMLScarletStatElement;
    };
    interface HTMLScarletSwitchElementEventMap {
        "scarletChange": boolean;
    }
    /**
     * A toggle switch for binary on/off settings.
     */
    interface HTMLScarletSwitchElement extends Components.ScarletSwitch, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletSwitchElementEventMap>(type: K, listener: (this: HTMLScarletSwitchElement, ev: ScarletSwitchCustomEvent<HTMLScarletSwitchElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletSwitchElementEventMap>(type: K, listener: (this: HTMLScarletSwitchElement, ev: ScarletSwitchCustomEvent<HTMLScarletSwitchElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletSwitchElement: {
        prototype: HTMLScarletSwitchElement;
        new (): HTMLScarletSwitchElement;
    };
    interface HTMLScarletTableElementEventMap {
        "scarletSort": ScarletTableSortChange;
        "scarletSelectionChange": Array<string | number>;
        "scarletRowClick": ScarletTableRow;
    }
    /**
     * A data table: sortable columns, optional row selection (checkboxes),
     * empty/loading states, and a horizontally-scrolling wrapper so a wide
     * table never breaks the surrounding layout on a narrow viewport (same
     * pattern `scarlet-tabs` uses for its tab list).
     * Cell content is `String(row[column.key])` by default — good enough for
     * plain text/numbers, but there's no per-cell slot system (that needs a
     * stable id per row and gets complex fast). For anything richer — a
     * `scarlet-badge` status, a formatted currency, a truncated link — pass
     * `formatCell`, a plain function `(row, column) => string` set as a JS
     * property (like `columns`/`rows` themselves, it isn't parseable from an
     * HTML attribute).
     * Sorting is handled internally using a locale-aware comparator (numeric
     * for `number` values, `localeCompare` otherwise) — good for typical
     * text/number columns without any setup. `scarletSort` still fires on every
     * click if a consumer wants to replace that with their own comparator (e.g.
     * sorting a formatted date column by its real underlying timestamp) by
     * reassigning `rows` themselves in response.
     */
    interface HTMLScarletTableElement extends Components.ScarletTable, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletTableElementEventMap>(type: K, listener: (this: HTMLScarletTableElement, ev: ScarletTableCustomEvent<HTMLScarletTableElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletTableElementEventMap>(type: K, listener: (this: HTMLScarletTableElement, ev: ScarletTableCustomEvent<HTMLScarletTableElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletTableElement: {
        prototype: HTMLScarletTableElement;
        new (): HTMLScarletTableElement;
    };
    interface HTMLScarletTabsElementEventMap {
        "scarletChange": string;
    }
    /**
     * A set of tabs with associated panels. Panel content is projected via a
     * named slot per item, matching `item.value`.
     */
    interface HTMLScarletTabsElement extends Components.ScarletTabs, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletTabsElementEventMap>(type: K, listener: (this: HTMLScarletTabsElement, ev: ScarletTabsCustomEvent<HTMLScarletTabsElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletTabsElementEventMap>(type: K, listener: (this: HTMLScarletTabsElement, ev: ScarletTabsCustomEvent<HTMLScarletTabsElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletTabsElement: {
        prototype: HTMLScarletTabsElement;
        new (): HTMLScarletTabsElement;
    };
    /**
     * Body copy with the design system's text scale, color tokens and
     * single-line truncation — the general-purpose counterpart to `scarlet-heading`.
     */
    interface HTMLScarletTextElement extends Components.ScarletText, HTMLStencilElement {
    }
    var HTMLScarletTextElement: {
        prototype: HTMLScarletTextElement;
        new (): HTMLScarletTextElement;
    };
    interface HTMLScarletTextareaElementEventMap {
        "scarletInput": string;
        "scarletChange": string;
        "scarletFocus": FocusEvent;
        "scarletBlur": FocusEvent;
    }
    /**
     * A multi-line labeled text input with helper/error text and built-in accessibility wiring.
     */
    interface HTMLScarletTextareaElement extends Components.ScarletTextarea, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletTextareaElementEventMap>(type: K, listener: (this: HTMLScarletTextareaElement, ev: ScarletTextareaCustomEvent<HTMLScarletTextareaElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletTextareaElementEventMap>(type: K, listener: (this: HTMLScarletTextareaElement, ev: ScarletTextareaCustomEvent<HTMLScarletTextareaElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletTextareaElement: {
        prototype: HTMLScarletTextareaElement;
        new (): HTMLScarletTextareaElement;
    };
    /**
     * A vertical sequence of events — an order's status history, an activity
     * feed, a step-by-step audit trail.
     */
    interface HTMLScarletTimelineElement extends Components.ScarletTimeline, HTMLStencilElement {
    }
    var HTMLScarletTimelineElement: {
        prototype: HTMLScarletTimelineElement;
        new (): HTMLScarletTimelineElement;
    };
    interface HTMLScarletToastElementEventMap {
        "scarletDismiss": void;
    }
    /**
     * A self-dismissing status message, fixed to a corner of the viewport.
     * For multiple simultaneous toasts, render several instances inside a
     * consumer-owned stacking container (this component does not manage a queue).
     */
    interface HTMLScarletToastElement extends Components.ScarletToast, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletToastElementEventMap>(type: K, listener: (this: HTMLScarletToastElement, ev: ScarletToastCustomEvent<HTMLScarletToastElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletToastElementEventMap>(type: K, listener: (this: HTMLScarletToastElement, ev: ScarletToastCustomEvent<HTMLScarletToastElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletToastElement: {
        prototype: HTMLScarletToastElement;
        new (): HTMLScarletToastElement;
    };
    /**
     * A horizontal bar for grouping related actions (buttons, a search field,
     * an icon menu) — `role="toolbar"` with token-based gap, distinct from
     * `scarlet-stack` in intent (actions, not arbitrary layout) even though
     * the underlying flex mechanics are similar.
     */
    interface HTMLScarletToolbarElement extends Components.ScarletToolbar, HTMLStencilElement {
    }
    var HTMLScarletToolbarElement: {
        prototype: HTMLScarletToolbarElement;
        new (): HTMLScarletToolbarElement;
    };
    interface HTMLScarletTooltipElementEventMap {
        "scarletShow": void;
        "scarletHide": void;
    }
    /**
     * A text hint shown next to its trigger on hover/focus. Positioning is
     * plain CSS anchored to the host — it does not flip to stay in the
     * viewport, so pick a `placement` that has room to render.
     */
    interface HTMLScarletTooltipElement extends Components.ScarletTooltip, HTMLStencilElement {
        addEventListener<K extends keyof HTMLScarletTooltipElementEventMap>(type: K, listener: (this: HTMLScarletTooltipElement, ev: ScarletTooltipCustomEvent<HTMLScarletTooltipElementEventMap[K]>) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | AddEventListenerOptions): void;
        addEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | AddEventListenerOptions): void;
        removeEventListener<K extends keyof HTMLScarletTooltipElementEventMap>(type: K, listener: (this: HTMLScarletTooltipElement, ev: ScarletTooltipCustomEvent<HTMLScarletTooltipElementEventMap[K]>) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof DocumentEventMap>(type: K, listener: (this: Document, ev: DocumentEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener<K extends keyof HTMLElementEventMap>(type: K, listener: (this: HTMLElement, ev: HTMLElementEventMap[K]) => any, options?: boolean | EventListenerOptions): void;
        removeEventListener(type: string, listener: EventListenerOrEventListenerObject, options?: boolean | EventListenerOptions): void;
    }
    var HTMLScarletTooltipElement: {
        prototype: HTMLScarletTooltipElement;
        new (): HTMLScarletTooltipElement;
    };
    interface HTMLElementTagNameMap {
        "scarlet-accordion": HTMLScarletAccordionElement;
        "scarlet-alert": HTMLScarletAlertElement;
        "scarlet-avatar": HTMLScarletAvatarElement;
        "scarlet-avatar-group": HTMLScarletAvatarGroupElement;
        "scarlet-badge": HTMLScarletBadgeElement;
        "scarlet-breadcrumb": HTMLScarletBreadcrumbElement;
        "scarlet-button": HTMLScarletButtonElement;
        "scarlet-card": HTMLScarletCardElement;
        "scarlet-checkbox": HTMLScarletCheckboxElement;
        "scarlet-checkbox-group": HTMLScarletCheckboxGroupElement;
        "scarlet-chip": HTMLScarletChipElement;
        "scarlet-combobox": HTMLScarletComboboxElement;
        "scarlet-container": HTMLScarletContainerElement;
        "scarlet-date-picker": HTMLScarletDatePickerElement;
        "scarlet-date-range-picker": HTMLScarletDateRangePickerElement;
        "scarlet-divider": HTMLScarletDividerElement;
        "scarlet-drawer": HTMLScarletDrawerElement;
        "scarlet-empty-state": HTMLScarletEmptyStateElement;
        "scarlet-file-upload": HTMLScarletFileUploadElement;
        "scarlet-grid": HTMLScarletGridElement;
        "scarlet-grid-item": HTMLScarletGridItemElement;
        "scarlet-heading": HTMLScarletHeadingElement;
        "scarlet-icon": HTMLScarletIconElement;
        "scarlet-input": HTMLScarletInputElement;
        "scarlet-input-cep": HTMLScarletInputCepElement;
        "scarlet-input-credit-card": HTMLScarletInputCreditCardElement;
        "scarlet-input-currency": HTMLScarletInputCurrencyElement;
        "scarlet-input-date": HTMLScarletInputDateElement;
        "scarlet-input-document": HTMLScarletInputDocumentElement;
        "scarlet-input-license-plate": HTMLScarletInputLicensePlateElement;
        "scarlet-input-percentage": HTMLScarletInputPercentageElement;
        "scarlet-input-phone": HTMLScarletInputPhoneElement;
        "scarlet-link": HTMLScarletLinkElement;
        "scarlet-menu": HTMLScarletMenuElement;
        "scarlet-modal": HTMLScarletModalElement;
        "scarlet-number-input": HTMLScarletNumberInputElement;
        "scarlet-pagination": HTMLScarletPaginationElement;
        "scarlet-popover": HTMLScarletPopoverElement;
        "scarlet-progress": HTMLScarletProgressElement;
        "scarlet-radio": HTMLScarletRadioElement;
        "scarlet-radio-group": HTMLScarletRadioGroupElement;
        "scarlet-select": HTMLScarletSelectElement;
        "scarlet-skeleton": HTMLScarletSkeletonElement;
        "scarlet-spinner": HTMLScarletSpinnerElement;
        "scarlet-stack": HTMLScarletStackElement;
        "scarlet-stat": HTMLScarletStatElement;
        "scarlet-switch": HTMLScarletSwitchElement;
        "scarlet-table": HTMLScarletTableElement;
        "scarlet-tabs": HTMLScarletTabsElement;
        "scarlet-text": HTMLScarletTextElement;
        "scarlet-textarea": HTMLScarletTextareaElement;
        "scarlet-timeline": HTMLScarletTimelineElement;
        "scarlet-toast": HTMLScarletToastElement;
        "scarlet-toolbar": HTMLScarletToolbarElement;
        "scarlet-tooltip": HTMLScarletTooltipElement;
    }
}
declare namespace LocalJSX {
    /**
     * A set of collapsible sections. Panel content is projected via a named
     * slot per item, matching `item.value`.
     */
    interface ScarletAccordion {
        /**
          * Values of the currently expanded sections.
          * @default []
         */
        "expandedValues"?: string[];
        /**
          * The sections to render.
          * @default []
         */
        "items"?: ScarletAccordionItem[];
        /**
          * Allows more than one section to be expanded at the same time.
          * @default false
         */
        "multiple"?: false;
        /**
          * Emitted when the set of expanded sections changes.
         */
        "onScarletChange"?: (event: ScarletAccordionCustomEvent<string[]>) => void;
    }
    /**
     * An inline status message with optional title, icon and dismiss action.
     */
    interface ScarletAlert {
        /**
          * Shows a dismiss (close) button.
          * @default false
         */
        "dismissible"?: false;
        /**
          * Shows a status icon before the content.
          * @default true
         */
        "icon"?: true;
        /**
          * Emitted when the dismiss button is activated. The event is cancelable: call `event.preventDefault()` to keep the alert visible and handle removal yourself (e.g. to run a custom exit animation).
         */
        "onScarletDismiss"?: (event: ScarletAlertCustomEvent<void>) => void;
        /**
          * Semantic status of the alert.
          * @default 'info'
         */
        "status"?: ScarletAlertStatus;
        /**
          * Visual style of the alert.
          * @default 'soft'
         */
        "variant"?: ScarletAlertVariant;
    }
    /**
     * A user/entity avatar: shows an image, falling back to initials derived
     * from `name`, falling back to a generic icon.
     */
    interface ScarletAvatar {
        /**
          * Accessible alt text for the image. Defaults to `name` when omitted.
         */
        "alt"?: string;
        /**
          * Full name used to derive initials and the default alt text.
         */
        "name"?: string;
        /**
          * Shape of the avatar.
          * @default 'circle'
         */
        "shape"?: ScarletAvatarShape;
        /**
          * Size of the avatar.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Image URL.
         */
        "src"?: string;
    }
    /**
     * Overlaps a set of `<scarlet-avatar>` children into a stack, with an
     * optional "+N" indicator when there are more than `max`. Propagates `size`
     * down to every avatar so they all render at the same size regardless of
     * what each one was given individually.
     */
    interface ScarletAvatarGroup {
        /**
          * Caps how many avatars are shown before the rest collapse into a "+N" indicator. Unset shows every avatar.
         */
        "max"?: number;
        /**
          * Size applied to every avatar in the group.
          * @default 'md'
         */
        "size"?: Size;
    }
    /**
     * A small status/count label, or a bare notification dot.
     */
    interface ScarletBadge {
        /**
          * Semantic color of the badge.
          * @default 'primary'
         */
        "color"?: Color;
        /**
          * Renders as a small notification dot instead of a text pill.
          * @default false
         */
        "dot"?: false;
        /**
          * Size of the badge.
          * @default 'sm'
         */
        "size"?: ScarletBadgeSize;
        /**
          * Visual style of the badge.
          * @default 'solid'
         */
        "variant"?: ScarletBadgeVariant;
    }
    /**
     * A trail of links showing the current page's position in a hierarchy,
     * following the WAI-ARIA breadcrumb pattern (`<nav aria-label>` wrapping an
     * ordered list, the last item marked `aria-current="page"`).
     * Items render as real `<a href>` elements — usable with no JS at all — but
     * every click first emits a cancelable `scarletNavigate`, so a framework
     * router can call `preventDefault()` and handle the navigation itself
     * instead of a full page load.
     */
    interface ScarletBreadcrumb {
        /**
          * Accessible label for the `<nav>` landmark.
          * @default 'Navegação estrutural'
         */
        "ariaLabel"?: "Navegação estrutural";
        /**
          * The trail of items, in order from the root down to the current page.
          * @default []
         */
        "items"?: ScarletBreadcrumbItem[];
        /**
          * Emitted when a link item is clicked, before the browser navigates. Cancelable: call `event.preventDefault()` (on the emitted `CustomEvent`, not the click) to stop the real navigation and handle it yourself, e.g. with a client-side router.
         */
        "onScarletNavigate"?: (event: ScarletBreadcrumbCustomEvent<ScarletBreadcrumbItem>) => void;
        /**
          * Text rendered between items. Purely decorative (hidden from assistive tech) — the DOM order already conveys the hierarchy.
          * @default '›'
         */
        "separator"?: "›";
    }
    /**
     * A clickable action element with solid, outline, ghost and link variants.
     * An icon (e.g. `<scarlet-icon>`) goes in the `start`/`end` slot alongside
     * the default slot's text — `<scarlet-button><scarlet-icon slot="start"
     * name="check" />Salvar</scarlet-button>`. For an icon with no visible text
     * at all, add `iconOnly` (which turns the button square instead of its
     * usual text-driven width) and `ariaLabel` (required then — nothing else
     * gives the button an accessible name).
     */
    interface ScarletButton {
        /**
          * Accessible label. Required when the button has no visible text (icon-only buttons).
         */
        "ariaLabel"?: string;
        /**
          * Semantic color of the button.
          * @default 'primary'
         */
        "color"?: Color;
        /**
          * Disables the button, preventing interaction and the `scarletClick` event.
          * @default false
         */
        "disabled"?: false;
        /**
          * Stretches the button to fill the width of its container.
          * @default false
         */
        "fullWidth"?: false;
        /**
          * Makes the button square (width matches its height) instead of sized to its text — for a button whose only content is an icon. Set `ariaLabel` alongside it.
          * @default false
         */
        "iconOnly"?: false;
        /**
          * Shows a loading spinner and blocks interaction, without changing layout width.
          * @default false
         */
        "loading"?: false;
        /**
          * Emitted when the button is activated by click or keyboard, and is not disabled/loading.
         */
        "onScarletClick"?: (event: ScarletButtonCustomEvent<MouseEvent>) => void;
        /**
          * Size of the button.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Native `type` attribute passed to the underlying `<button>`.
          * @default 'button'
         */
        "type"?: 'button' | 'submit' | 'reset';
        /**
          * Visual style of the button.
          * @default 'solid'
         */
        "variant"?: Variant;
    }
    /**
     * A container for grouping related content, with optional header/footer slots.
     */
    interface ScarletCard {
        /**
          * Makes the whole card behave as a single clickable/keyboard-activatable control.
          * @default false
         */
        "interactive"?: false;
        /**
          * Emitted when an interactive card is activated by click, Enter or Space.
         */
        "onScarletClick"?: (event: ScarletCardCustomEvent<MouseEvent | KeyboardEvent>) => void;
        /**
          * Internal padding of the card body.
          * @default 'md'
         */
        "padding"?: Size;
        /**
          * Visual style of the card.
          * @default 'elevated'
         */
        "variant"?: ScarletCardVariant;
    }
    /**
     * A checkbox input with label, indeterminate state and accessible wiring.
     */
    interface ScarletCheckbox {
        /**
          * Whether the checkbox is checked.
          * @default false
         */
        "checked"?: boolean;
        /**
          * Disables the checkbox.
          * @default false
         */
        "disabled"?: false;
        /**
          * Shows a visual "partially checked" state, independent of `checked`.
          * @default false
         */
        "indeterminate"?: false;
        /**
          * Visible label rendered next to the checkbox.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the checked state changes via user interaction. Doesn't bubble — mirrors `scarlet-radio`'s `scarletChange`: a parent `<scarlet-checkbox-group>` re-emits its own `scarletChange` (string[] detail) whenever a child's checked state changes, and if this one bubbled too, a consumer listening on the *group* would catch both: this child event (boolean detail) and the group's own re-emission, firing twice with the wrong detail on the first hit.
         */
        "onScarletChange"?: (event: ScarletCheckboxCustomEvent<boolean>) => void;
        /**
          * Same payload as `scarletChange`, always bubbling — for a parent `<scarlet-checkbox-group>` to listen to internally instead of the public `scarletChange`.
         */
        "onScarletCheckboxChange"?: (event: ScarletCheckboxCustomEvent<boolean>) => void;
        /**
          * Marks the checkbox as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Value submitted with a parent form when checked.
         */
        "value"?: string;
    }
    /**
     * Groups a set of `<scarlet-checkbox>` options and coordinates which ones
     * are checked as a single `value` array — unlike `<scarlet-radio-group>`,
     * each checkbox keeps its own native Tab stop (WAI-ARIA's `group` role, not
     * `radiogroup`, doesn't call for roving tabindex/arrow-key navigation the
     * way a radio group does).
     */
    interface ScarletCheckboxGroup {
        /**
          * Accessible label for the group, when there is no visible heading nearby.
         */
        "ariaLabel"?: string;
        /**
          * Disables every checkbox in the group.
          * @default false
         */
        "disabled"?: false;
        /**
          * Lays the checkboxes out in a row instead of stacked.
          * @default false
         */
        "horizontal"?: false;
        /**
          * Name applied to every checkbox in the group, so they submit together in a form.
         */
        "name"?: string;
        /**
          * Emitted when the set of checked values changes.
         */
        "onScarletChange"?: (event: ScarletCheckboxGroupCustomEvent<string[]>) => void;
        /**
          * Values of the currently checked checkboxes.
          * @default []
         */
        "value"?: string[];
    }
    /**
     * A small, dismissible tag — for an active filter, a selected option, an
     * added item in a multi-value input. Not a `<scarlet-badge>` with a close
     * button bolted on: unlike a badge, this is meant to be interactive.
     */
    interface ScarletChip {
        /**
          * Semantic color of the chip.
          * @default 'neutral'
         */
        "color"?: Color;
        /**
          * Disables the remove button and dims the chip.
          * @default false
         */
        "disabled"?: false;
        /**
          * Emitted when the remove button is activated. The chip does not remove itself — the consumer owns the list it came from.
         */
        "onScarletRemove"?: (event: ScarletChipCustomEvent<void>) => void;
        /**
          * Shows a remove ("x") button.
          * @default false
         */
        "removable"?: false;
        /**
          * Visual style of the chip.
          * @default 'soft'
         */
        "variant"?: ScarletChipVariant;
    }
    /**
     * A searchable select — type to filter a list of options, following the
     * WAI-ARIA 1.2 "editable combobox with list autocomplete" pattern. Unlike
     * `scarlet-menu`/`scarlet-date-picker`'s popovers, DOM focus never leaves
     * the text input while the list is open: the highlighted option is tracked
     * with `aria-activedescendant` instead of moving real focus button-to-
     * button, so the user can keep typing without ever losing their place.
     * Known limitation: like `scarlet-tooltip`/`scarlet-date-picker`/
     * `scarlet-menu`, positioning is plain CSS anchored to the field — it
     * doesn't flip or reposition to stay in the viewport.
     */
    interface ScarletCombobox {
        /**
          * Disables the field.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the field. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the field. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the field as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the field.
         */
        "label"?: string;
        /**
          * Message shown in the list when no option matches the current query.
          * @default 'Nenhum resultado encontrado.'
         */
        "noResultsMessage"?: "Nenhum resultado encontrado.";
        /**
          * Emitted when the field loses focus.
         */
        "onScarletBlur"?: (event: ScarletComboboxCustomEvent<FocusEvent>) => void;
        /**
          * Emitted with the newly selected option's value when an option is picked.
         */
        "onScarletChange"?: (event: ScarletComboboxCustomEvent<string>) => void;
        /**
          * Emitted when the field gains focus.
         */
        "onScarletFocus"?: (event: ScarletComboboxCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current query text (not the selected value).
         */
        "onScarletInput"?: (event: ScarletComboboxCustomEvent<string>) => void;
        /**
          * The options to search through.
          * @default []
         */
        "options"?: ScarletComboboxOption[];
        /**
          * Placeholder text shown when nothing is typed.
         */
        "placeholder"?: string;
        /**
          * Marks the field as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the field.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Value of the currently selected option.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A centered, max-width content wrapper — the outermost layout primitive
     * for page sections.
     */
    interface ScarletContainer {
        /**
          * Centers the container horizontally within its parent.
          * @default true
         */
        "center"?: true;
        /**
          * Maximum width of the container.
          * @default 'lg'
         */
        "maxWidth"?: ScarletContainerMaxWidth;
        /**
          * Adds horizontal padding.
          * @default true
         */
        "padding"?: true;
    }
    /**
     * A `DD/MM/AAAA` date input (typing, masking and calendar validation shared
     * with `scarlet-input-date`) plus a calendar popover for picking a date
     * visually — a button next to the field opens a month grid; arrow keys move
     * within it (Home/End jump to the start/end of the week, PageUp/PageDown
     * change month, Shift+PageUp/PageDown change year), Enter/Space picks the
     * focused day, Escape closes and returns focus to the toggle button.
     * Known limitations:
     * - The popover closes on Escape, on picking a day, or on clicking outside
     *   it — not on Tab-ing past its last focusable element. A keyboard user who
     *   tabs out instead of pressing Escape will move focus past the component
     *   with the panel still visually open.
     * - Like `scarlet-tooltip`, positioning is plain CSS anchored to the host —
     *   it never flips or shifts to stay in the viewport. It's shrunk to never
     *   exceed the viewport's own width, so it can't run off the right edge of
     *   the *screen*, but placing the field near that edge can still overflow
     *   past its own container.
     */
    interface ScarletDatePicker {
        /**
          * Disables the field and the calendar toggle.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the field. Takes priority over automatic calendar validation errors.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the field. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the field as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the field.
         */
        "label"?: string;
        /**
          * Latest selectable date, as `DD/MM/AAAA`. Days after it render disabled in the calendar.
         */
        "max"?: string;
        /**
          * Earliest selectable date, as `DD/MM/AAAA`. Days before it render disabled in the calendar (typing them is still possible; `validate` doesn't enforce range).
         */
        "min"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the text field loses focus.
         */
        "onScarletBlur"?: (event: ScarletDatePickerCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the field loses focus after its value has changed, and when a day is picked from the calendar.
         */
        "onScarletChange"?: (event: ScarletDatePickerCustomEvent<string>) => void;
        /**
          * Emitted when the text field gains focus.
         */
        "onScarletFocus"?: (event: ScarletDatePickerCustomEvent<FocusEvent>) => void;
        /**
          * Emitted after the calendar popover closes.
         */
        "onScarletHide"?: (event: ScarletDatePickerCustomEvent<void>) => void;
        /**
          * Emitted on every keystroke, and when a day is picked from the calendar, with the current formatted value.
         */
        "onScarletInput"?: (event: ScarletDatePickerCustomEvent<string>) => void;
        /**
          * Emitted after the calendar popover opens.
         */
        "onScarletShow"?: (event: ScarletDatePickerCustomEvent<void>) => void;
        /**
          * Emitted after a `validate`-triggered check, with the resulting validity.
         */
        "onScarletValidityChange"?: (event: ScarletDatePickerCustomEvent<boolean>) => void;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'DD/MM/AAAA'
         */
        "placeholder"?: "DD/MM/AAAA";
        /**
          * Marks the field as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the field.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Validates the value is a real calendar date on blur once it's complete (8 digits), showing a default "Data inválida" message when it isn't — unless `errorMessage` is already set, which always wins.
          * @default true
         */
        "validate"?: true;
        /**
          * Current formatted value, e.g. `31/12/2026`.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * Two `DD/MM/AAAA` fields (start/end) sharing one calendar popover for
     * picking both ends of a range — built the same way `scarlet-date-picker`
     * is (same masking/validation per field, same popover mechanics), doubled.
     * See that component's own doc comment for the shared known limitations
     * (no Tab-out auto-close, no viewport flip).
     * Picking works the classic two-click way: the first day clicked becomes
     * the start (clearing any previous end); the second becomes the end and
     * closes the popover — unless it's *before* the start, in which case it
     * becomes the new start instead and the popover stays open for the end.
     */
    interface ScarletDateRangePicker {
        /**
          * Disables both fields and the calendar toggle.
          * @default false
         */
        "disabled"?: false;
        /**
          * Placeholder for the end field.
          * @default 'Data final'
         */
        "endPlaceholder"?: "Data final";
        /**
          * End of the range, as `DD/MM/AAAA`.
          * @default ''
         */
        "endValue"?: string;
        /**
          * Error message rendered below the fields.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the fields. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the fields as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above both fields.
         */
        "label"?: string;
        /**
          * Latest selectable date, as `DD/MM/AAAA`.
         */
        "max"?: string;
        /**
          * Earliest selectable date, as `DD/MM/AAAA`.
         */
        "min"?: string;
        /**
          * Emitted whenever either bound changes — a keystroke, a day pick, or a blur-triggered mask correction.
         */
        "onScarletChange"?: (event: ScarletDateRangePickerCustomEvent<ScarletDateRangeChange>) => void;
        /**
          * Emitted after the popover closes.
         */
        "onScarletHide"?: (event: ScarletDateRangePickerCustomEvent<void>) => void;
        /**
          * Emitted after the popover opens.
         */
        "onScarletShow"?: (event: ScarletDateRangePickerCustomEvent<void>) => void;
        /**
          * Marks the fields as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the fields.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Placeholder for the start field.
          * @default 'Data inicial'
         */
        "startPlaceholder"?: "Data inicial";
        /**
          * Start of the range, as `DD/MM/AAAA`.
          * @default ''
         */
        "startValue"?: string;
    }
    /**
     * A visual separator between sections of content, with an optional centered label.
     */
    interface ScarletDivider {
        /**
          * Optional text rendered centered on the line (horizontal orientation only).
         */
        "label"?: string;
        /**
          * Direction of the divider line.
          * @default 'horizontal'
         */
        "orientation"?: ScarletDividerOrientation;
    }
    /**
     * A panel that slides in from an edge of the screen — the mobile-friendlier
     * sibling of `scarlet-modal` for a filter panel, a form, a details view.
     * Built the same way `scarlet-modal` is, on the native `<dialog>` element,
     * for the same free focus trapping/top-layer stacking/Escape handling — see
     * that component's own comments for the mechanics shared here (focus
     * restore, the cancelable `scarletClose`, `show()`/`hide()`).
     * `size` sets the dimension along the axis the drawer slides on: width for
     * `left`/`right`, height for `top`/`bottom`.
     */
    interface ScarletDrawer {
        /**
          * Accessible label for the dialog. When omitted, the `header` slot's content is used instead (via `aria-labelledby`) — set this explicitly only when the drawer has no visible header, or the header text alone isn't a good accessible name.
         */
        "ariaLabel"?: string;
        /**
          * Closes the drawer when the backdrop (area outside the panel) is clicked.
          * @default true
         */
        "dismissOnBackdropClick"?: true;
        /**
          * Closes the drawer when Escape is pressed.
          * @default true
         */
        "dismissOnEsc"?: true;
        /**
          * Emitted when the drawer is about to close (Escape, backdrop click, the close button, or the `open` prop being set to false). Cancelable: call `event.preventDefault()` to keep it open.
         */
        "onScarletClose"?: (event: ScarletDrawerCustomEvent<void>) => void;
        /**
          * Emitted after the drawer opens.
         */
        "onScarletShow"?: (event: ScarletDrawerCustomEvent<void>) => void;
        /**
          * Whether the drawer is open.
          * @default false
         */
        "open"?: boolean;
        /**
          * Which edge of the screen the drawer slides in from.
          * @default 'right'
         */
        "placement"?: ScarletDrawerPlacement;
        /**
          * Size along the slide axis: width for `left`/`right`, height for `top`/`bottom`.
          * @default 'md'
         */
        "size"?: ScarletDrawerSize;
    }
    /**
     * A placeholder for a list, search result, or section with nothing to
     * show — an icon, a heading, an optional description, and an optional
     * action (e.g. "Limpar filtros" or "Criar o primeiro item").
     */
    interface ScarletEmptyState {
        /**
          * Supporting text below the heading.
         */
        "description"?: string;
        /**
          * Main heading.
          * @default 'Nenhum resultado encontrado.'
         */
        "heading"?: "Nenhum resultado encontrado.";
        /**
          * Icon shown above the heading. Ignored if the `icon` slot has content.
         */
        "icon"?: ScarletIconName;
    }
    /**
     * A drag-and-drop file picker, backed by a real (visually hidden) native
     * `<input type="file">` — so it's a real form control and works with no JS
     * at all for the "click to browse" path; only the drag-and-drop layer is
     * enhancement on top.
     */
    interface ScarletFileUpload {
        /**
          * Native `accept` attribute, e.g. `"image/*"` or `".pdf,.docx"`.
         */
        "accept"?: string;
        /**
          * Disables the dropzone and the native input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the dropzone. Takes priority over the automatic max-size error.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the dropzone. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Visible label rendered above the dropzone.
         */
        "label"?: string;
        /**
          * Rejects any file over this size, showing a default error message instead of accepting it.
         */
        "maxSizeBytes"?: number;
        /**
          * Allows more than one file.
          * @default false
         */
        "multiple"?: false;
        /**
          * Emitted with the full current file list whenever it changes — a selection, a drop, or a removal.
         */
        "onScarletChange"?: (event: ScarletFileUploadCustomEvent<File[]>) => void;
    }
    /**
     * A CSS grid layout primitive with equal-width columns and token-based
     * gaps. Pair with `<scarlet-grid-item>` to span multiple columns/rows.
     * `columns` can change per breakpoint via `columnsSm`/`columnsMd`/
     * `columnsLg`/`columnsXl` — each one only takes effect from its breakpoint
     * up and, left unset, falls back to the next smaller breakpoint that *is*
     * set (mobile-first cascade), down to `columns` itself. E.g. `columns={1}
     * columns-md={3}` renders 1 column below 768px and 3 from 768px up, with
     * no separate `sm`/`lg`/`xl` value needed.
     */
    interface ScarletGrid {
        /**
          * Cross-axis alignment (`align-items`) of each item within its own cell.
          * @default 'stretch'
         */
        "align"?: Alignment;
        /**
          * Overrides the column gap.
         */
        "columnGap"?: Size;
        /**
          * Number of equal-width columns below the `sm` breakpoint (or at every size, if no responsive override is set).
          * @default 12
         */
        "columns"?: 12;
        /**
          * Column count from the `lg` breakpoint (1024px) up. Falls back to `columnsMd`/`columnsSm`/`columns` when unset.
         */
        "columnsLg"?: number;
        /**
          * Column count from the `md` breakpoint (768px) up. Falls back to `columnsSm`/`columns` when unset.
         */
        "columnsMd"?: number;
        /**
          * Column count from the `sm` breakpoint (640px) up. Falls back to `columns` when unset.
         */
        "columnsSm"?: number;
        /**
          * Column count from the `xl` breakpoint (1280px) up. Falls back to `columnsLg`/`columnsMd`/`columnsSm`/`columns` when unset.
         */
        "columnsXl"?: number;
        /**
          * Gap between rows and columns, from the design system's spacing scale. Overridden individually by `rowGap`/`columnGap`.
          * @default 'md'
         */
        "gap"?: Size;
        /**
          * In-cell horizontal alignment (`justify-items`) of each item within its own cell — the column analog of `align`.
          * @default 'stretch'
         */
        "justify"?: Alignment;
        /**
          * Overrides the row gap.
         */
        "rowGap"?: Size;
    }
    /**
     * A cell inside a `<scarlet-grid>` that can span multiple columns/rows.
     * `colSpan`/`rowSpan` can change per breakpoint via `colSpanSm`/`colSpanMd`/
     * `colSpanLg`/`colSpanXl` (and the `rowSpan` equivalents) — the classic "full
     * row on mobile, a couple columns on desktop" pattern is
     * `col-span="{columns}" col-span-md="2"`. Each one only takes effect from
     * its breakpoint up and, left unset, falls back to the next smaller
     * breakpoint that *is* set (mobile-first cascade, same as `<scarlet-grid>`'s
     * own `columns`/`columnsSm`/etc.), down to `colSpan`/`rowSpan` themselves.
     */
    interface ScarletGridItem {
        /**
          * Number of columns this item spans below the `sm` breakpoint (or at every size, if no responsive override is set).
          * @default 1
         */
        "colSpan"?: 1;
        /**
          * Column span from the `lg` breakpoint (1024px) up. Falls back to `colSpanMd`/`colSpanSm`/`colSpan` when unset.
         */
        "colSpanLg"?: number;
        /**
          * Column span from the `md` breakpoint (768px) up. Falls back to `colSpanSm`/`colSpan` when unset.
         */
        "colSpanMd"?: number;
        /**
          * Column span from the `sm` breakpoint (640px) up. Falls back to `colSpan` when unset.
         */
        "colSpanSm"?: number;
        /**
          * Column span from the `xl` breakpoint (1280px) up. Falls back to `colSpanLg`/`colSpanMd`/`colSpanSm`/`colSpan` when unset.
         */
        "colSpanXl"?: number;
        /**
          * Number of rows this item spans below the `sm` breakpoint (or at every size, if no responsive override is set).
          * @default 1
         */
        "rowSpan"?: 1;
        /**
          * Row span from the `lg` breakpoint (1024px) up. Falls back to `rowSpanMd`/`rowSpanSm`/`rowSpan` when unset.
         */
        "rowSpanLg"?: number;
        /**
          * Row span from the `md` breakpoint (768px) up. Falls back to `rowSpanSm`/`rowSpan` when unset.
         */
        "rowSpanMd"?: number;
        /**
          * Row span from the `sm` breakpoint (640px) up. Falls back to `rowSpan` when unset.
         */
        "rowSpanSm"?: number;
        /**
          * Row span from the `xl` breakpoint (1280px) up. Falls back to `rowSpanLg`/`rowSpanMd`/`rowSpanSm`/`rowSpan` when unset.
         */
        "rowSpanXl"?: number;
    }
    /**
     * A semantic heading (`<h1>`–`<h6>`) with an independently controllable
     * visual size — document structure (`level`) and visual hierarchy
     * (`variant`) don't have to match one-to-one.
     */
    interface ScarletHeading {
        /**
          * Text alignment.
          * @default 'left'
         */
        "align"?: TextAlign;
        /**
          * Text color token.
          * @default 'primary'
         */
        "color"?: TextColor;
        /**
          * Semantic heading level — which HTML tag (`h1`–`h6`) is rendered.
          * @default 2
         */
        "level"?: ScarletHeadingLevel;
        /**
          * Visual size. Defaults to a sensible size for `level` when omitted.
         */
        "variant"?: ScarletHeadingVariant;
    }
    /**
     * Renders an icon from the design system's shared built-in set. Icons are
     * stroke-based and inherit `color`/`font-size` from their context, so they
     * line up with surrounding text and other components by default.
     */
    interface ScarletIcon {
        /**
          * Accessible label. Omit for a purely decorative icon (the default) — it is then hidden from assistive tech.
         */
        "label"?: string;
        /**
          * Name of the built-in icon to render.
         */
        "name"?: ScarletIconName1;
        /**
          * Size of the icon, as any valid CSS length. Defaults to `1em`, so it scales with the surrounding font size.
         */
        "size"?: string;
    }
    /**
     * A labeled text input with helper/error text and built-in accessibility wiring.
     */
    interface ScarletInput {
        /**
          * Native `autocomplete` attribute.
         */
        "autocomplete"?: string;
        /**
          * Disables the input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the input loses focus.
         */
        "onScarletBlur"?: (event: ScarletInputCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the input loses focus after its value has changed.
         */
        "onScarletChange"?: (event: ScarletInputCustomEvent<string>) => void;
        /**
          * Emitted when the input gains focus.
         */
        "onScarletFocus"?: (event: ScarletInputCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current value.
         */
        "onScarletInput"?: (event: ScarletInputCustomEvent<string>) => void;
        /**
          * Placeholder text shown when the input is empty.
         */
        "placeholder"?: string;
        /**
          * Makes the input read-only.
          * @default false
         */
        "readonly"?: false;
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Native input type.
          * @default 'text'
         */
        "type"?: ScarletInputType;
        /**
          * Current value of the input.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A Brazilian postal code (CEP) input — formats as `01310-100`. This
     * component only formats the value; looking up the matching address (e.g.
     * via ViaCEP) is the consuming app's responsibility — listen for
     * `scarletChange` and call your own API with `getRawValue()`.
     */
    interface ScarletInputCep {
        /**
          * Disables the input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the input loses focus.
         */
        "onScarletBlur"?: (event: ScarletInputCepCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the input loses focus after its value has changed. Also emitted as soon as the 8th digit is typed (`scarletComplete`) — the more useful hook for triggering an address lookup without waiting for blur.
         */
        "onScarletChange"?: (event: ScarletInputCepCustomEvent<string>) => void;
        /**
          * Emitted once the value reaches 8 digits (a complete CEP), on every keystroke that keeps it complete.
         */
        "onScarletComplete"?: (event: ScarletInputCepCustomEvent<string>) => void;
        /**
          * Emitted when the input gains focus.
         */
        "onScarletFocus"?: (event: ScarletInputCepCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current formatted value.
         */
        "onScarletInput"?: (event: ScarletInputCepCustomEvent<string>) => void;
        /**
          * Placeholder text shown when the input is empty.
          * @default '00000-000'
         */
        "placeholder"?: "00000-000";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Current formatted value, e.g. `01310-100`.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A credit card number input — formats in groups of 4 (or Amex's 4-6-5 /
     * Diners' 4-6-4 grouping once that brand is detected from the card's
     * BIN/IIN prefix), with a Luhn checksum validation on blur.
     */
    interface ScarletInputCreditCard {
        /**
          * Disables the input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the input. Takes priority over automatic Luhn validation errors.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the input loses focus.
         */
        "onScarletBlur"?: (event: ScarletInputCreditCardCustomEvent<FocusEvent>) => void;
        /**
          * Emitted whenever the detected card brand changes (including to `undefined`).
         */
        "onScarletBrandChange"?: (event: ScarletInputCreditCardCustomEvent<CreditCardBrand | undefined>) => void;
        /**
          * Emitted when the input loses focus after its value has changed.
         */
        "onScarletChange"?: (event: ScarletInputCreditCardCustomEvent<string>) => void;
        /**
          * Emitted when the input gains focus.
         */
        "onScarletFocus"?: (event: ScarletInputCreditCardCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current formatted value.
         */
        "onScarletInput"?: (event: ScarletInputCreditCardCustomEvent<string>) => void;
        /**
          * Emitted after a `validate`-triggered check, with the resulting validity.
         */
        "onScarletValidityChange"?: (event: ScarletInputCreditCardCustomEvent<boolean>) => void;
        /**
          * Placeholder text shown when the input is empty.
          * @default '0000 0000 0000 0000'
         */
        "placeholder"?: "0000 0000 0000 0000";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Validates the number against the Luhn checksum on blur once it's complete, showing a default "Número de cartão inválido" message when it fails — unless `errorMessage` is already set, which always wins.
          * @default true
         */
        "validate"?: true;
        /**
          * Current formatted value.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A monetary input that formats digits as currency growing from the right
     * (like a card machine) — typing "1234" produces "R$ 12,34". Unlike the
     * other masked inputs, `scarletInput`/`scarletChange` emit the plain
     * **numeric** amount (e.g. `12.34`), not the formatted string — that's
     * almost always what you actually want from a money field. Read the
     * formatted text itself via `value`.
     */
    interface ScarletInputCurrency {
        /**
          * Currency symbol shown before the amount.
          * @default 'R$'
         */
        "currencySymbol"?: "R$";
        /**
          * Disables the input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the input loses focus.
         */
        "onScarletBlur"?: (event: ScarletInputCurrencyCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the input loses focus after its value has changed, with the current numeric amount.
         */
        "onScarletChange"?: (event: ScarletInputCurrencyCustomEvent<number>) => void;
        /**
          * Emitted when the input gains focus.
         */
        "onScarletFocus"?: (event: ScarletInputCurrencyCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current numeric amount (e.g. `12.34`).
         */
        "onScarletInput"?: (event: ScarletInputCurrencyCustomEvent<number>) => void;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'R$ 0,00'
         */
        "placeholder"?: "R$ 0,00";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Current formatted value, e.g. `R$ 1.234,56`.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A `DD/MM/AAAA` date input. Unlike a plain positional mask, `validate`
     * checks it's a *real* calendar date (rejects `31/02/2026`, honors leap
     * years) on blur, not just that 8 digits were typed.
     */
    interface ScarletInputDate {
        /**
          * Disables the input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the input. Takes priority over automatic calendar validation errors.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the input loses focus.
         */
        "onScarletBlur"?: (event: ScarletInputDateCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the input loses focus after its value has changed.
         */
        "onScarletChange"?: (event: ScarletInputDateCustomEvent<string>) => void;
        /**
          * Emitted when the input gains focus.
         */
        "onScarletFocus"?: (event: ScarletInputDateCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current formatted value.
         */
        "onScarletInput"?: (event: ScarletInputDateCustomEvent<string>) => void;
        /**
          * Emitted after a `validate`-triggered check, with the resulting validity.
         */
        "onScarletValidityChange"?: (event: ScarletInputDateCustomEvent<boolean>) => void;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'DD/MM/AAAA'
         */
        "placeholder"?: "DD/MM/AAAA";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Validates the value is a real calendar date on blur once it's complete (8 digits), showing a default "Data inválida" message when it isn't — unless `errorMessage` is already set, which always wins.
          * @default true
         */
        "validate"?: true;
        /**
          * Current formatted value, e.g. `31/12/2026`.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A single "CPF/CNPJ" field that auto-detects and formats whichever
     * document type is being typed (11 digits → CPF `123.456.789-01`, 12+ →
     * CNPJ `12.345.678/0001-90`) — the common pattern for a field that accepts
     * either an individual or a company.
     */
    interface ScarletInputDocument {
        /**
          * Disables the input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the input. Takes priority over automatic check-digit validation errors.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the input loses focus.
         */
        "onScarletBlur"?: (event: ScarletInputDocumentCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the input loses focus after its value has changed.
         */
        "onScarletChange"?: (event: ScarletInputDocumentCustomEvent<string>) => void;
        /**
          * Emitted when the input gains focus.
         */
        "onScarletFocus"?: (event: ScarletInputDocumentCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current formatted value.
         */
        "onScarletInput"?: (event: ScarletInputDocumentCustomEvent<string>) => void;
        /**
          * Emitted after a `validate`-triggered check, with the resulting validity.
         */
        "onScarletValidityChange"?: (event: ScarletInputDocumentCustomEvent<boolean>) => void;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'CPF ou CNPJ'
         */
        "placeholder"?: "CPF ou CNPJ";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Validates the CPF/CNPJ check digits on blur once the value is complete (11 or 14 digits), showing a default "CPF/CNPJ inválido" message when they don't check out — unless `errorMessage` is already set, which always wins.
          * @default true
         */
        "validate"?: true;
        /**
          * Current formatted value.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A Brazilian vehicle plate input — formats as the old `ABC-1234` pattern
     * or the newer Mercosul `ABC1D23` pattern, detected automatically from
     * whether a letter or digit lands in the 5th character.
     */
    interface ScarletInputLicensePlate {
        /**
          * Disables the input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the input loses focus.
         */
        "onScarletBlur"?: (event: ScarletInputLicensePlateCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the input loses focus after its value has changed.
         */
        "onScarletChange"?: (event: ScarletInputLicensePlateCustomEvent<string>) => void;
        /**
          * Emitted when the input gains focus.
         */
        "onScarletFocus"?: (event: ScarletInputLicensePlateCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current formatted value.
         */
        "onScarletInput"?: (event: ScarletInputLicensePlateCustomEvent<string>) => void;
        /**
          * Placeholder text shown when the input is empty.
          * @default 'ABC-1234'
         */
        "placeholder"?: "ABC-1234";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Current formatted value, e.g. `ABC-1234` or `ABC1D23`.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A percentage input that formats digits growing from the right, like
     * `scarlet-input-currency` — typing "1234" produces "12,34%". Emits the
     * plain numeric percentage (e.g. `12.34`) via `scarletInput`/`scarletChange`.
     */
    interface ScarletInputPercentage {
        /**
          * How many digits are kept after the decimal comma.
          * @default 2
         */
        "decimals"?: 2;
        /**
          * Disables the input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the input loses focus.
         */
        "onScarletBlur"?: (event: ScarletInputPercentageCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the input loses focus after its value has changed, with the current numeric percentage.
         */
        "onScarletChange"?: (event: ScarletInputPercentageCustomEvent<number>) => void;
        /**
          * Emitted when the input gains focus.
         */
        "onScarletFocus"?: (event: ScarletInputPercentageCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current numeric percentage (e.g. `12.34`).
         */
        "onScarletInput"?: (event: ScarletInputPercentageCustomEvent<number>) => void;
        /**
          * Placeholder text shown when the input is empty.
          * @default '0,00%'
         */
        "placeholder"?: "0,00%";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Current formatted value, e.g. `12,34%`.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A Brazilian phone number input — formats as `(11) 91234-5678` (mobile,
     * 11 digits) or `(11) 1234-5678` (landline, 10 digits), switching
     * automatically as the user types.
     */
    interface ScarletInputPhone {
        /**
          * Disables the input.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the input. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the input. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the input as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the input.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the input loses focus.
         */
        "onScarletBlur"?: (event: ScarletInputPhoneCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the input loses focus after its value has changed.
         */
        "onScarletChange"?: (event: ScarletInputPhoneCustomEvent<string>) => void;
        /**
          * Emitted when the input gains focus.
         */
        "onScarletFocus"?: (event: ScarletInputPhoneCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current formatted value.
         */
        "onScarletInput"?: (event: ScarletInputPhoneCustomEvent<string>) => void;
        /**
          * Placeholder text shown when the input is empty.
          * @default '(11) 91234-5678'
         */
        "placeholder"?: "(11) 91234-5678";
        /**
          * Marks the input as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the input.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Current formatted value, e.g. `(11) 91234-5678`.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A styled inline text link — for a link inside a sentence/paragraph.
     * `scarlet-button variant="link"` is the button-semantics equivalent (an
     * action styled like a link); this is the reverse, a real `<a>` styled like
     * one. `target="_blank"` automatically gets `rel="noopener noreferrer"`
     * (unless `rel` is set explicitly) and a small external-link icon.
     */
    interface ScarletLink {
        /**
          * Semantic color of the link.
          * @default 'primary'
         */
        "color"?: Color;
        /**
          * Renders as inert text — no `href`, `aria-disabled`.
          * @default false
         */
        "disabled"?: false;
        /**
          * Native `href`. Omitting it (and setting `disabled`) renders inert text styled like a link.
         */
        "href"?: string;
        /**
          * Native `rel`. Defaults to `noopener noreferrer` when `target="_blank"`.
         */
        "rel"?: string;
        /**
          * Native `target`, e.g. `_blank`.
         */
        "target"?: string;
        /**
          * When the underline shows.
          * @default 'hover'
         */
        "underline"?: ScarletLinkUnderline;
    }
    /**
     * A dropdown menu of actions opened from an arbitrary trigger element (the
     * `trigger` slot — typically an icon button like "⋮"), following the
     * WAI-ARIA menu button pattern: the trigger gets `aria-haspopup="menu"`/
     * `aria-expanded` (set directly on whatever element is slotted in), the
     * popover is `role="menu"` with `role="menuitem"` buttons, arrow keys move
     * a roving tab stop within the list, Home/End jump to the first/last enabled
     * item, Escape closes and returns focus to the trigger. Enter/Space opening
     * the menu is *not* handled here — the trigger must be a real, natively
     * activatable element (a `<button>` or `<scarlet-button>`); its own native
     * click already opens the menu, and re-handling those keys here would fire
     * twice (open, then immediately close again from the resulting click).
     * Known limitation: like `scarlet-tooltip`/`scarlet-date-picker`,
     * positioning is plain CSS anchored to the trigger — it doesn't flip or
     * reposition to stay in the viewport, and it doesn't close on Tab-ing past
     * its last item.
     */
    interface ScarletMenu {
        /**
          * Accessible label for the menu list, when the trigger's own accessible name doesn't already describe it.
          * @default 'Menu'
         */
        "ariaLabel"?: "Menu";
        /**
          * The actions to list.
          * @default []
         */
        "items"?: ScarletMenuItem[];
        /**
          * Emitted after the menu closes.
         */
        "onScarletHide"?: (event: ScarletMenuCustomEvent<void>) => void;
        /**
          * Emitted when an enabled item is picked. The menu closes right after.
         */
        "onScarletSelect"?: (event: ScarletMenuCustomEvent<ScarletMenuItem>) => void;
        /**
          * Emitted after the menu opens.
         */
        "onScarletShow"?: (event: ScarletMenuCustomEvent<void>) => void;
        /**
          * Which side of the trigger the menu aligns to.
          * @default 'start'
         */
        "placement"?: 'start' | 'end';
    }
    /**
     * A modal dialog built on the native `<dialog>` element, which provides
     * focus trapping, top-layer stacking and Escape handling for free.
     */
    interface ScarletModal {
        /**
          * Accessible label for the dialog. When omitted, the `header` slot's content is used instead (via `aria-labelledby`) — set this explicitly only when the modal has no visible header, or the header text alone isn't a good accessible name.
         */
        "ariaLabel"?: string;
        /**
          * Closes the modal when the backdrop (area outside the dialog box) is clicked.
          * @default true
         */
        "dismissOnBackdropClick"?: true;
        /**
          * Closes the modal when Escape is pressed.
          * @default true
         */
        "dismissOnEsc"?: true;
        /**
          * Emitted when the modal is about to close (Escape, backdrop click, the close button, or the `open` prop being set to false). Cancelable: call `event.preventDefault()` to keep it open.
         */
        "onScarletClose"?: (event: ScarletModalCustomEvent<void>) => void;
        /**
          * Emitted after the modal opens.
         */
        "onScarletShow"?: (event: ScarletModalCustomEvent<void>) => void;
        /**
          * Whether the modal is open.
          * @default false
         */
        "open"?: boolean;
        /**
          * Size of the modal.
          * @default 'md'
         */
        "size"?: ScarletModalSize;
    }
    /**
     * A numeric input with decrement/increment buttons — for a quantity field,
     * not a general-purpose text field that happens to hold numbers (that's
     * `scarlet-input type="number"`). Stays within `min`/`max` on every path:
     * the buttons, typing, and blur all clamp.
     */
    interface ScarletNumberInput {
        /**
          * Disables the field and both buttons.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the field. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the field. Hidden while an error is shown.
         */
        "helperText"?: string;
        /**
          * Marks the field as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the field.
         */
        "label"?: string;
        /**
          * Upper bound. Omit for no maximum.
         */
        "max"?: number;
        /**
          * Lower bound. Omit for no minimum.
         */
        "min"?: number;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the value is committed — a +/- click, or the field losing focus — always clamped to `min`/`max`.
         */
        "onScarletChange"?: (event: ScarletNumberInputCustomEvent<number>) => void;
        /**
          * Emitted on every keystroke, with the raw (not yet clamped) numeric value.
         */
        "onScarletInput"?: (event: ScarletNumberInputCustomEvent<number>) => void;
        /**
          * Marks the field as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the field.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Amount each +/- click changes the value by.
          * @default 1
         */
        "step"?: 1;
        /**
          * Current value.
          * @default 0
         */
        "value"?: number;
    }
    /**
     * Page number navigation, following the WAI-ARIA pattern of a `<nav>`
     * landmark wrapping a list of buttons — the current page is a real button
     * (not a link, since this component doesn't own routing) marked
     * `aria-current="page"`. Collapses distant page numbers into an ellipsis
     * once `totalPages` is large, always keeping the first page, the last page,
     * and `siblingCount` pages on each side of the current one visible.
     */
    interface ScarletPagination {
        /**
          * Accessible label for the `<nav>` landmark.
          * @default 'Paginação'
         */
        "ariaLabel"?: "Paginação";
        /**
          * Emitted when the page changes via any control (a page number, prev, or next).
         */
        "onScarletChange"?: (event: ScarletPaginationCustomEvent<number>) => void;
        /**
          * Current page (1-indexed).
          * @default 1
         */
        "page"?: number;
        /**
          * How many page numbers to show on each side of the current page before collapsing into an ellipsis.
          * @default 1
         */
        "siblingCount"?: 1;
        /**
          * Total number of pages.
          * @default 1
         */
        "totalPages"?: 1;
    }
    /**
     * A generic overlay anchored to a trigger element (the `trigger` slot),
     * with arbitrary content in the default slot — the shared primitive
     * `scarlet-menu`/`scarlet-date-picker`/`scarlet-combobox` each built their
     * own narrower version of for their specific popovers. Use this one
     * directly for anything that doesn't need a menu list or a calendar grid:
     * a rich tooltip, a filter panel, a confirmation prompt.
     * `trigger="click"` (the default) toggles on click, closes on Escape or a
     * click outside, and sets `aria-haspopup`/`aria-expanded` on whatever's
     * slotted as the trigger. `trigger="hover"` opens/closes on mouse enter/
     * leave instead, with no click/Escape handling — matching how a hover
     * tooltip behaves, not a dialog.
     * Known limitation: like `scarlet-tooltip`/`scarlet-menu`, positioning is
     * plain CSS anchored to the trigger — it doesn't flip or reposition to stay
     * in the viewport.
     */
    interface ScarletPopover {
        /**
          * Accessible label for the popover content region.
         */
        "ariaLabel"?: string;
        /**
          * Emitted after the popover closes.
         */
        "onScarletHide"?: (event: ScarletPopoverCustomEvent<void>) => void;
        /**
          * Emitted after the popover opens.
         */
        "onScarletShow"?: (event: ScarletPopoverCustomEvent<void>) => void;
        /**
          * Which side of the trigger the popover opens on.
          * @default 'bottom'
         */
        "placement"?: ScarletPopoverPlacement;
        /**
          * What interaction opens/closes the popover.
          * @default 'click'
         */
        "triggerMode"?: ScarletPopoverTriggerMode;
    }
    /**
     * A determinate progress bar (0–100%) — for a file upload, a multi-step
     * form, anything with a real, known completion percentage. For an
     * indeterminate "something's happening" state, use `scarlet-spinner`.
     */
    interface ScarletProgress {
        /**
          * Accessible label for the `progressbar` role.
          * @default 'Progresso'
         */
        "ariaLabel"?: "Progresso";
        /**
          * Semantic color of the filled portion.
          * @default 'primary'
         */
        "color"?: Color;
        /**
          * Value that represents 100%.
          * @default 100
         */
        "max"?: 100;
        /**
          * Shows the percentage as text next to the track.
          * @default false
         */
        "showLabel"?: false;
        /**
          * Height of the track.
          * @default 'md'
         */
        "size"?: ScarletProgressSize;
        /**
          * Current value.
          * @default 0
         */
        "value"?: 0;
    }
    /**
     * A single radio option. Use inside a `<scarlet-radio-group>` for mutually
     * exclusive selection — standalone, it behaves like an isolated toggle.
     */
    interface ScarletRadio {
        /**
          * Whether this radio is selected. Managed by the parent `<scarlet-radio-group>` when present.
          * @default false
         */
        "checked"?: boolean;
        /**
          * Disables this radio. Set by a parent `<scarlet-radio-group>` when the whole group is disabled.
          * @default false
         */
        "disabled"?: boolean;
        /**
          * Whether this radio is a tab stop. A parent `<scarlet-radio-group>` keeps this `true` on exactly one radio at a time (roving tabindex, per the WAI-ARIA radiogroup pattern) so the group has a single Tab stop and arrow keys move both focus and selection — matching how native radio buttons behave. Standalone (no group), this defaults to `true` so a lone radio is reachable by Tab like any other control.
          * @default true
         */
        "focusable"?: boolean;
        /**
          * Visible label rendered next to the radio.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form. Set by a parent `<scarlet-radio-group>` when present.
         */
        "name"?: string;
        /**
          * Emitted when this radio becomes selected via user interaction. Deliberately non-bubbling: a parent `<scarlet-radio-group>` re-emits its own `scarletChange` (with a different, string `detail`) once it has processed this one, so this event must never reach a listener attached to the group too, or such a listener would see both fire under the same name. Listen directly on the radio (which still works for standalone usage — the "at target" phase runs regardless of `bubbles`) or on the group, not both.
         */
        "onScarletChange"?: (event: ScarletRadioCustomEvent<boolean>) => void;
        "onScarletRadioChange"?: (event: ScarletRadioCustomEvent<boolean>) => void;
        /**
          * Value identifying this option within its group.
         */
        "value"?: string;
    }
    /**
     * Groups a set of `<scarlet-radio>` options and enforces single selection —
     * native browser radio grouping does not cross shadow-DOM boundaries, so
     * this component coordinates `checked`/`name`/`disabled` across its children.
     */
    interface ScarletRadioGroup {
        /**
          * Accessible label for the group, when there is no visible heading nearby.
         */
        "ariaLabel"?: string;
        /**
          * Disables every radio in the group.
          * @default false
         */
        "disabled"?: false;
        /**
          * Lays the radios out in a row instead of stacked.
          * @default false
         */
        "horizontal"?: false;
        /**
          * Name applied to every radio in the group, so they submit together in a form.
         */
        "name"?: string;
        /**
          * Emitted when the selected value changes.
         */
        "onScarletChange"?: (event: ScarletRadioGroupCustomEvent<string | undefined>) => void;
        /**
          * Value of the currently selected radio.
         */
        "value"?: string;
    }
    /**
     * A labeled native `<select>` dropdown with helper/error text and built-in
     * accessibility wiring. Options are passed as a `options` property (an
     * array), not slotted — native `<select>` cannot reliably project slotted
     * `<option>` elements into its picker UI across browsers.
     */
    interface ScarletSelect {
        /**
          * Disables the select.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the select. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the select. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the select as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the select.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the select loses focus.
         */
        "onScarletBlur"?: (event: ScarletSelectCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the selected value changes.
         */
        "onScarletChange"?: (event: ScarletSelectCustomEvent<string>) => void;
        /**
          * Emitted when the select gains focus.
         */
        "onScarletFocus"?: (event: ScarletSelectCustomEvent<FocusEvent>) => void;
        /**
          * Options rendered inside the select.
          * @default []
         */
        "options"?: ScarletSelectOption[];
        /**
          * Placeholder shown as a disabled first option when no value is selected.
         */
        "placeholder"?: string;
        /**
          * Marks the select as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Size of the select.
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Currently selected value.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A loading placeholder shape — swap it in for content that hasn't arrived
     * yet (a card, an avatar, a paragraph). Purely decorative (`aria-hidden`):
     * announcing the *loading* state itself (e.g. `aria-busy` on the section
     * being replaced) is the consumer's responsibility, not this component's.
     */
    interface ScarletSkeleton {
        /**
          * Any valid CSS height. Defaults to one text line's height for `text`, or `width`'s value for `circle`.
         */
        "height"?: string;
        /**
          * Number of stacked lines, only for `variant="text"`. The last line renders narrower, like a paragraph's ragged edge.
          * @default 1
         */
        "lines"?: 1;
        /**
          * Shape of the placeholder.
          * @default 'text'
         */
        "variant"?: ScarletSkeletonVariant;
        /**
          * Any valid CSS width. Defaults to 100% for `text`/`rect`, or `height`'s value for `circle`.
         */
        "width"?: string;
    }
    /**
     * A loading indicator — `variant="circle"` for the generic spinner (the
     * same shape `scarlet-button`'s own `loading` state uses), `variant="logo"`
     * for the Scarlet mark itself pulsing, e.g. for a full-page loading state.
     * `role="status"` with `label` as its accessible name, so assistive tech
     * announces the loading state without needing separate visible text.
     */
    interface ScarletSpinner {
        /**
          * Accessible label, announced by assistive tech via `role="status"`.
          * @default 'Carregando'
         */
        "label"?: "Carregando";
        /**
          * Size of the indicator.
          * @default 'md'
         */
        "size"?: ScarletSpinnerSize;
        /**
          * Which loading indicator to show.
          * @default 'circle'
         */
        "variant"?: ScarletSpinnerVariant;
    }
    /**
     * A flexbox layout primitive for stacking or rowing children with
     * consistent, token-based spacing.
     * `direction` can change per breakpoint via `directionSm`/`directionMd`/
     * `directionLg`/`directionXl` — the classic "stacked on mobile, row on
     * desktop" pattern is `direction="column" direction-md="row"`. Each one
     * only takes effect from its breakpoint up and, left unset, falls back to
     * the next smaller breakpoint that *is* set (mobile-first cascade), down
     * to `direction` itself.
     */
    interface ScarletStack {
        /**
          * Cross-axis alignment (`align-items`).
          * @default 'stretch'
         */
        "align"?: Alignment;
        /**
          * Flex direction below the `sm` breakpoint (or at every size, if no responsive override is set).
          * @default 'column'
         */
        "direction"?: Direction;
        /**
          * Flex direction from the `lg` breakpoint (1024px) up. Falls back to `directionMd`/`directionSm`/`direction` when unset.
         */
        "directionLg"?: Direction;
        /**
          * Flex direction from the `md` breakpoint (768px) up. Falls back to `directionSm`/`direction` when unset.
         */
        "directionMd"?: Direction;
        /**
          * Flex direction from the `sm` breakpoint (640px) up. Falls back to `direction` when unset.
         */
        "directionSm"?: Direction;
        /**
          * Flex direction from the `xl` breakpoint (1280px) up. Falls back to `directionLg`/`directionMd`/`directionSm`/`direction` when unset.
         */
        "directionXl"?: Direction;
        /**
          * Space between items, from the design system's spacing scale.
          * @default 'md'
         */
        "gap"?: Size;
        /**
          * Main-axis distribution (`justify-content`).
          * @default 'start'
         */
        "justify"?: JustifyContent;
        /**
          * Whether items wrap onto multiple lines.
          * @default 'nowrap'
         */
        "wrap"?: Wrap;
    }
    /**
     * A single labeled metric — a number/value, its label, and an optional
     * change indicator (e.g. "+12%" with an up/down arrow) for a dashboard.
     */
    interface ScarletStat {
        /**
          * Change text, e.g. "+12% vs. mês anterior". Omit to hide the whole change row.
         */
        "change"?: string;
        /**
          * Label above the value, e.g. "Receita total".
          * @default ''
         */
        "label"?: "";
        /**
          * Direction the change indicates — colors and arrows the change text accordingly.
          * @default 'neutral'
         */
        "trend"?: ScarletStatTrend;
        /**
          * The metric itself, e.g. "R$ 42.900".
          * @default ''
         */
        "value"?: "";
    }
    /**
     * A toggle switch for binary on/off settings.
     */
    interface ScarletSwitch {
        /**
          * Whether the switch is on.
          * @default false
         */
        "checked"?: boolean;
        /**
          * Disables the switch.
          * @default false
         */
        "disabled"?: false;
        /**
          * Visible label rendered next to the switch.
         */
        "label"?: string;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the on/off state changes via user interaction.
         */
        "onScarletChange"?: (event: ScarletSwitchCustomEvent<boolean>) => void;
        /**
          * Marks the switch as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Value submitted with a parent form when on.
         */
        "value"?: string;
    }
    /**
     * A data table: sortable columns, optional row selection (checkboxes),
     * empty/loading states, and a horizontally-scrolling wrapper so a wide
     * table never breaks the surrounding layout on a narrow viewport (same
     * pattern `scarlet-tabs` uses for its tab list).
     * Cell content is `String(row[column.key])` by default — good enough for
     * plain text/numbers, but there's no per-cell slot system (that needs a
     * stable id per row and gets complex fast). For anything richer — a
     * `scarlet-badge` status, a formatted currency, a truncated link — pass
     * `formatCell`, a plain function `(row, column) => string` set as a JS
     * property (like `columns`/`rows` themselves, it isn't parseable from an
     * HTML attribute).
     * Sorting is handled internally using a locale-aware comparator (numeric
     * for `number` values, `localeCompare` otherwise) — good for typical
     * text/number columns without any setup. `scarletSort` still fires on every
     * click if a consumer wants to replace that with their own comparator (e.g.
     * sorting a formatted date column by its real underlying timestamp) by
     * reassigning `rows` themselves in response.
     */
    interface ScarletTable {
        /**
          * Accessible label for the table, when there is no visible heading nearby.
         */
        "ariaLabel"?: string;
        /**
          * Makes whole rows clickable (emits `scarletRowClick`), e.g. to open a detail view.
          * @default false
         */
        "clickableRows"?: false;
        /**
          * Column definitions, in display order.
          * @default []
         */
        "columns"?: ScarletTableColumn[];
        /**
          * Message shown instead of rows when `rows` is empty.
          * @default 'Nenhum registro encontrado.'
         */
        "emptyMessage"?: "Nenhum registro encontrado.";
        /**
          * Optional per-cell formatter, overriding the default `String(row[column.key])`. Set as a JS property, not an HTML attribute.
         */
        "formatCell"?: (row: ScarletTableRow, column: ScarletTableColumn) => string;
        /**
          * Shows `loadingMessage` instead of rows/empty state.
          * @default false
         */
        "loading"?: false;
        /**
          * Message shown while `loading` is true.
          * @default 'Carregando…'
         */
        "loadingMessage"?: "Carregando…";
        /**
          * Emitted with the row's data when a row is clicked. Only fires when `clickableRows` is set.
         */
        "onScarletRowClick"?: (event: ScarletTableCustomEvent<ScarletTableRow>) => void;
        /**
          * Emitted with the full array of selected row keys whenever a row or the "select all" checkbox is toggled.
         */
        "onScarletSelectionChange"?: (event: ScarletTableCustomEvent<Array<string | number>>) => void;
        /**
          * Emitted when a sortable column header is activated. The table already re-sorts itself with a generic comparator — listen here only to replace that with custom logic.
         */
        "onScarletSort"?: (event: ScarletTableCustomEvent<ScarletTableSortChange>) => void;
        /**
          * Field on each row object holding its unique identifier — used for selection tracking.
          * @default 'id'
         */
        "rowKey"?: "id";
        /**
          * The data to render, one object per row.
          * @default []
         */
        "rows"?: ScarletTableRow[];
        /**
          * Shows a checkbox column and enables row selection.
          * @default false
         */
        "selectable"?: false;
        /**
          * Keys (from `rowKey`) of the currently selected rows.
          * @default []
         */
        "selectedRowKeys"?: Array<string | number>;
    }
    /**
     * A set of tabs with associated panels. Panel content is projected via a
     * named slot per item, matching `item.value`.
     */
    interface ScarletTabs {
        /**
          * The tabs to render.
          * @default []
         */
        "items"?: ScarletTabItem[];
        /**
          * Emitted when the selected tab changes.
         */
        "onScarletChange"?: (event: ScarletTabsCustomEvent<string>) => void;
        /**
          * Value of the currently selected tab. Defaults to the first enabled item.
         */
        "value"?: string;
    }
    /**
     * Body copy with the design system's text scale, color tokens and
     * single-line truncation — the general-purpose counterpart to `scarlet-heading`.
     */
    interface ScarletText {
        /**
          * Text alignment.
          * @default 'left'
         */
        "align"?: TextAlign;
        /**
          * HTML tag rendered for the text.
          * @default 'p'
         */
        "as"?: ScarletTextAs;
        /**
          * Text color token.
          * @default 'primary'
         */
        "color"?: TextColor;
        /**
          * Truncates overflowing text to a single line with an ellipsis.
          * @default false
         */
        "truncate"?: false;
        /**
          * Visual size, from the design system's body/caption text scale.
          * @default 'body-md'
         */
        "variant"?: ScarletTextVariant;
        /**
          * Overrides the variant's default font weight.
         */
        "weight"?: ScarletTextWeight;
    }
    /**
     * A multi-line labeled text input with helper/error text and built-in accessibility wiring.
     */
    interface ScarletTextarea {
        /**
          * Disables the textarea.
          * @default false
         */
        "disabled"?: false;
        /**
          * Error message rendered below the textarea. Implies the invalid state.
         */
        "errorMessage"?: string;
        /**
          * Helper text rendered below the textarea. Hidden while `errorMessage` is set.
         */
        "helperText"?: string;
        /**
          * Marks the textarea as invalid, independent of `errorMessage`.
          * @default false
         */
        "invalid"?: false;
        /**
          * Visible label rendered above the textarea.
         */
        "label"?: string;
        /**
          * Maximum number of characters allowed.
         */
        "maxlength"?: number;
        /**
          * Name submitted with a parent form.
         */
        "name"?: string;
        /**
          * Emitted when the textarea loses focus.
         */
        "onScarletBlur"?: (event: ScarletTextareaCustomEvent<FocusEvent>) => void;
        /**
          * Emitted when the textarea loses focus after its value has changed.
         */
        "onScarletChange"?: (event: ScarletTextareaCustomEvent<string>) => void;
        /**
          * Emitted when the textarea gains focus.
         */
        "onScarletFocus"?: (event: ScarletTextareaCustomEvent<FocusEvent>) => void;
        /**
          * Emitted on every keystroke with the current value.
         */
        "onScarletInput"?: (event: ScarletTextareaCustomEvent<string>) => void;
        /**
          * Placeholder text shown when the textarea is empty.
         */
        "placeholder"?: string;
        /**
          * Makes the textarea read-only.
          * @default false
         */
        "readonly"?: false;
        /**
          * Marks the textarea as required in a parent form.
          * @default false
         */
        "required"?: false;
        /**
          * Controls whether/how the user can resize the textarea.
          * @default 'vertical'
         */
        "resize"?: ScarletTextareaResize;
        /**
          * Number of visible text rows.
          * @default 4
         */
        "rows"?: 4;
        /**
          * Size of the textarea (affects padding and font size).
          * @default 'md'
         */
        "size"?: Size;
        /**
          * Current value of the textarea.
          * @default ''
         */
        "value"?: string;
    }
    /**
     * A vertical sequence of events — an order's status history, an activity
     * feed, a step-by-step audit trail.
     */
    interface ScarletTimeline {
        /**
          * The events, in order from first to last.
          * @default []
         */
        "items"?: ScarletTimelineItem[];
    }
    /**
     * A self-dismissing status message, fixed to a corner of the viewport.
     * For multiple simultaneous toasts, render several instances inside a
     * consumer-owned stacking container (this component does not manage a queue).
     */
    interface ScarletToast {
        /**
          * Shows a dismiss (close) button.
          * @default true
         */
        "dismissible"?: true;
        /**
          * Milliseconds before the toast auto-dismisses. `0` disables auto-dismiss.
          * @default 4000
         */
        "duration"?: 4000;
        /**
          * Emitted when the toast is dismissed (by timer, close button, or the `open` prop being set to false). Cancelable: call `event.preventDefault()` to keep it visible and handle removal yourself.
         */
        "onScarletDismiss"?: (event: ScarletToastCustomEvent<void>) => void;
        /**
          * Whether the toast is visible.
          * @default true
         */
        "open"?: boolean;
        /**
          * Corner of the viewport the toast is anchored to.
          * @default 'bottom-right'
         */
        "position"?: ScarletToastPosition;
        /**
          * Semantic status of the toast.
          * @default 'info'
         */
        "status"?: ScarletAlertStatus;
    }
    /**
     * A horizontal bar for grouping related actions (buttons, a search field,
     * an icon menu) — `role="toolbar"` with token-based gap, distinct from
     * `scarlet-stack` in intent (actions, not arbitrary layout) even though
     * the underlying flex mechanics are similar.
     */
    interface ScarletToolbar {
        /**
          * Accessible label for the `toolbar` role — required whenever there's more than one toolbar on the page.
         */
        "ariaLabel"?: string;
        /**
          * Main-axis distribution of items.
          * @default 'start'
         */
        "justify"?: JustifyContent;
    }
    /**
     * A text hint shown next to its trigger on hover/focus. Positioning is
     * plain CSS anchored to the host — it does not flip to stay in the
     * viewport, so pick a `placement` that has room to render.
     */
    interface ScarletTooltip {
        /**
          * Text content of the tooltip.
         */
        "content"?: string;
        /**
          * Delay in milliseconds before the tooltip appears.
          * @default 200
         */
        "delay"?: 200;
        /**
          * Disables the tooltip entirely.
          * @default false
         */
        "disabled"?: false;
        /**
          * Emitted when the tooltip is hidden.
         */
        "onScarletHide"?: (event: ScarletTooltipCustomEvent<void>) => void;
        /**
          * Emitted when the tooltip becomes visible.
         */
        "onScarletShow"?: (event: ScarletTooltipCustomEvent<void>) => void;
        /**
          * Where the tooltip renders relative to its trigger.
          * @default 'top'
         */
        "placement"?: ScarletTooltipPlacement;
    }

    interface ScarletAccordionAttributes {
        "multiple": false;
    }
    interface ScarletAlertAttributes {
        "status": ScarletAlertStatus;
        "variant": ScarletAlertVariant;
        "icon": true;
        "dismissible": false;
    }
    interface ScarletAvatarAttributes {
        "src": string;
        "alt": string;
        "name": string;
        "size": Size;
        "shape": ScarletAvatarShape;
    }
    interface ScarletAvatarGroupAttributes {
        "max": number;
        "size": Size;
    }
    interface ScarletBadgeAttributes {
        "color": Color;
        "variant": ScarletBadgeVariant;
        "size": ScarletBadgeSize;
        "dot": false;
    }
    interface ScarletBreadcrumbAttributes {
        "ariaLabel": "Navegação estrutural";
        "separator": "›";
    }
    interface ScarletButtonAttributes {
        "variant": Variant;
        "color": Color;
        "size": Size;
        "type": 'button' | 'submit' | 'reset';
        "disabled": false;
        "loading": false;
        "fullWidth": false;
        "iconOnly": false;
        "ariaLabel": string;
    }
    interface ScarletCardAttributes {
        "variant": ScarletCardVariant;
        "padding": Size;
        "interactive": false;
    }
    interface ScarletCheckboxAttributes {
        "checked": boolean;
        "indeterminate": false;
        "disabled": false;
        "required": false;
        "name": string;
        "value": string;
        "label": string;
    }
    interface ScarletCheckboxGroupAttributes {
        "name": string;
        "disabled": false;
        "horizontal": false;
        "ariaLabel": string;
    }
    interface ScarletChipAttributes {
        "color": Color;
        "variant": ScarletChipVariant;
        "removable": false;
        "disabled": false;
    }
    interface ScarletComboboxAttributes {
        "value": string;
        "placeholder": string;
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
        "noResultsMessage": "Nenhum resultado encontrado.";
    }
    interface ScarletContainerAttributes {
        "maxWidth": ScarletContainerMaxWidth;
        "padding": true;
        "center": true;
    }
    interface ScarletDatePickerAttributes {
        "name": string;
        "value": string;
        "placeholder": "DD/MM/AAAA";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
        "validate": true;
        "min": string;
        "max": string;
    }
    interface ScarletDateRangePickerAttributes {
        "startValue": string;
        "endValue": string;
        "startPlaceholder": "Data inicial";
        "endPlaceholder": "Data final";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
        "min": string;
        "max": string;
    }
    interface ScarletDividerAttributes {
        "orientation": ScarletDividerOrientation;
        "label": string;
    }
    interface ScarletDrawerAttributes {
        "open": boolean;
        "placement": ScarletDrawerPlacement;
        "size": ScarletDrawerSize;
        "ariaLabel": string;
        "dismissOnBackdropClick": true;
        "dismissOnEsc": true;
    }
    interface ScarletEmptyStateAttributes {
        "icon": ScarletIconName;
        "heading": "Nenhum resultado encontrado.";
        "description": string;
    }
    interface ScarletFileUploadAttributes {
        "accept": string;
        "multiple": false;
        "disabled": false;
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "maxSizeBytes": number;
    }
    interface ScarletGridAttributes {
        "columns": 12;
        "columnsSm": number;
        "columnsMd": number;
        "columnsLg": number;
        "columnsXl": number;
        "gap": Size;
        "rowGap": Size;
        "columnGap": Size;
        "align": Alignment;
        "justify": Alignment;
    }
    interface ScarletGridItemAttributes {
        "colSpan": 1;
        "colSpanSm": number;
        "colSpanMd": number;
        "colSpanLg": number;
        "colSpanXl": number;
        "rowSpan": 1;
        "rowSpanSm": number;
        "rowSpanMd": number;
        "rowSpanLg": number;
        "rowSpanXl": number;
    }
    interface ScarletHeadingAttributes {
        "level": ScarletHeadingLevel;
        "variant": ScarletHeadingVariant;
        "color": TextColor;
        "align": TextAlign;
    }
    interface ScarletIconAttributes {
        "name": ScarletIconName;
        "size": string;
        "label": string;
    }
    interface ScarletInputAttributes {
        "type": ScarletInputType;
        "name": string;
        "value": string;
        "placeholder": string;
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "readonly": false;
        "required": false;
        "size": Size;
        "autocomplete": string;
    }
    interface ScarletInputCepAttributes {
        "name": string;
        "value": string;
        "placeholder": "00000-000";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
    }
    interface ScarletInputCreditCardAttributes {
        "name": string;
        "value": string;
        "placeholder": "0000 0000 0000 0000";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
        "validate": true;
    }
    interface ScarletInputCurrencyAttributes {
        "name": string;
        "value": string;
        "currencySymbol": "R$";
        "placeholder": "R$ 0,00";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
    }
    interface ScarletInputDateAttributes {
        "name": string;
        "value": string;
        "placeholder": "DD/MM/AAAA";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
        "validate": true;
    }
    interface ScarletInputDocumentAttributes {
        "name": string;
        "value": string;
        "placeholder": "CPF ou CNPJ";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
        "validate": true;
    }
    interface ScarletInputLicensePlateAttributes {
        "name": string;
        "value": string;
        "placeholder": "ABC-1234";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
    }
    interface ScarletInputPercentageAttributes {
        "name": string;
        "value": string;
        "decimals": 2;
        "placeholder": "0,00%";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
    }
    interface ScarletInputPhoneAttributes {
        "name": string;
        "value": string;
        "placeholder": "(11) 91234-5678";
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
    }
    interface ScarletLinkAttributes {
        "href": string;
        "target": string;
        "rel": string;
        "underline": ScarletLinkUnderline;
        "color": Color;
        "disabled": false;
    }
    interface ScarletMenuAttributes {
        "ariaLabel": "Menu";
        "placement": 'start' | 'end';
    }
    interface ScarletModalAttributes {
        "open": boolean;
        "size": ScarletModalSize;
        "ariaLabel": string;
        "dismissOnBackdropClick": true;
        "dismissOnEsc": true;
    }
    interface ScarletNumberInputAttributes {
        "value": number;
        "min": number;
        "max": number;
        "step": 1;
        "name": string;
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
    }
    interface ScarletPaginationAttributes {
        "totalPages": 1;
        "page": number;
        "siblingCount": 1;
        "ariaLabel": "Paginação";
    }
    interface ScarletPopoverAttributes {
        "placement": ScarletPopoverPlacement;
        "triggerMode": ScarletPopoverTriggerMode;
        "ariaLabel": string;
    }
    interface ScarletProgressAttributes {
        "value": 0;
        "max": 100;
        "color": Color;
        "size": ScarletProgressSize;
        "showLabel": false;
        "ariaLabel": "Progresso";
    }
    interface ScarletRadioAttributes {
        "value": string;
        "checked": boolean;
        "disabled": boolean;
        "name": string;
        "label": string;
        "focusable": boolean;
    }
    interface ScarletRadioGroupAttributes {
        "name": string;
        "value": string;
        "disabled": false;
        "horizontal": false;
        "ariaLabel": string;
    }
    interface ScarletSelectAttributes {
        "name": string;
        "value": string;
        "placeholder": string;
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "required": false;
        "size": Size;
    }
    interface ScarletSkeletonAttributes {
        "variant": ScarletSkeletonVariant;
        "width": string;
        "height": string;
        "lines": 1;
    }
    interface ScarletSpinnerAttributes {
        "variant": ScarletSpinnerVariant;
        "size": ScarletSpinnerSize;
        "label": "Carregando";
    }
    interface ScarletStackAttributes {
        "direction": Direction;
        "directionSm": Direction;
        "directionMd": Direction;
        "directionLg": Direction;
        "directionXl": Direction;
        "gap": Size;
        "align": Alignment;
        "justify": JustifyContent;
        "wrap": Wrap;
    }
    interface ScarletStatAttributes {
        "label": "";
        "value": "";
        "change": string;
        "trend": ScarletStatTrend;
    }
    interface ScarletSwitchAttributes {
        "checked": boolean;
        "disabled": false;
        "required": false;
        "name": string;
        "value": string;
        "label": string;
    }
    interface ScarletTableAttributes {
        "rowKey": "id";
        "selectable": false;
        "clickableRows": false;
        "emptyMessage": "Nenhum registro encontrado.";
        "loading": false;
        "loadingMessage": "Carregando…";
        "ariaLabel": string;
    }
    interface ScarletTabsAttributes {
        "value": string;
    }
    interface ScarletTextAttributes {
        "as": ScarletTextAs;
        "variant": ScarletTextVariant;
        "color": TextColor;
        "align": TextAlign;
        "weight": ScarletTextWeight;
        "truncate": false;
    }
    interface ScarletTextareaAttributes {
        "name": string;
        "value": string;
        "placeholder": string;
        "label": string;
        "helperText": string;
        "errorMessage": string;
        "invalid": false;
        "disabled": false;
        "readonly": false;
        "required": false;
        "rows": 4;
        "maxlength": number;
        "resize": ScarletTextareaResize;
        "size": Size;
    }
    interface ScarletToastAttributes {
        "open": boolean;
        "status": ScarletAlertStatus;
        "duration": 4000;
        "dismissible": true;
        "position": ScarletToastPosition;
    }
    interface ScarletToolbarAttributes {
        "ariaLabel": string;
        "justify": JustifyContent;
    }
    interface ScarletTooltipAttributes {
        "content": string;
        "placement": ScarletTooltipPlacement;
        "delay": 200;
        "disabled": false;
    }

    interface IntrinsicElements {
        "scarlet-accordion": Omit<ScarletAccordion, keyof ScarletAccordionAttributes> & { [K in keyof ScarletAccordion & keyof ScarletAccordionAttributes]?: ScarletAccordion[K] } & { [K in keyof ScarletAccordion & keyof ScarletAccordionAttributes as `attr:${K}`]?: ScarletAccordionAttributes[K] } & { [K in keyof ScarletAccordion & keyof ScarletAccordionAttributes as `prop:${K}`]?: ScarletAccordion[K] };
        "scarlet-alert": Omit<ScarletAlert, keyof ScarletAlertAttributes> & { [K in keyof ScarletAlert & keyof ScarletAlertAttributes]?: ScarletAlert[K] } & { [K in keyof ScarletAlert & keyof ScarletAlertAttributes as `attr:${K}`]?: ScarletAlertAttributes[K] } & { [K in keyof ScarletAlert & keyof ScarletAlertAttributes as `prop:${K}`]?: ScarletAlert[K] };
        "scarlet-avatar": Omit<ScarletAvatar, keyof ScarletAvatarAttributes> & { [K in keyof ScarletAvatar & keyof ScarletAvatarAttributes]?: ScarletAvatar[K] } & { [K in keyof ScarletAvatar & keyof ScarletAvatarAttributes as `attr:${K}`]?: ScarletAvatarAttributes[K] } & { [K in keyof ScarletAvatar & keyof ScarletAvatarAttributes as `prop:${K}`]?: ScarletAvatar[K] };
        "scarlet-avatar-group": Omit<ScarletAvatarGroup, keyof ScarletAvatarGroupAttributes> & { [K in keyof ScarletAvatarGroup & keyof ScarletAvatarGroupAttributes]?: ScarletAvatarGroup[K] } & { [K in keyof ScarletAvatarGroup & keyof ScarletAvatarGroupAttributes as `attr:${K}`]?: ScarletAvatarGroupAttributes[K] } & { [K in keyof ScarletAvatarGroup & keyof ScarletAvatarGroupAttributes as `prop:${K}`]?: ScarletAvatarGroup[K] };
        "scarlet-badge": Omit<ScarletBadge, keyof ScarletBadgeAttributes> & { [K in keyof ScarletBadge & keyof ScarletBadgeAttributes]?: ScarletBadge[K] } & { [K in keyof ScarletBadge & keyof ScarletBadgeAttributes as `attr:${K}`]?: ScarletBadgeAttributes[K] } & { [K in keyof ScarletBadge & keyof ScarletBadgeAttributes as `prop:${K}`]?: ScarletBadge[K] };
        "scarlet-breadcrumb": Omit<ScarletBreadcrumb, keyof ScarletBreadcrumbAttributes> & { [K in keyof ScarletBreadcrumb & keyof ScarletBreadcrumbAttributes]?: ScarletBreadcrumb[K] } & { [K in keyof ScarletBreadcrumb & keyof ScarletBreadcrumbAttributes as `attr:${K}`]?: ScarletBreadcrumbAttributes[K] } & { [K in keyof ScarletBreadcrumb & keyof ScarletBreadcrumbAttributes as `prop:${K}`]?: ScarletBreadcrumb[K] };
        "scarlet-button": Omit<ScarletButton, keyof ScarletButtonAttributes> & { [K in keyof ScarletButton & keyof ScarletButtonAttributes]?: ScarletButton[K] } & { [K in keyof ScarletButton & keyof ScarletButtonAttributes as `attr:${K}`]?: ScarletButtonAttributes[K] } & { [K in keyof ScarletButton & keyof ScarletButtonAttributes as `prop:${K}`]?: ScarletButton[K] };
        "scarlet-card": Omit<ScarletCard, keyof ScarletCardAttributes> & { [K in keyof ScarletCard & keyof ScarletCardAttributes]?: ScarletCard[K] } & { [K in keyof ScarletCard & keyof ScarletCardAttributes as `attr:${K}`]?: ScarletCardAttributes[K] } & { [K in keyof ScarletCard & keyof ScarletCardAttributes as `prop:${K}`]?: ScarletCard[K] };
        "scarlet-checkbox": Omit<ScarletCheckbox, keyof ScarletCheckboxAttributes> & { [K in keyof ScarletCheckbox & keyof ScarletCheckboxAttributes]?: ScarletCheckbox[K] } & { [K in keyof ScarletCheckbox & keyof ScarletCheckboxAttributes as `attr:${K}`]?: ScarletCheckboxAttributes[K] } & { [K in keyof ScarletCheckbox & keyof ScarletCheckboxAttributes as `prop:${K}`]?: ScarletCheckbox[K] };
        "scarlet-checkbox-group": Omit<ScarletCheckboxGroup, keyof ScarletCheckboxGroupAttributes> & { [K in keyof ScarletCheckboxGroup & keyof ScarletCheckboxGroupAttributes]?: ScarletCheckboxGroup[K] } & { [K in keyof ScarletCheckboxGroup & keyof ScarletCheckboxGroupAttributes as `attr:${K}`]?: ScarletCheckboxGroupAttributes[K] } & { [K in keyof ScarletCheckboxGroup & keyof ScarletCheckboxGroupAttributes as `prop:${K}`]?: ScarletCheckboxGroup[K] };
        "scarlet-chip": Omit<ScarletChip, keyof ScarletChipAttributes> & { [K in keyof ScarletChip & keyof ScarletChipAttributes]?: ScarletChip[K] } & { [K in keyof ScarletChip & keyof ScarletChipAttributes as `attr:${K}`]?: ScarletChipAttributes[K] } & { [K in keyof ScarletChip & keyof ScarletChipAttributes as `prop:${K}`]?: ScarletChip[K] };
        "scarlet-combobox": Omit<ScarletCombobox, keyof ScarletComboboxAttributes> & { [K in keyof ScarletCombobox & keyof ScarletComboboxAttributes]?: ScarletCombobox[K] } & { [K in keyof ScarletCombobox & keyof ScarletComboboxAttributes as `attr:${K}`]?: ScarletComboboxAttributes[K] } & { [K in keyof ScarletCombobox & keyof ScarletComboboxAttributes as `prop:${K}`]?: ScarletCombobox[K] };
        "scarlet-container": Omit<ScarletContainer, keyof ScarletContainerAttributes> & { [K in keyof ScarletContainer & keyof ScarletContainerAttributes]?: ScarletContainer[K] } & { [K in keyof ScarletContainer & keyof ScarletContainerAttributes as `attr:${K}`]?: ScarletContainerAttributes[K] } & { [K in keyof ScarletContainer & keyof ScarletContainerAttributes as `prop:${K}`]?: ScarletContainer[K] };
        "scarlet-date-picker": Omit<ScarletDatePicker, keyof ScarletDatePickerAttributes> & { [K in keyof ScarletDatePicker & keyof ScarletDatePickerAttributes]?: ScarletDatePicker[K] } & { [K in keyof ScarletDatePicker & keyof ScarletDatePickerAttributes as `attr:${K}`]?: ScarletDatePickerAttributes[K] } & { [K in keyof ScarletDatePicker & keyof ScarletDatePickerAttributes as `prop:${K}`]?: ScarletDatePicker[K] };
        "scarlet-date-range-picker": Omit<ScarletDateRangePicker, keyof ScarletDateRangePickerAttributes> & { [K in keyof ScarletDateRangePicker & keyof ScarletDateRangePickerAttributes]?: ScarletDateRangePicker[K] } & { [K in keyof ScarletDateRangePicker & keyof ScarletDateRangePickerAttributes as `attr:${K}`]?: ScarletDateRangePickerAttributes[K] } & { [K in keyof ScarletDateRangePicker & keyof ScarletDateRangePickerAttributes as `prop:${K}`]?: ScarletDateRangePicker[K] };
        "scarlet-divider": Omit<ScarletDivider, keyof ScarletDividerAttributes> & { [K in keyof ScarletDivider & keyof ScarletDividerAttributes]?: ScarletDivider[K] } & { [K in keyof ScarletDivider & keyof ScarletDividerAttributes as `attr:${K}`]?: ScarletDividerAttributes[K] } & { [K in keyof ScarletDivider & keyof ScarletDividerAttributes as `prop:${K}`]?: ScarletDivider[K] };
        "scarlet-drawer": Omit<ScarletDrawer, keyof ScarletDrawerAttributes> & { [K in keyof ScarletDrawer & keyof ScarletDrawerAttributes]?: ScarletDrawer[K] } & { [K in keyof ScarletDrawer & keyof ScarletDrawerAttributes as `attr:${K}`]?: ScarletDrawerAttributes[K] } & { [K in keyof ScarletDrawer & keyof ScarletDrawerAttributes as `prop:${K}`]?: ScarletDrawer[K] };
        "scarlet-empty-state": Omit<ScarletEmptyState, keyof ScarletEmptyStateAttributes> & { [K in keyof ScarletEmptyState & keyof ScarletEmptyStateAttributes]?: ScarletEmptyState[K] } & { [K in keyof ScarletEmptyState & keyof ScarletEmptyStateAttributes as `attr:${K}`]?: ScarletEmptyStateAttributes[K] } & { [K in keyof ScarletEmptyState & keyof ScarletEmptyStateAttributes as `prop:${K}`]?: ScarletEmptyState[K] };
        "scarlet-file-upload": Omit<ScarletFileUpload, keyof ScarletFileUploadAttributes> & { [K in keyof ScarletFileUpload & keyof ScarletFileUploadAttributes]?: ScarletFileUpload[K] } & { [K in keyof ScarletFileUpload & keyof ScarletFileUploadAttributes as `attr:${K}`]?: ScarletFileUploadAttributes[K] } & { [K in keyof ScarletFileUpload & keyof ScarletFileUploadAttributes as `prop:${K}`]?: ScarletFileUpload[K] };
        "scarlet-grid": Omit<ScarletGrid, keyof ScarletGridAttributes> & { [K in keyof ScarletGrid & keyof ScarletGridAttributes]?: ScarletGrid[K] } & { [K in keyof ScarletGrid & keyof ScarletGridAttributes as `attr:${K}`]?: ScarletGridAttributes[K] } & { [K in keyof ScarletGrid & keyof ScarletGridAttributes as `prop:${K}`]?: ScarletGrid[K] };
        "scarlet-grid-item": Omit<ScarletGridItem, keyof ScarletGridItemAttributes> & { [K in keyof ScarletGridItem & keyof ScarletGridItemAttributes]?: ScarletGridItem[K] } & { [K in keyof ScarletGridItem & keyof ScarletGridItemAttributes as `attr:${K}`]?: ScarletGridItemAttributes[K] } & { [K in keyof ScarletGridItem & keyof ScarletGridItemAttributes as `prop:${K}`]?: ScarletGridItem[K] };
        "scarlet-heading": Omit<ScarletHeading, keyof ScarletHeadingAttributes> & { [K in keyof ScarletHeading & keyof ScarletHeadingAttributes]?: ScarletHeading[K] } & { [K in keyof ScarletHeading & keyof ScarletHeadingAttributes as `attr:${K}`]?: ScarletHeadingAttributes[K] } & { [K in keyof ScarletHeading & keyof ScarletHeadingAttributes as `prop:${K}`]?: ScarletHeading[K] };
        "scarlet-icon": Omit<ScarletIcon, keyof ScarletIconAttributes> & { [K in keyof ScarletIcon & keyof ScarletIconAttributes]?: ScarletIcon[K] } & { [K in keyof ScarletIcon & keyof ScarletIconAttributes as `attr:${K}`]?: ScarletIconAttributes[K] } & { [K in keyof ScarletIcon & keyof ScarletIconAttributes as `prop:${K}`]?: ScarletIcon[K] };
        "scarlet-input": Omit<ScarletInput, keyof ScarletInputAttributes> & { [K in keyof ScarletInput & keyof ScarletInputAttributes]?: ScarletInput[K] } & { [K in keyof ScarletInput & keyof ScarletInputAttributes as `attr:${K}`]?: ScarletInputAttributes[K] } & { [K in keyof ScarletInput & keyof ScarletInputAttributes as `prop:${K}`]?: ScarletInput[K] };
        "scarlet-input-cep": Omit<ScarletInputCep, keyof ScarletInputCepAttributes> & { [K in keyof ScarletInputCep & keyof ScarletInputCepAttributes]?: ScarletInputCep[K] } & { [K in keyof ScarletInputCep & keyof ScarletInputCepAttributes as `attr:${K}`]?: ScarletInputCepAttributes[K] } & { [K in keyof ScarletInputCep & keyof ScarletInputCepAttributes as `prop:${K}`]?: ScarletInputCep[K] };
        "scarlet-input-credit-card": Omit<ScarletInputCreditCard, keyof ScarletInputCreditCardAttributes> & { [K in keyof ScarletInputCreditCard & keyof ScarletInputCreditCardAttributes]?: ScarletInputCreditCard[K] } & { [K in keyof ScarletInputCreditCard & keyof ScarletInputCreditCardAttributes as `attr:${K}`]?: ScarletInputCreditCardAttributes[K] } & { [K in keyof ScarletInputCreditCard & keyof ScarletInputCreditCardAttributes as `prop:${K}`]?: ScarletInputCreditCard[K] };
        "scarlet-input-currency": Omit<ScarletInputCurrency, keyof ScarletInputCurrencyAttributes> & { [K in keyof ScarletInputCurrency & keyof ScarletInputCurrencyAttributes]?: ScarletInputCurrency[K] } & { [K in keyof ScarletInputCurrency & keyof ScarletInputCurrencyAttributes as `attr:${K}`]?: ScarletInputCurrencyAttributes[K] } & { [K in keyof ScarletInputCurrency & keyof ScarletInputCurrencyAttributes as `prop:${K}`]?: ScarletInputCurrency[K] };
        "scarlet-input-date": Omit<ScarletInputDate, keyof ScarletInputDateAttributes> & { [K in keyof ScarletInputDate & keyof ScarletInputDateAttributes]?: ScarletInputDate[K] } & { [K in keyof ScarletInputDate & keyof ScarletInputDateAttributes as `attr:${K}`]?: ScarletInputDateAttributes[K] } & { [K in keyof ScarletInputDate & keyof ScarletInputDateAttributes as `prop:${K}`]?: ScarletInputDate[K] };
        "scarlet-input-document": Omit<ScarletInputDocument, keyof ScarletInputDocumentAttributes> & { [K in keyof ScarletInputDocument & keyof ScarletInputDocumentAttributes]?: ScarletInputDocument[K] } & { [K in keyof ScarletInputDocument & keyof ScarletInputDocumentAttributes as `attr:${K}`]?: ScarletInputDocumentAttributes[K] } & { [K in keyof ScarletInputDocument & keyof ScarletInputDocumentAttributes as `prop:${K}`]?: ScarletInputDocument[K] };
        "scarlet-input-license-plate": Omit<ScarletInputLicensePlate, keyof ScarletInputLicensePlateAttributes> & { [K in keyof ScarletInputLicensePlate & keyof ScarletInputLicensePlateAttributes]?: ScarletInputLicensePlate[K] } & { [K in keyof ScarletInputLicensePlate & keyof ScarletInputLicensePlateAttributes as `attr:${K}`]?: ScarletInputLicensePlateAttributes[K] } & { [K in keyof ScarletInputLicensePlate & keyof ScarletInputLicensePlateAttributes as `prop:${K}`]?: ScarletInputLicensePlate[K] };
        "scarlet-input-percentage": Omit<ScarletInputPercentage, keyof ScarletInputPercentageAttributes> & { [K in keyof ScarletInputPercentage & keyof ScarletInputPercentageAttributes]?: ScarletInputPercentage[K] } & { [K in keyof ScarletInputPercentage & keyof ScarletInputPercentageAttributes as `attr:${K}`]?: ScarletInputPercentageAttributes[K] } & { [K in keyof ScarletInputPercentage & keyof ScarletInputPercentageAttributes as `prop:${K}`]?: ScarletInputPercentage[K] };
        "scarlet-input-phone": Omit<ScarletInputPhone, keyof ScarletInputPhoneAttributes> & { [K in keyof ScarletInputPhone & keyof ScarletInputPhoneAttributes]?: ScarletInputPhone[K] } & { [K in keyof ScarletInputPhone & keyof ScarletInputPhoneAttributes as `attr:${K}`]?: ScarletInputPhoneAttributes[K] } & { [K in keyof ScarletInputPhone & keyof ScarletInputPhoneAttributes as `prop:${K}`]?: ScarletInputPhone[K] };
        "scarlet-link": Omit<ScarletLink, keyof ScarletLinkAttributes> & { [K in keyof ScarletLink & keyof ScarletLinkAttributes]?: ScarletLink[K] } & { [K in keyof ScarletLink & keyof ScarletLinkAttributes as `attr:${K}`]?: ScarletLinkAttributes[K] } & { [K in keyof ScarletLink & keyof ScarletLinkAttributes as `prop:${K}`]?: ScarletLink[K] };
        "scarlet-menu": Omit<ScarletMenu, keyof ScarletMenuAttributes> & { [K in keyof ScarletMenu & keyof ScarletMenuAttributes]?: ScarletMenu[K] } & { [K in keyof ScarletMenu & keyof ScarletMenuAttributes as `attr:${K}`]?: ScarletMenuAttributes[K] } & { [K in keyof ScarletMenu & keyof ScarletMenuAttributes as `prop:${K}`]?: ScarletMenu[K] };
        "scarlet-modal": Omit<ScarletModal, keyof ScarletModalAttributes> & { [K in keyof ScarletModal & keyof ScarletModalAttributes]?: ScarletModal[K] } & { [K in keyof ScarletModal & keyof ScarletModalAttributes as `attr:${K}`]?: ScarletModalAttributes[K] } & { [K in keyof ScarletModal & keyof ScarletModalAttributes as `prop:${K}`]?: ScarletModal[K] };
        "scarlet-number-input": Omit<ScarletNumberInput, keyof ScarletNumberInputAttributes> & { [K in keyof ScarletNumberInput & keyof ScarletNumberInputAttributes]?: ScarletNumberInput[K] } & { [K in keyof ScarletNumberInput & keyof ScarletNumberInputAttributes as `attr:${K}`]?: ScarletNumberInputAttributes[K] } & { [K in keyof ScarletNumberInput & keyof ScarletNumberInputAttributes as `prop:${K}`]?: ScarletNumberInput[K] };
        "scarlet-pagination": Omit<ScarletPagination, keyof ScarletPaginationAttributes> & { [K in keyof ScarletPagination & keyof ScarletPaginationAttributes]?: ScarletPagination[K] } & { [K in keyof ScarletPagination & keyof ScarletPaginationAttributes as `attr:${K}`]?: ScarletPaginationAttributes[K] } & { [K in keyof ScarletPagination & keyof ScarletPaginationAttributes as `prop:${K}`]?: ScarletPagination[K] };
        "scarlet-popover": Omit<ScarletPopover, keyof ScarletPopoverAttributes> & { [K in keyof ScarletPopover & keyof ScarletPopoverAttributes]?: ScarletPopover[K] } & { [K in keyof ScarletPopover & keyof ScarletPopoverAttributes as `attr:${K}`]?: ScarletPopoverAttributes[K] } & { [K in keyof ScarletPopover & keyof ScarletPopoverAttributes as `prop:${K}`]?: ScarletPopover[K] };
        "scarlet-progress": Omit<ScarletProgress, keyof ScarletProgressAttributes> & { [K in keyof ScarletProgress & keyof ScarletProgressAttributes]?: ScarletProgress[K] } & { [K in keyof ScarletProgress & keyof ScarletProgressAttributes as `attr:${K}`]?: ScarletProgressAttributes[K] } & { [K in keyof ScarletProgress & keyof ScarletProgressAttributes as `prop:${K}`]?: ScarletProgress[K] };
        "scarlet-radio": Omit<ScarletRadio, keyof ScarletRadioAttributes> & { [K in keyof ScarletRadio & keyof ScarletRadioAttributes]?: ScarletRadio[K] } & { [K in keyof ScarletRadio & keyof ScarletRadioAttributes as `attr:${K}`]?: ScarletRadioAttributes[K] } & { [K in keyof ScarletRadio & keyof ScarletRadioAttributes as `prop:${K}`]?: ScarletRadio[K] };
        "scarlet-radio-group": Omit<ScarletRadioGroup, keyof ScarletRadioGroupAttributes> & { [K in keyof ScarletRadioGroup & keyof ScarletRadioGroupAttributes]?: ScarletRadioGroup[K] } & { [K in keyof ScarletRadioGroup & keyof ScarletRadioGroupAttributes as `attr:${K}`]?: ScarletRadioGroupAttributes[K] } & { [K in keyof ScarletRadioGroup & keyof ScarletRadioGroupAttributes as `prop:${K}`]?: ScarletRadioGroup[K] };
        "scarlet-select": Omit<ScarletSelect, keyof ScarletSelectAttributes> & { [K in keyof ScarletSelect & keyof ScarletSelectAttributes]?: ScarletSelect[K] } & { [K in keyof ScarletSelect & keyof ScarletSelectAttributes as `attr:${K}`]?: ScarletSelectAttributes[K] } & { [K in keyof ScarletSelect & keyof ScarletSelectAttributes as `prop:${K}`]?: ScarletSelect[K] };
        "scarlet-skeleton": Omit<ScarletSkeleton, keyof ScarletSkeletonAttributes> & { [K in keyof ScarletSkeleton & keyof ScarletSkeletonAttributes]?: ScarletSkeleton[K] } & { [K in keyof ScarletSkeleton & keyof ScarletSkeletonAttributes as `attr:${K}`]?: ScarletSkeletonAttributes[K] } & { [K in keyof ScarletSkeleton & keyof ScarletSkeletonAttributes as `prop:${K}`]?: ScarletSkeleton[K] };
        "scarlet-spinner": Omit<ScarletSpinner, keyof ScarletSpinnerAttributes> & { [K in keyof ScarletSpinner & keyof ScarletSpinnerAttributes]?: ScarletSpinner[K] } & { [K in keyof ScarletSpinner & keyof ScarletSpinnerAttributes as `attr:${K}`]?: ScarletSpinnerAttributes[K] } & { [K in keyof ScarletSpinner & keyof ScarletSpinnerAttributes as `prop:${K}`]?: ScarletSpinner[K] };
        "scarlet-stack": Omit<ScarletStack, keyof ScarletStackAttributes> & { [K in keyof ScarletStack & keyof ScarletStackAttributes]?: ScarletStack[K] } & { [K in keyof ScarletStack & keyof ScarletStackAttributes as `attr:${K}`]?: ScarletStackAttributes[K] } & { [K in keyof ScarletStack & keyof ScarletStackAttributes as `prop:${K}`]?: ScarletStack[K] };
        "scarlet-stat": Omit<ScarletStat, keyof ScarletStatAttributes> & { [K in keyof ScarletStat & keyof ScarletStatAttributes]?: ScarletStat[K] } & { [K in keyof ScarletStat & keyof ScarletStatAttributes as `attr:${K}`]?: ScarletStatAttributes[K] } & { [K in keyof ScarletStat & keyof ScarletStatAttributes as `prop:${K}`]?: ScarletStat[K] };
        "scarlet-switch": Omit<ScarletSwitch, keyof ScarletSwitchAttributes> & { [K in keyof ScarletSwitch & keyof ScarletSwitchAttributes]?: ScarletSwitch[K] } & { [K in keyof ScarletSwitch & keyof ScarletSwitchAttributes as `attr:${K}`]?: ScarletSwitchAttributes[K] } & { [K in keyof ScarletSwitch & keyof ScarletSwitchAttributes as `prop:${K}`]?: ScarletSwitch[K] };
        "scarlet-table": Omit<ScarletTable, keyof ScarletTableAttributes> & { [K in keyof ScarletTable & keyof ScarletTableAttributes]?: ScarletTable[K] } & { [K in keyof ScarletTable & keyof ScarletTableAttributes as `attr:${K}`]?: ScarletTableAttributes[K] } & { [K in keyof ScarletTable & keyof ScarletTableAttributes as `prop:${K}`]?: ScarletTable[K] };
        "scarlet-tabs": Omit<ScarletTabs, keyof ScarletTabsAttributes> & { [K in keyof ScarletTabs & keyof ScarletTabsAttributes]?: ScarletTabs[K] } & { [K in keyof ScarletTabs & keyof ScarletTabsAttributes as `attr:${K}`]?: ScarletTabsAttributes[K] } & { [K in keyof ScarletTabs & keyof ScarletTabsAttributes as `prop:${K}`]?: ScarletTabs[K] };
        "scarlet-text": Omit<ScarletText, keyof ScarletTextAttributes> & { [K in keyof ScarletText & keyof ScarletTextAttributes]?: ScarletText[K] } & { [K in keyof ScarletText & keyof ScarletTextAttributes as `attr:${K}`]?: ScarletTextAttributes[K] } & { [K in keyof ScarletText & keyof ScarletTextAttributes as `prop:${K}`]?: ScarletText[K] };
        "scarlet-textarea": Omit<ScarletTextarea, keyof ScarletTextareaAttributes> & { [K in keyof ScarletTextarea & keyof ScarletTextareaAttributes]?: ScarletTextarea[K] } & { [K in keyof ScarletTextarea & keyof ScarletTextareaAttributes as `attr:${K}`]?: ScarletTextareaAttributes[K] } & { [K in keyof ScarletTextarea & keyof ScarletTextareaAttributes as `prop:${K}`]?: ScarletTextarea[K] };
        "scarlet-timeline": ScarletTimeline;
        "scarlet-toast": Omit<ScarletToast, keyof ScarletToastAttributes> & { [K in keyof ScarletToast & keyof ScarletToastAttributes]?: ScarletToast[K] } & { [K in keyof ScarletToast & keyof ScarletToastAttributes as `attr:${K}`]?: ScarletToastAttributes[K] } & { [K in keyof ScarletToast & keyof ScarletToastAttributes as `prop:${K}`]?: ScarletToast[K] };
        "scarlet-toolbar": Omit<ScarletToolbar, keyof ScarletToolbarAttributes> & { [K in keyof ScarletToolbar & keyof ScarletToolbarAttributes]?: ScarletToolbar[K] } & { [K in keyof ScarletToolbar & keyof ScarletToolbarAttributes as `attr:${K}`]?: ScarletToolbarAttributes[K] } & { [K in keyof ScarletToolbar & keyof ScarletToolbarAttributes as `prop:${K}`]?: ScarletToolbar[K] };
        "scarlet-tooltip": Omit<ScarletTooltip, keyof ScarletTooltipAttributes> & { [K in keyof ScarletTooltip & keyof ScarletTooltipAttributes]?: ScarletTooltip[K] } & { [K in keyof ScarletTooltip & keyof ScarletTooltipAttributes as `attr:${K}`]?: ScarletTooltipAttributes[K] } & { [K in keyof ScarletTooltip & keyof ScarletTooltipAttributes as `prop:${K}`]?: ScarletTooltip[K] };
    }
}
export { LocalJSX as JSX };
declare module "@stencil/core" {
    export namespace JSX {
        interface IntrinsicElements {
            /**
             * A set of collapsible sections. Panel content is projected via a named
             * slot per item, matching `item.value`.
             */
            "scarlet-accordion": LocalJSX.IntrinsicElements["scarlet-accordion"] & JSXBase.HTMLAttributes<HTMLScarletAccordionElement>;
            /**
             * An inline status message with optional title, icon and dismiss action.
             */
            "scarlet-alert": LocalJSX.IntrinsicElements["scarlet-alert"] & JSXBase.HTMLAttributes<HTMLScarletAlertElement>;
            /**
             * A user/entity avatar: shows an image, falling back to initials derived
             * from `name`, falling back to a generic icon.
             */
            "scarlet-avatar": LocalJSX.IntrinsicElements["scarlet-avatar"] & JSXBase.HTMLAttributes<HTMLScarletAvatarElement>;
            /**
             * Overlaps a set of `<scarlet-avatar>` children into a stack, with an
             * optional "+N" indicator when there are more than `max`. Propagates `size`
             * down to every avatar so they all render at the same size regardless of
             * what each one was given individually.
             */
            "scarlet-avatar-group": LocalJSX.IntrinsicElements["scarlet-avatar-group"] & JSXBase.HTMLAttributes<HTMLScarletAvatarGroupElement>;
            /**
             * A small status/count label, or a bare notification dot.
             */
            "scarlet-badge": LocalJSX.IntrinsicElements["scarlet-badge"] & JSXBase.HTMLAttributes<HTMLScarletBadgeElement>;
            /**
             * A trail of links showing the current page's position in a hierarchy,
             * following the WAI-ARIA breadcrumb pattern (`<nav aria-label>` wrapping an
             * ordered list, the last item marked `aria-current="page"`).
             * Items render as real `<a href>` elements — usable with no JS at all — but
             * every click first emits a cancelable `scarletNavigate`, so a framework
             * router can call `preventDefault()` and handle the navigation itself
             * instead of a full page load.
             */
            "scarlet-breadcrumb": LocalJSX.IntrinsicElements["scarlet-breadcrumb"] & JSXBase.HTMLAttributes<HTMLScarletBreadcrumbElement>;
            /**
             * A clickable action element with solid, outline, ghost and link variants.
             * An icon (e.g. `<scarlet-icon>`) goes in the `start`/`end` slot alongside
             * the default slot's text — `<scarlet-button><scarlet-icon slot="start"
             * name="check" />Salvar</scarlet-button>`. For an icon with no visible text
             * at all, add `iconOnly` (which turns the button square instead of its
             * usual text-driven width) and `ariaLabel` (required then — nothing else
             * gives the button an accessible name).
             */
            "scarlet-button": LocalJSX.IntrinsicElements["scarlet-button"] & JSXBase.HTMLAttributes<HTMLScarletButtonElement>;
            /**
             * A container for grouping related content, with optional header/footer slots.
             */
            "scarlet-card": LocalJSX.IntrinsicElements["scarlet-card"] & JSXBase.HTMLAttributes<HTMLScarletCardElement>;
            /**
             * A checkbox input with label, indeterminate state and accessible wiring.
             */
            "scarlet-checkbox": LocalJSX.IntrinsicElements["scarlet-checkbox"] & JSXBase.HTMLAttributes<HTMLScarletCheckboxElement>;
            /**
             * Groups a set of `<scarlet-checkbox>` options and coordinates which ones
             * are checked as a single `value` array — unlike `<scarlet-radio-group>`,
             * each checkbox keeps its own native Tab stop (WAI-ARIA's `group` role, not
             * `radiogroup`, doesn't call for roving tabindex/arrow-key navigation the
             * way a radio group does).
             */
            "scarlet-checkbox-group": LocalJSX.IntrinsicElements["scarlet-checkbox-group"] & JSXBase.HTMLAttributes<HTMLScarletCheckboxGroupElement>;
            /**
             * A small, dismissible tag — for an active filter, a selected option, an
             * added item in a multi-value input. Not a `<scarlet-badge>` with a close
             * button bolted on: unlike a badge, this is meant to be interactive.
             */
            "scarlet-chip": LocalJSX.IntrinsicElements["scarlet-chip"] & JSXBase.HTMLAttributes<HTMLScarletChipElement>;
            /**
             * A searchable select — type to filter a list of options, following the
             * WAI-ARIA 1.2 "editable combobox with list autocomplete" pattern. Unlike
             * `scarlet-menu`/`scarlet-date-picker`'s popovers, DOM focus never leaves
             * the text input while the list is open: the highlighted option is tracked
             * with `aria-activedescendant` instead of moving real focus button-to-
             * button, so the user can keep typing without ever losing their place.
             * Known limitation: like `scarlet-tooltip`/`scarlet-date-picker`/
             * `scarlet-menu`, positioning is plain CSS anchored to the field — it
             * doesn't flip or reposition to stay in the viewport.
             */
            "scarlet-combobox": LocalJSX.IntrinsicElements["scarlet-combobox"] & JSXBase.HTMLAttributes<HTMLScarletComboboxElement>;
            /**
             * A centered, max-width content wrapper — the outermost layout primitive
             * for page sections.
             */
            "scarlet-container": LocalJSX.IntrinsicElements["scarlet-container"] & JSXBase.HTMLAttributes<HTMLScarletContainerElement>;
            /**
             * A `DD/MM/AAAA` date input (typing, masking and calendar validation shared
             * with `scarlet-input-date`) plus a calendar popover for picking a date
             * visually — a button next to the field opens a month grid; arrow keys move
             * within it (Home/End jump to the start/end of the week, PageUp/PageDown
             * change month, Shift+PageUp/PageDown change year), Enter/Space picks the
             * focused day, Escape closes and returns focus to the toggle button.
             * Known limitations:
             * - The popover closes on Escape, on picking a day, or on clicking outside
             *   it — not on Tab-ing past its last focusable element. A keyboard user who
             *   tabs out instead of pressing Escape will move focus past the component
             *   with the panel still visually open.
             * - Like `scarlet-tooltip`, positioning is plain CSS anchored to the host —
             *   it never flips or shifts to stay in the viewport. It's shrunk to never
             *   exceed the viewport's own width, so it can't run off the right edge of
             *   the *screen*, but placing the field near that edge can still overflow
             *   past its own container.
             */
            "scarlet-date-picker": LocalJSX.IntrinsicElements["scarlet-date-picker"] & JSXBase.HTMLAttributes<HTMLScarletDatePickerElement>;
            /**
             * Two `DD/MM/AAAA` fields (start/end) sharing one calendar popover for
             * picking both ends of a range — built the same way `scarlet-date-picker`
             * is (same masking/validation per field, same popover mechanics), doubled.
             * See that component's own doc comment for the shared known limitations
             * (no Tab-out auto-close, no viewport flip).
             * Picking works the classic two-click way: the first day clicked becomes
             * the start (clearing any previous end); the second becomes the end and
             * closes the popover — unless it's *before* the start, in which case it
             * becomes the new start instead and the popover stays open for the end.
             */
            "scarlet-date-range-picker": LocalJSX.IntrinsicElements["scarlet-date-range-picker"] & JSXBase.HTMLAttributes<HTMLScarletDateRangePickerElement>;
            /**
             * A visual separator between sections of content, with an optional centered label.
             */
            "scarlet-divider": LocalJSX.IntrinsicElements["scarlet-divider"] & JSXBase.HTMLAttributes<HTMLScarletDividerElement>;
            /**
             * A panel that slides in from an edge of the screen — the mobile-friendlier
             * sibling of `scarlet-modal` for a filter panel, a form, a details view.
             * Built the same way `scarlet-modal` is, on the native `<dialog>` element,
             * for the same free focus trapping/top-layer stacking/Escape handling — see
             * that component's own comments for the mechanics shared here (focus
             * restore, the cancelable `scarletClose`, `show()`/`hide()`).
             * `size` sets the dimension along the axis the drawer slides on: width for
             * `left`/`right`, height for `top`/`bottom`.
             */
            "scarlet-drawer": LocalJSX.IntrinsicElements["scarlet-drawer"] & JSXBase.HTMLAttributes<HTMLScarletDrawerElement>;
            /**
             * A placeholder for a list, search result, or section with nothing to
             * show — an icon, a heading, an optional description, and an optional
             * action (e.g. "Limpar filtros" or "Criar o primeiro item").
             */
            "scarlet-empty-state": LocalJSX.IntrinsicElements["scarlet-empty-state"] & JSXBase.HTMLAttributes<HTMLScarletEmptyStateElement>;
            /**
             * A drag-and-drop file picker, backed by a real (visually hidden) native
             * `<input type="file">` — so it's a real form control and works with no JS
             * at all for the "click to browse" path; only the drag-and-drop layer is
             * enhancement on top.
             */
            "scarlet-file-upload": LocalJSX.IntrinsicElements["scarlet-file-upload"] & JSXBase.HTMLAttributes<HTMLScarletFileUploadElement>;
            /**
             * A CSS grid layout primitive with equal-width columns and token-based
             * gaps. Pair with `<scarlet-grid-item>` to span multiple columns/rows.
             * `columns` can change per breakpoint via `columnsSm`/`columnsMd`/
             * `columnsLg`/`columnsXl` — each one only takes effect from its breakpoint
             * up and, left unset, falls back to the next smaller breakpoint that *is*
             * set (mobile-first cascade), down to `columns` itself. E.g. `columns={1}
             * columns-md={3}` renders 1 column below 768px and 3 from 768px up, with
             * no separate `sm`/`lg`/`xl` value needed.
             */
            "scarlet-grid": LocalJSX.IntrinsicElements["scarlet-grid"] & JSXBase.HTMLAttributes<HTMLScarletGridElement>;
            /**
             * A cell inside a `<scarlet-grid>` that can span multiple columns/rows.
             * `colSpan`/`rowSpan` can change per breakpoint via `colSpanSm`/`colSpanMd`/
             * `colSpanLg`/`colSpanXl` (and the `rowSpan` equivalents) — the classic "full
             * row on mobile, a couple columns on desktop" pattern is
             * `col-span="{columns}" col-span-md="2"`. Each one only takes effect from
             * its breakpoint up and, left unset, falls back to the next smaller
             * breakpoint that *is* set (mobile-first cascade, same as `<scarlet-grid>`'s
             * own `columns`/`columnsSm`/etc.), down to `colSpan`/`rowSpan` themselves.
             */
            "scarlet-grid-item": LocalJSX.IntrinsicElements["scarlet-grid-item"] & JSXBase.HTMLAttributes<HTMLScarletGridItemElement>;
            /**
             * A semantic heading (`<h1>`–`<h6>`) with an independently controllable
             * visual size — document structure (`level`) and visual hierarchy
             * (`variant`) don't have to match one-to-one.
             */
            "scarlet-heading": LocalJSX.IntrinsicElements["scarlet-heading"] & JSXBase.HTMLAttributes<HTMLScarletHeadingElement>;
            /**
             * Renders an icon from the design system's shared built-in set. Icons are
             * stroke-based and inherit `color`/`font-size` from their context, so they
             * line up with surrounding text and other components by default.
             */
            "scarlet-icon": LocalJSX.IntrinsicElements["scarlet-icon"] & JSXBase.HTMLAttributes<HTMLScarletIconElement>;
            /**
             * A labeled text input with helper/error text and built-in accessibility wiring.
             */
            "scarlet-input": LocalJSX.IntrinsicElements["scarlet-input"] & JSXBase.HTMLAttributes<HTMLScarletInputElement>;
            /**
             * A Brazilian postal code (CEP) input — formats as `01310-100`. This
             * component only formats the value; looking up the matching address (e.g.
             * via ViaCEP) is the consuming app's responsibility — listen for
             * `scarletChange` and call your own API with `getRawValue()`.
             */
            "scarlet-input-cep": LocalJSX.IntrinsicElements["scarlet-input-cep"] & JSXBase.HTMLAttributes<HTMLScarletInputCepElement>;
            /**
             * A credit card number input — formats in groups of 4 (or Amex's 4-6-5 /
             * Diners' 4-6-4 grouping once that brand is detected from the card's
             * BIN/IIN prefix), with a Luhn checksum validation on blur.
             */
            "scarlet-input-credit-card": LocalJSX.IntrinsicElements["scarlet-input-credit-card"] & JSXBase.HTMLAttributes<HTMLScarletInputCreditCardElement>;
            /**
             * A monetary input that formats digits as currency growing from the right
             * (like a card machine) — typing "1234" produces "R$ 12,34". Unlike the
             * other masked inputs, `scarletInput`/`scarletChange` emit the plain
             * **numeric** amount (e.g. `12.34`), not the formatted string — that's
             * almost always what you actually want from a money field. Read the
             * formatted text itself via `value`.
             */
            "scarlet-input-currency": LocalJSX.IntrinsicElements["scarlet-input-currency"] & JSXBase.HTMLAttributes<HTMLScarletInputCurrencyElement>;
            /**
             * A `DD/MM/AAAA` date input. Unlike a plain positional mask, `validate`
             * checks it's a *real* calendar date (rejects `31/02/2026`, honors leap
             * years) on blur, not just that 8 digits were typed.
             */
            "scarlet-input-date": LocalJSX.IntrinsicElements["scarlet-input-date"] & JSXBase.HTMLAttributes<HTMLScarletInputDateElement>;
            /**
             * A single "CPF/CNPJ" field that auto-detects and formats whichever
             * document type is being typed (11 digits → CPF `123.456.789-01`, 12+ →
             * CNPJ `12.345.678/0001-90`) — the common pattern for a field that accepts
             * either an individual or a company.
             */
            "scarlet-input-document": LocalJSX.IntrinsicElements["scarlet-input-document"] & JSXBase.HTMLAttributes<HTMLScarletInputDocumentElement>;
            /**
             * A Brazilian vehicle plate input — formats as the old `ABC-1234` pattern
             * or the newer Mercosul `ABC1D23` pattern, detected automatically from
             * whether a letter or digit lands in the 5th character.
             */
            "scarlet-input-license-plate": LocalJSX.IntrinsicElements["scarlet-input-license-plate"] & JSXBase.HTMLAttributes<HTMLScarletInputLicensePlateElement>;
            /**
             * A percentage input that formats digits growing from the right, like
             * `scarlet-input-currency` — typing "1234" produces "12,34%". Emits the
             * plain numeric percentage (e.g. `12.34`) via `scarletInput`/`scarletChange`.
             */
            "scarlet-input-percentage": LocalJSX.IntrinsicElements["scarlet-input-percentage"] & JSXBase.HTMLAttributes<HTMLScarletInputPercentageElement>;
            /**
             * A Brazilian phone number input — formats as `(11) 91234-5678` (mobile,
             * 11 digits) or `(11) 1234-5678` (landline, 10 digits), switching
             * automatically as the user types.
             */
            "scarlet-input-phone": LocalJSX.IntrinsicElements["scarlet-input-phone"] & JSXBase.HTMLAttributes<HTMLScarletInputPhoneElement>;
            /**
             * A styled inline text link — for a link inside a sentence/paragraph.
             * `scarlet-button variant="link"` is the button-semantics equivalent (an
             * action styled like a link); this is the reverse, a real `<a>` styled like
             * one. `target="_blank"` automatically gets `rel="noopener noreferrer"`
             * (unless `rel` is set explicitly) and a small external-link icon.
             */
            "scarlet-link": LocalJSX.IntrinsicElements["scarlet-link"] & JSXBase.HTMLAttributes<HTMLScarletLinkElement>;
            /**
             * A dropdown menu of actions opened from an arbitrary trigger element (the
             * `trigger` slot — typically an icon button like "⋮"), following the
             * WAI-ARIA menu button pattern: the trigger gets `aria-haspopup="menu"`/
             * `aria-expanded` (set directly on whatever element is slotted in), the
             * popover is `role="menu"` with `role="menuitem"` buttons, arrow keys move
             * a roving tab stop within the list, Home/End jump to the first/last enabled
             * item, Escape closes and returns focus to the trigger. Enter/Space opening
             * the menu is *not* handled here — the trigger must be a real, natively
             * activatable element (a `<button>` or `<scarlet-button>`); its own native
             * click already opens the menu, and re-handling those keys here would fire
             * twice (open, then immediately close again from the resulting click).
             * Known limitation: like `scarlet-tooltip`/`scarlet-date-picker`,
             * positioning is plain CSS anchored to the trigger — it doesn't flip or
             * reposition to stay in the viewport, and it doesn't close on Tab-ing past
             * its last item.
             */
            "scarlet-menu": LocalJSX.IntrinsicElements["scarlet-menu"] & JSXBase.HTMLAttributes<HTMLScarletMenuElement>;
            /**
             * A modal dialog built on the native `<dialog>` element, which provides
             * focus trapping, top-layer stacking and Escape handling for free.
             */
            "scarlet-modal": LocalJSX.IntrinsicElements["scarlet-modal"] & JSXBase.HTMLAttributes<HTMLScarletModalElement>;
            /**
             * A numeric input with decrement/increment buttons — for a quantity field,
             * not a general-purpose text field that happens to hold numbers (that's
             * `scarlet-input type="number"`). Stays within `min`/`max` on every path:
             * the buttons, typing, and blur all clamp.
             */
            "scarlet-number-input": LocalJSX.IntrinsicElements["scarlet-number-input"] & JSXBase.HTMLAttributes<HTMLScarletNumberInputElement>;
            /**
             * Page number navigation, following the WAI-ARIA pattern of a `<nav>`
             * landmark wrapping a list of buttons — the current page is a real button
             * (not a link, since this component doesn't own routing) marked
             * `aria-current="page"`. Collapses distant page numbers into an ellipsis
             * once `totalPages` is large, always keeping the first page, the last page,
             * and `siblingCount` pages on each side of the current one visible.
             */
            "scarlet-pagination": LocalJSX.IntrinsicElements["scarlet-pagination"] & JSXBase.HTMLAttributes<HTMLScarletPaginationElement>;
            /**
             * A generic overlay anchored to a trigger element (the `trigger` slot),
             * with arbitrary content in the default slot — the shared primitive
             * `scarlet-menu`/`scarlet-date-picker`/`scarlet-combobox` each built their
             * own narrower version of for their specific popovers. Use this one
             * directly for anything that doesn't need a menu list or a calendar grid:
             * a rich tooltip, a filter panel, a confirmation prompt.
             * `trigger="click"` (the default) toggles on click, closes on Escape or a
             * click outside, and sets `aria-haspopup`/`aria-expanded` on whatever's
             * slotted as the trigger. `trigger="hover"` opens/closes on mouse enter/
             * leave instead, with no click/Escape handling — matching how a hover
             * tooltip behaves, not a dialog.
             * Known limitation: like `scarlet-tooltip`/`scarlet-menu`, positioning is
             * plain CSS anchored to the trigger — it doesn't flip or reposition to stay
             * in the viewport.
             */
            "scarlet-popover": LocalJSX.IntrinsicElements["scarlet-popover"] & JSXBase.HTMLAttributes<HTMLScarletPopoverElement>;
            /**
             * A determinate progress bar (0–100%) — for a file upload, a multi-step
             * form, anything with a real, known completion percentage. For an
             * indeterminate "something's happening" state, use `scarlet-spinner`.
             */
            "scarlet-progress": LocalJSX.IntrinsicElements["scarlet-progress"] & JSXBase.HTMLAttributes<HTMLScarletProgressElement>;
            /**
             * A single radio option. Use inside a `<scarlet-radio-group>` for mutually
             * exclusive selection — standalone, it behaves like an isolated toggle.
             */
            "scarlet-radio": LocalJSX.IntrinsicElements["scarlet-radio"] & JSXBase.HTMLAttributes<HTMLScarletRadioElement>;
            /**
             * Groups a set of `<scarlet-radio>` options and enforces single selection —
             * native browser radio grouping does not cross shadow-DOM boundaries, so
             * this component coordinates `checked`/`name`/`disabled` across its children.
             */
            "scarlet-radio-group": LocalJSX.IntrinsicElements["scarlet-radio-group"] & JSXBase.HTMLAttributes<HTMLScarletRadioGroupElement>;
            /**
             * A labeled native `<select>` dropdown with helper/error text and built-in
             * accessibility wiring. Options are passed as a `options` property (an
             * array), not slotted — native `<select>` cannot reliably project slotted
             * `<option>` elements into its picker UI across browsers.
             */
            "scarlet-select": LocalJSX.IntrinsicElements["scarlet-select"] & JSXBase.HTMLAttributes<HTMLScarletSelectElement>;
            /**
             * A loading placeholder shape — swap it in for content that hasn't arrived
             * yet (a card, an avatar, a paragraph). Purely decorative (`aria-hidden`):
             * announcing the *loading* state itself (e.g. `aria-busy` on the section
             * being replaced) is the consumer's responsibility, not this component's.
             */
            "scarlet-skeleton": LocalJSX.IntrinsicElements["scarlet-skeleton"] & JSXBase.HTMLAttributes<HTMLScarletSkeletonElement>;
            /**
             * A loading indicator — `variant="circle"` for the generic spinner (the
             * same shape `scarlet-button`'s own `loading` state uses), `variant="logo"`
             * for the Scarlet mark itself pulsing, e.g. for a full-page loading state.
             * `role="status"` with `label` as its accessible name, so assistive tech
             * announces the loading state without needing separate visible text.
             */
            "scarlet-spinner": LocalJSX.IntrinsicElements["scarlet-spinner"] & JSXBase.HTMLAttributes<HTMLScarletSpinnerElement>;
            /**
             * A flexbox layout primitive for stacking or rowing children with
             * consistent, token-based spacing.
             * `direction` can change per breakpoint via `directionSm`/`directionMd`/
             * `directionLg`/`directionXl` — the classic "stacked on mobile, row on
             * desktop" pattern is `direction="column" direction-md="row"`. Each one
             * only takes effect from its breakpoint up and, left unset, falls back to
             * the next smaller breakpoint that *is* set (mobile-first cascade), down
             * to `direction` itself.
             */
            "scarlet-stack": LocalJSX.IntrinsicElements["scarlet-stack"] & JSXBase.HTMLAttributes<HTMLScarletStackElement>;
            /**
             * A single labeled metric — a number/value, its label, and an optional
             * change indicator (e.g. "+12%" with an up/down arrow) for a dashboard.
             */
            "scarlet-stat": LocalJSX.IntrinsicElements["scarlet-stat"] & JSXBase.HTMLAttributes<HTMLScarletStatElement>;
            /**
             * A toggle switch for binary on/off settings.
             */
            "scarlet-switch": LocalJSX.IntrinsicElements["scarlet-switch"] & JSXBase.HTMLAttributes<HTMLScarletSwitchElement>;
            /**
             * A data table: sortable columns, optional row selection (checkboxes),
             * empty/loading states, and a horizontally-scrolling wrapper so a wide
             * table never breaks the surrounding layout on a narrow viewport (same
             * pattern `scarlet-tabs` uses for its tab list).
             * Cell content is `String(row[column.key])` by default — good enough for
             * plain text/numbers, but there's no per-cell slot system (that needs a
             * stable id per row and gets complex fast). For anything richer — a
             * `scarlet-badge` status, a formatted currency, a truncated link — pass
             * `formatCell`, a plain function `(row, column) => string` set as a JS
             * property (like `columns`/`rows` themselves, it isn't parseable from an
             * HTML attribute).
             * Sorting is handled internally using a locale-aware comparator (numeric
             * for `number` values, `localeCompare` otherwise) — good for typical
             * text/number columns without any setup. `scarletSort` still fires on every
             * click if a consumer wants to replace that with their own comparator (e.g.
             * sorting a formatted date column by its real underlying timestamp) by
             * reassigning `rows` themselves in response.
             */
            "scarlet-table": LocalJSX.IntrinsicElements["scarlet-table"] & JSXBase.HTMLAttributes<HTMLScarletTableElement>;
            /**
             * A set of tabs with associated panels. Panel content is projected via a
             * named slot per item, matching `item.value`.
             */
            "scarlet-tabs": LocalJSX.IntrinsicElements["scarlet-tabs"] & JSXBase.HTMLAttributes<HTMLScarletTabsElement>;
            /**
             * Body copy with the design system's text scale, color tokens and
             * single-line truncation — the general-purpose counterpart to `scarlet-heading`.
             */
            "scarlet-text": LocalJSX.IntrinsicElements["scarlet-text"] & JSXBase.HTMLAttributes<HTMLScarletTextElement>;
            /**
             * A multi-line labeled text input with helper/error text and built-in accessibility wiring.
             */
            "scarlet-textarea": LocalJSX.IntrinsicElements["scarlet-textarea"] & JSXBase.HTMLAttributes<HTMLScarletTextareaElement>;
            /**
             * A vertical sequence of events — an order's status history, an activity
             * feed, a step-by-step audit trail.
             */
            "scarlet-timeline": LocalJSX.IntrinsicElements["scarlet-timeline"] & JSXBase.HTMLAttributes<HTMLScarletTimelineElement>;
            /**
             * A self-dismissing status message, fixed to a corner of the viewport.
             * For multiple simultaneous toasts, render several instances inside a
             * consumer-owned stacking container (this component does not manage a queue).
             */
            "scarlet-toast": LocalJSX.IntrinsicElements["scarlet-toast"] & JSXBase.HTMLAttributes<HTMLScarletToastElement>;
            /**
             * A horizontal bar for grouping related actions (buttons, a search field,
             * an icon menu) — `role="toolbar"` with token-based gap, distinct from
             * `scarlet-stack` in intent (actions, not arbitrary layout) even though
             * the underlying flex mechanics are similar.
             */
            "scarlet-toolbar": LocalJSX.IntrinsicElements["scarlet-toolbar"] & JSXBase.HTMLAttributes<HTMLScarletToolbarElement>;
            /**
             * A text hint shown next to its trigger on hover/focus. Positioning is
             * plain CSS anchored to the host — it does not flip to stay in the
             * viewport, so pick a `placement` that has room to render.
             */
            "scarlet-tooltip": LocalJSX.IntrinsicElements["scarlet-tooltip"] & JSXBase.HTMLAttributes<HTMLScarletTooltipElement>;
        }
    }
}
